**Daily POA – Wed 2026‑03‑04**

1. **08:30–09:00 — Documentation catch-up**

- Paste yesterday’s notes into `discovery.md`, `design.md`, `decisions.md`, `testing.md`, and `build.md` (use the snippets we drafted).
- Double-check that the inline marketplace map + Sponsored Display deferral are clearly referenced in design/decisions.

2. **09:00–10:00 — Final QA confirmation**

- Re-run the three validation checks (profile→marketplace, unknown audit, parity vs. prod) just to confirm nothing drifted overnight.
- Take screenshots or CSV snippets for the ticket/hand-off.

3. **10:00–11:00 — Prep production deployment plan**

- Outline the exact steps to update `WAREHOUSE_SOURCE.MARKETING_FCT_ACTIVITY` / `WAREHOUSE.MARKETING_FCT_ACTIVITY` (and any dependent views) with the new SQL.
- Document rollback (recreate the old view) and note that Sponsored Display remains commented out until approved.

4. **11:00–12:00 — Coordinate platform dim refresh**

- Confirm with the owner when `MARKETING_DIM_PLATFORM_NAME` / `MARKETING_DIM_PLATFORM` will be redeployed to include the Display literal so test + prod stay aligned.
- If needed, capture a reminder to swap the literal back in once the dim hits prod.

5. **13:00–14:00 — Ticket/status update**

- Write a concise update for the Jira/AirTable ticket summarizing: inline marketplace map, Sponsored Display deferred, QA results, pending deploy.
- Flag the outstanding item (Brazil canonical row missing in shared dim) as a future improvement.

6. **14:00–16:00 — Deployment (if approved)**

- Apply the SQL to `WAREHOUSE_SOURCE` and `WAREHOUSE`, re-run the QA queries in prod, and notify stakeholders once validated.
- If deploy is deferred, use the slot to draft the monitoring query that alerts when a new Amazon Ads profile appears (so we can add it to the inline map quickly).