### 1. Information (do now)

**Action**: Update `information.md` with the new stakeholder comment (profile ID → country/currency mapping) and the branch name you created (`feature/paulrussell/GP-169/…`). Note the target (DEV only) and dependencies (Marketing fact, Amazon Ads source tables).

---

### 2. Discovery (start now)

Drop your EDA queries + findings into `discovery.md`, organized by theme:

1. **Inventory checks**

- `SHOW SCHEMAS/TABLES` commands → confirm Amazon connector tables under `TEST_DG1_GEP.AMAZON_ADS`.
- Marketing fact views (`WAREHOUSE_SOURCE` / `WAREHOUSE`) enumerated.

2. **Source currency integrity**

- Null-currency query results (all zero).
- Currency distribution query (USD vs CAD only so far). Add a note that MX/BR aren’t surfacing yet—tie to Justin’s profile list.

3. **Marketplace dimension context**

- Key structure (product/vendor/location/company derived from `SHARED_DIM_MARKETPLACE`).
- Sample marketplace IDs you pulled (`Amazon US`, `Amazon CA` → `NULL_NULL_-1_163`, etc.).

4. **New stakeholder mapping**

- Paste Justin’s profileID/country/currency list with a note: “Use profile_id to map to marketplace deterministically.”

---

### 3. Design (next)

Outline the updated approach:

- **Inputs**: Sponsored Brands/Products Display reports with `PROFILEID`.
- **Mapping**: New `MARKETPLACE_PROFILE_MAP` (profile_id → marketplace_id, marketplace_name, country, currency).
- **Logic**:

1. Extend `BASE_METRICS` to select `PROFILEID`.
2. Replace current currency-only `MARKETPLACE_MAP` with profile-based join.
3. Fallback behavior when a profile_id isn’t mapped (default to `UNKNOWN_MARKETPLACE`, emit QA alert).

- **Validation**: Compare aggregated spend per profile_id vs marketplace to prove 1:1 mapping.

Include a diagram or bullet flow showing data path: Amazon reports → base metrics (with profile_id) → profile map → marketing fact.

---

### 4. Build (after design sign-off)

Turn the design into a step-by-step checklist in `build.md`:

1. Update `marketing_fct_activity.sql`:

- Project `PROFILEID` in each adapter.
- Create new `MARKETPLACE_PROFILE_MAP` CTE (hard-coded list for now).
- Join to map `MARKETPLACE_ID`.

2. Update tests doc (`notes/tests/fct_campaign_conversions.md` equivalent for marketing fact if one exists; otherwise create one).
3. Run local syntax check / preview query.
4. Commit/push branch.

---

### 5. Testing (while/after build)

Add the following planned queries to `testing.md`:

- **Profile coverage**: `SELECT profileid, campaignbudgetcurrencycode, COUNT(*) FROM … GROUP BY …` to confirm each profile only shows one currency.
- **Marketplace join validation**: After code change, `SELECT marketplace_name, currency_code, SUM(cost)` from the fact to ensure US spend only lands on Amazon US, etc.
- **Null detection**: Query for rows where `marketplace_id = 'UNKNOWN_MARKETPLACE'` should be zero.

Document actual results once run.

---

### 6. Decisions (ongoing)

Whenever Steven/Justin gives guidance (e.g., “use profile_id, no aggregation”), record it here with dates:

- 2026‑02‑27 — Justin confirmed profile_id ↔ marketplace mapping; use profile map instead of currency.
- 2026‑02‑27 — Branch name created; working in DEV only.

---

### Plan to finish today

1. **Finalize discovery doc** (15 min) – paste current SQL snippets + findings.
2. **Draft design note** (30 min) – outline new profile-map CTE + join changes.
3. **Implement SQL changes** (60 min) – update `marketing_fct_activity.sql` per design.
4. **Write tests note** (15 min) – list queries and expected outputs.
5. **Run quick validation queries** (30 min) – confirm profile/currency mapping, preview updated view.
6. **Update ticket & branch** (15 min) – push changes, summarize in DE-006 ticket.