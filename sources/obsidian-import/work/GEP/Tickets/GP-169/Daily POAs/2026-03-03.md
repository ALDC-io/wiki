**Daily POA – Tue 2026‑03‑03**

**Goal:** Finalize the marketplace-profile SQL implementation (marketing_fct_activity + dependencies) before EOD.

---

### 09:15–10:00 — Confirm canonical data coverage

1. **Inventory canonical rows** in `WAREHOUSE.SHARED_DIM_MARKETPLACE`

- Query for `PRODUCT_ID IS NULL AND DEFAULT_VENDOR IS NULL AND LOCATION_ID = -1`.
- Verify which `COMPANY_ID`s exist (expect 163/166/168; confirm whether 174 is missing).

2. **Decide remediation for gaps**

- If Brazil (174) is absent, either:  
    a) raise a quick data request to add the canonical row, **or**  
    b) patch `MARKETPLACE_PROFILE_MAP` with a temporary hard-coded `MARKETPLACE_ID` for BR (document as tech debt).
- Capture outcome + owner in the ticket.

### 10:00–11:30 — Finalize SQL changes

1. **Update `MARKETPLACE_PROFILE_MAP`** as needed after the canonical-row review (e.g., add temporary BR literal if we can’t get the dim updated today).
2. **Propagate the same CTE** to any mirrored view (both `WAREHOUSE_SOURCE` and `WAREHOUSE` definitions already in the file). Double-check there’s no duplicate logic elsewhere (older adapters, QA views).
3. **Inline comments**: note why we derive `COMPANY_ID` via `SPLIT_PART`, and call out the fallback behavior for markets without canonical rows.

### 11:30–13:00 — QA + validation

1. **Unit QA queries**

- `SELECT PROFILE_ID, MARKETPLACE_ID, MARKETPLACE_NAME FROM MARKETPLACE_PROFILE_MAP;` → ensure every profile resolves, and unknowns match expectations.
- Aggregation sanity: `SELECT MARKETPLACE_ID, MARKETPLACE_NAME, SUM(COST) FROM WAREHOUSE_TEST_PAUL.MARKETING_FCT_ACTIVITY_GP169 GROUP BY 1,2;` (expect one marketplace per profile).
- `SELECT * FROM ... WHERE MARKETPLACE_ID = 'UNKNOWN_MARKETPLACE';` → should be zero after fixes except for intentionally uncovered profiles.

2. **Edge-case checks**

- Confirm Sponsored Display rows also pick up the mapping (no missing joins).
- Verify fact row counts unchanged aside from marketplace columns.

### 13:00–14:00 — Documentation + ticket grooming

1. **Update DE-006/GP-169 design notes** with the new canonical-join approach and the derived company-ID rationale.
2. **Ticket comments**

- Summarize what changed, QA evidence, and any remaining follow-ups (e.g., “Need canonical Brazil row in shared dim”).
- Note deploy dependencies (dimension update, QA queries) so reviewers know the sequence.

### 14:00–16:00 — Coordination & sign-off

1. **If Brazil canonical row is outstanding**, follow up with data owner (Justin?) to get ETA or approval for hard-coded interim value.
2. **Peer review / walkthrough**

- Share the updated SQL snippet + QA results with stakeholders (Slack or meeting) to confirm it meets the requirement of deterministic marketplace attribution.

3. **Finalize release plan**

- Once approved, prep whatever deployment step is needed (dbt run, Snowflake view refresh).
- Schedule the actual apply after sign-off; ensure rollback plan documented (revert to previous view definition).

### 16:00 EOD — Status update

- Post final summary: code merged/applied, QA results, outstanding risks (if any), next steps for tomorrow.

Let me know if you want calendar holds or a quick sync added for the canonical-row decision.