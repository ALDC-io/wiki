---
tags: [standup, daily]
date: 2026-09-08
---

# Stand-Up -- 2026-09-08

*Reporting on Friday 2026-09-04. Jira issue types are **not marked** — no Atlassian connector this
session, and types are never guessed.*

**Friday (2026-09-04) — done:**

- **FU92-427 (Trade Desk outage) — root cause PROVEN, and three of the ticket's own premises refuted.**
  The static `TTD-Auth` token has a **365-day lifespan** and expired 2026-08-05T20:54:29Z; three
  independent instruments agree (Cosmos `_ts`, the token's embedded issuance field, the installing
  git commit). Also killed: the "hard deadline ~09-04" (loss is **rolling, not a cliff** — our own
  warehouse shows a 272-day backdate succeeding), "unrecoverable" (the identical 2025 outage
  recovered **12/12** partition dates), and "86 failures" (actually **1808/1824**). Gap at the
  consumer layer is 29 days of **absent rows, not false zeros**; exposure ~**$4,558** (DERIVED).
- **FU92-427 — recovery proven armed, no manual re-queue needed.** Direct read shows **60 partitions
  still `active`/`in_queue`**, with a positive control (941 partitions on the same template *are*
  retired, so the probe discriminates). They self-retry the moment a token works.
- **FU92-431 (Microsoft Ads) — root cause PROVEN, and the obvious fix is the wrong one.** The Function
  App presents a **March-expired client secret** while `Eclipse Secret 2026-05-22` (valid to 2028)
  sits unused — **90 error traces across 90 days, exactly one per day**. Rotating the token alone
  repeats May's failure. Fix `MICROSOFT_ADS_CLIENT_SECRET` in `aldcprodfnapf921c01` first.
- **FU92-428 / FU92-429 — code raised, both PRs still open.** `ALDC-io/workflows#33` (POST route +
  the proven `split_ids("")` fix) and `ALDC-io/flight-check#37` (send ids in a body); merge
  workflows-first. 429's half is **groundwork only** — flag defaults False, no UI, zero flights
  affected. Verified today: both **OPEN, unmerged**, last touched 09-04 16:04Z.
- **Two unreported outages found and ticketed on the way** — FU92-431, and **FU92-432 Viant Reach &
  Frequency dead since 2026-04-25 (four months)** on a Snowflake network-policy IP block at ~553
  failures/day. FU92-416's three-month-old question also answered (365 days).
- **Safety finding filed:** Eclipse `work_template_delete` calls `warehouse_reset` → `DROP TABLE` —
  the intuitive way to repoint a `schedule_id` would destroy **14,665,156 rows / 935 days**. Use
  `work_template_update` only.
- **GP-318 / GP-319 (Marketing Model design research, for Nicholas) — the model he would review is
  two months short, and it is our cut not the source.** Read-only DAX across both TEST models:
  2026-08 and 2026-09 order lines are present in the Daily model (118,308 and 11,663 rows) and
  **absent from the Marketing Model** (`2d8587b5`, max month 2026-07; 3,429,863 rows / 92 days vs
  Daily's 3,556,263 / 127). Both refreshed the same day, so this is a **pinned `RangeEnd` in the
  Marketing model's own M**, not an upstream gap. Negative control (fabricated object) raised on
  both models — ALL_PASS. Evidence `docs/evidence/navira-sales-repair/marketing_model_orderline_window.json`.
- **GP-318 Batch-1 — the field-list strip verified at the browse surface.** `$SYSTEM.MDSCHEMA_HIERARCHIES`
  over MSOLAP, the route an Excel field list actually reads: all **10** hidden columns confirmed
  **no longer offered**, and the control column (`MAP Min ASINs Floor[Min ASINs]`, the client's
  slicer) **still offered**. 517 browse rows, positive and negative controls both pass. ⚠ The probe
  file itself records that this is **not a substitute** for a cold Analyze-in-Excel connect plus a
  field-list screenshot — that render check is still outstanding.
- **Evidence + wiki committed and pushed.** `feature/fu92-427-trade-desk-evidence` @ `6db5240`
  (FINDINGS, rotation runbook, 4 verification probes for 427; FINDINGS for 431); wiki `14fa948`.

**Ready to deploy (root cause proven, fix written):**

- **FU92-431 Microsoft Ads — UNBLOCKED, deploy today.** Step 1 is a single prod app-setting change:
  point `MICROSOFT_ADS_CLIENT_SECRET` in `aldcprodfnapf921c01` at the existing `Eclipse Secret
  2026-05-22` (valid to 2028, hint `0R.`, already in the vault). Nothing is missing — the valid
  secret exists and is identified. **Do this before re-minting the token**, or it dies again in 90
  days exactly as it did in May. Steps 2-5 (re-mint the refresh token via the consent flow, write to
  Cosmos, verify the 07:00 UTC run goes to zero severity-3, recover 18 partition dates 08-16 → 09-02)
  need real-time Fusion coordination.
- **FU92-428 — two PRs ready to merge**, `ALDC-io/workflows#33` **then** `ALDC-io/flight-check#37`
  (workflows keeps the GET form deliberately, so it is backward-compatible). Verified today: both
  still OPEN. ⚠ Merging is not the same as fixed — the 124-flight render on job `7d9b5cc1` has still
  never been run, so I would merge and *then* validate, not call it done.
- **FU92-427 Trade Desk — runbook written end to end, gated on one credential.**
  `R1-ROTATION-RUNBOOK.md` is complete: rollback capture before the write, a field-by-field re-read
  diff, and the two traps that make a 200 meaningless (`work_connection_update` upserts a
  wrong-shaped payload and returns 200 having written nothing). **Step 0 is the only blocker** — the
  Dashlane triage. Everything after it is ~1 hour, and recovery is automatic (60 partitions still
  queued, they self-retry).

**Not ready — diagnosed only, no fix written:**

- **FU92-432 Viant** (Snowflake network-policy IP block, dead four months) — cause identified, no
  runbook yet. **FU92-429** — blocked on two product decisions, and its code is groundwork that
  changes behaviour for zero flights. The two hardening follow-ups (`critical: []` alert routing;
  `function_app.py:295-301` swallowing every exception, which is *why* 431 was silent for five
  months) are unticketed.

**Deployed Friday: nothing.** All probes read-only; no prod mutation anywhere.

**Still investigating / open:**

- **FU92-427** — Dashlane has **4 Trade Desk records**, untriaged. Need type (partner-portal login vs
  bare API token) and last-modified date; anything after 2025-08-05 means someone rotated and never
  deployed. That triage decides one-hour fix vs Fusion92 provisioning request for partner `frqci16`.
- **FU92-428** — the render has **never been validated**; the 124-flight export on job `7d9b5cc1` is
  still unrun, so the ticket's consumer-layer criteria are unmet. The 2,048-byte length mechanism is
  **LIKELY, not PROVEN** — the failing request was never captured, and larger jobs (`51bc3edc`, 207
  flights) were never reported failing.
- **FU92-429 — blocked on two product decisions (Karen/Vlad).** Needs approval rather than an
  engineering call because the flag changes what a buyer-facing spend number *means*: someone can
  mark reported spend as hand-entered, and nothing today distinguishes that from platform-measured
  spend.
  1. **Who may set it?** Roles on the account are `admin`, `media_ops`, `user`, `buyer`.
     Recommend **`admin` + `media_ops`**, following the existing
     `adminRoles = ["admin","media_ops"]` precedent in `F92StatusTransitions.tsx` — a convention
     already in the product rather than a new rule. Excludes `buyer`: a buyer editing the spend
     figure they are reported on is the audit hole.
  2. **Should the Flight Report / Flight Export mark manually-entered numbers?** Recommend **yes**
     — a buyer-facing workbook that cannot distinguish measured from hand-entered spend is the
     ambiguity that produces a dispute later. This is the one with real downstream consequence.

  **Two further spec questions are already closed by the implementation** — worth stating, because
  they were the ones flagged as changing the data model. It is a **read-time filter**, so (a)
  nothing is discarded when the flag goes on (direct rows stay in Snowflake, simply ignored), and
  (b) it is **instantly reversible** — clear the flag and direct matching resumes on the next
  request, no migration, no restore path.

  Until both are answered the ticket should not read as delivered: the backend is merged-ready and
  **inert** (flag defaults `False`, no UI), so it changes behaviour for **zero** flights and the
  reporter still sees "API Direct".
- **Unticketed, found Friday** — Flight Check ×2 dead (`firebase_v1`), and a pre-existing **January
  2026 gap** (6 days Performance / 12 Unique Impressions, ~590 futile retries each, permanently lost,
  never reported).

**Today (2026-09-08) — starting:**

- **Prefect connector work.** ⚠ No ticket number found — the newest handoff
  (`boot-prompts/connector-fleet-handoff-2026-08-19.md`) is ticketless, and its `next:` is still
  unrun: aggregate `GET :8765/api/connectors` by tier / migration_status / credential_status
  (works unauthenticated), then test the zombie-run hypothesis before any Tier 0 code work.
- **FU92-431** is the one thing that could be fixed and verified today without waiting on anyone.

**Blockers / watch:**

- 🔴 **The live prod Trade Desk token is committed in plaintext** to
  `clients/FUSION_92/eclipse/connections/trade_desk_my_reports.json` — repo value == prod value,
  verified by salted hash. Exposed regardless of how 427 resolves; the replacement must not be committed.
- ⛔ **Atlassian MCP was 404 all Friday** (HTTP+SSE retired after 2026-06-30). The **FU92-428 and
  FU92-429 Jira comments are still drafts on disk** and need pasting: `docs/drafts/fu92-428-pr-comment.md`,
  `docs/drafts/fu92-429-groundwork-comment.md`. 427/431/432/416 comments *were* posted before it died.
- **Detection worked, response did not** — 31 Slack alerts fired per template, the first ~25 seconds
  after the first 401, and both templates sit outside `critical: []`. Four-month Viant outage is the
  same shape.
