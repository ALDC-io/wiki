---
tags: [standup, daily]
date: 2026-09-04
---

# Stand-Up -- 2026-09-04

*Covers 2026-09-03 afternoon + today. Yesterday's standup already reported 09-02 afternoon and the 09-03 GP-318 morning work.*

> Jira issue types are **not marked** in this report — the Atlassian connector returned 404 all session, so types could not be read and were not guessed.

## Done

- **FU92-427 — Trade Desk outage root cause proven, and the ticket's own deadline refuted.** The
  static token has a **365-day lifespan** and expired 2026-08-05T20:54:29Z — three independent
  instruments agree (Cosmos `_ts`, the token's embedded issuance field, the git commit that
  installed it), and the anniversary falls inside the last-success/first-401 bracket. The stated
  ~09-04 "hard deadline" came from one hedged template comment whose *measured* successor was
  silently reverted by a merge; our own warehouse shows a **272-day backdate succeeding** in 2024.
  Loss is rolling, not a cliff. Commit `0794114`, **investigation only — nothing deployed**.
- **FU92-427 — the identical outage already happened in 2025 and recovered 100%.** 12 of 12
  partition dates hit a 401 and every one later completed, `schedule_id` unchanged. Direct read of
  `work_partition` confirms **60 partitions are still active and queued** (positive control: 941
  on the same template *are* retired, so the probe discriminates) — they self-retry the moment a
  token works, no manual re-queue.
- **FU92-431 — Microsoft Ads root cause proven, and the obvious fix is the wrong one.** The Function
  App presents a **March-expired client secret** while `Eclipse Secret 2026-05-22` (valid to 2028)
  sits unused; the timer runs daily and fails daily, **90 errors across 90 days**. A new secret was
  created 2026-05-22T19:17:37Z and a token minted **59 seconds later** — but the app setting was
  never updated, so the token rotted unused until it died of 90-day inactivity. Rotating the token
  alone repeats May's failure.
- **FU92-428 / FU92-429 pushed and in review** — `ALDC-io/workflows#33` (POST route + the proven
  `split_ids("")` fix, plus 429's backend) and `ALDC-io/flight-check#37` (send ids in a body).
  Merge order is workflows-first; the server keeps the GET form deliberately so it is
  backward-compatible. **429's half is labelled groundwork, not a client fix** — the flag defaults
  False with no UI, so it changes behaviour for zero flights.
- **Two new tickets filed for unreported outages found on the way** — Microsoft Ads (FU92-431) and
  Viant Reach & Frequency, dead since **2026-04-25, four months**, on a Snowflake network-policy IP
  block at ~553 failures/day. FU92-416's three-month-old question also answered (365 days) and Viant
  measured healthy, refuting that ticket's pairing.
- **GP-254 / GP-330 (separate session, 09-03) — Lectric feed audit corrected two premises.** The
  feed is not an API test: **5,310 order lines · $2,822,972 gross · 2024-05-31 → 2026-09-03**. Only
  column difference TEST vs PROD is `ENTITY_CODE`; delivery options split out to new ticket GP-330,
  awaiting Justin's reply on one-off extract vs persistent share.

## Today

- **Check Dashlane for a Trade Desk portal credential.** Key Vault, all repos and the wiki vault are
  now *measured* empty — this is the last place, and it decides whether FU92-427 is a one-hour fix
  or a Fusion92 provisioning request. The client email is drafted and ready either way.
- **FU92-429 — two product decisions, then build Layer 5.** Who may set the flag, and whether the
  buyer-facing export marks manual entries. Q1/Q2 from the spec turned out to be **already answered
  by the implementation** (it is a read-time filter, so nothing is discarded and it is instantly
  reversible).
- **FU92-428 — validate the render.** The 124-flight export on job `7d9b5cc1` has still never been
  run against the fix; the ticket's consumer-layer criteria are unmet.

## Blockers / watch

- ⛔ **The Atlassian MCP connector is down — 404, all session.** Distinct from the transient
  classifier block noted on 09-03: this is a server-not-found from the endpoint, retried twice. The
  HTTP+SSE transport was retired after 2026-06-30 and needs repointing to
  `https://mcp.atlassian.com/v1/mcp`. Consequence: the FU92-429 comment is a **draft on disk**, not
  a posted comment, and **no Jira-dependent blocker below could be re-verified today.**
- ⛔ **FU92-427 cannot start without a credential.** No Trade Desk token in Azure Key Vault (both
  vaults enumerated), any repo, or the wiki vault — all three measured today with proven-live
  instruments. No code has ever called the auth endpoint; every token was minted by hand by a
  since-offboarded colleague.
- 🔴 **The live prod Trade Desk token is committed in plaintext to the `clients` repo** — repo value
  and prod value verified identical by salted hash. Treat as exposed regardless of how FU92-427
  resolves; the replacement must not be committed.
- **FU92-428's length mechanism is LIKELY, not PROVEN** — the failing request was never captured.
  The 2,048-byte cap is the *IIS* default; on Linux it is ~16 KB and the payload would pass. Jobs
  `51bc3edc` (207 flights) and `9f96e977` (177) are larger and were never reported as failing — if
  one exports fine, that half of the diagnosis dies. The `split_ids("")` half is proven by execution
  and stands either way.
- **`aldc-launchpad` has 202 uncommitted files across 8 concurrent sessions** (measured by the
  pre-commit hook today). Everything this session staged was path-explicit, but any `git add -A`
  from any session would sweep the lot. A worktree per workstream would remove the hazard.
- **Carried from yesterday, not re-verified** (Jira unreachable): ALDC-1064 unauthenticated export,
  SALES-014 unowned, GP-318's 11 repeated-total columns.
