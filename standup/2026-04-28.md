---
tags: [standup, daily]
date: 2026-04-28
---

# Stand-Up — 2026-04-28

## What I Did Today

### Navira Roadmap — Heather Ad-Spend Cross-Reference
- Reviewed Heather Tabor's email listing Navira's current ad spend channels (AMZ US/UK/CA PPC, Google Ads Amazon + DTC, Meta Ads DTC, Target+)
- Cross-referenced against the [[navira|Navira roadmap]] — confirmed 5 of 7 channels already scoped in Phase 1A
- Verified via Snowflake raw tables that **AMZ US PPC** (708K rows) and **AMZ CA PPC** (68K rows) are already live and current
- Confirmed **AMZ UK PPC is not flowing** — no GBP profile data in raw `AMAZON_ADS` tables; auth gap, not build gap
- Traced the Amazon Ads OAuth flow end-to-end to identify exactly what Navira needs to do (authorize UK profile via ALDC OAuth URL)
- Drafted email to Lori with roadmap impact summary, OAuth link for Navira, and ALDC action items

### Navira Roadmap Updates (Wiki)
- Moved **Target+** from Phase 1B to Phase 1A — confirmed active spend
- Reduced Phase 1B scope from 4 to 3 interfaces (TikTok, Creator Connections, Email not confirmed as current spend)
- Added **Blockers** section to Navira README with UK PPC auth action item
- Resolved open question Q2 in Phase 1A (Amazon PPC connector status confirmed)
- Added AMZ US PPC and CA PPC to Production (already live) table with row counts

### Prefect Phase 0 — Sprint 0A (E2E Proven)
- ExchangeRates connector: full end-to-end pipeline completed (API → DataFrame → Parquet → Azure blob → Snowflake staging → merge → COMBINED → CURRENT)
- 1,032 rows (USD, 3 days) landed in `QA_DG1_ALDC_QA.EXCHANGE_RATES`
- Created `/prefect-connector` skill with automated credential retrieval + table snapshot safety
- Discovered `short_code` overloaded as module path (blocks rename without G3 fix)
- Discovered snapshot naming collides with framework schema scanner
- Aligned Phase 0 boot prompts with Heather's roadmap update

### Eclipse PR #75 — YoY Metrics Card Tooltip (Deployed to Production)
- Investigated PR #75 (`feature/metrics-card-yoy` → `eclipse-2.1`): adds Year over Year comparison as a violet tooltip section on Metrics Card visuals
- Confirmed 2-step deploy process from workflow files: GitHub Actions "Deploy to Azure App Service" (branch `eclipse-2.1`, environment "Production (Eclipse 2.1)") → stage slot, then Azure Portal slot swap
- Found exact app service names: `aldcprodwbapeclipse1c01` (frontend), `aldcprodwbapcore1c01` (backend); both in Production 2 / `aldcprodrsgp1c`
- Retrieved staging CosmosDB credentials from core_api app settings; created `paul.russell@aldc.io` user in staging `user` container for testing
- Verified YoY tooltip working in GEP account (metrics cards with "Show Previous Period" + timeframe < 1 year)
- Swapped backend slot first, then frontend — both production slots confirmed live (backend 00:59 UTC, frontend 01:00 UTC)
- Companion backend PR (`core_api` #233) also merged and deployed

## Blockers

- **AMZ UK PPC** — blocked on Navira authorizing UK Amazon Advertising profile via ALDC OAuth URL. Email drafted to Lori; awaiting send and response.
- **core_api deploy workflow** — all GitHub Actions deploy workflows in `core_api` repo are dummy placeholders. Real deploy mechanism unknown; needs clarification.

## Plan for Tomorrow

- Continue Phase 0 framework hardening: G2 (pytest framework) → G5 (Prefect logging) → G3 (account auto-discovery)
- Fix snapshot schema naming (move to `_PREFECT_SNAPSHOTS` schema)
- Follow up on UK PPC OAuth if email sent
