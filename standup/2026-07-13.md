---
tags: [standup, daily]
date: 2026-07-13
---

# Stand-Up -- 2026-07-13

## What I Did

### Friday (2026-07-10)

- **Navira marketing dashboard — redesign initiative kicked off.** Direction set to evolve the dashboard toward the client prototype: ROAS-led Overview (feature-flagged), design-system restyle, Live/Coming/Gated states (SmartScout next, pending creds), and fixes to prototype metric-law bugs (no cross-channel sum, ACoS≠TACoS; restore UK/marketplace grain + true-TACoS). **Phase 0 foundation** merged to `feature/insite-calendar-brand-reach-region` (design system + chart kit, Playwright 42/42, data-wiring getTraffic/getPlatformDetail/campaign-names); **359 vitest** + build green. Hub: `aldc-launchpad/navira-dashboard-redesign/` + `boot-prompts/navira-marketing-dashboard-redesign.md`.

### Sunday (2026-07-12)

- **Navira dashboard — prototype card rebuild.** Rebuilt all four views (Brand / Product / Campaign / Ad-Platform) to the prototype card-per-entity layout: header + 8-tile KPI scorecard + one unified expandable section table over a shared 40-column set, plus global Customize-Columns + Download-CSV. Local-only, **no deploy**.
- **Full-scaffold, honest-placeholder approach.** Unsourced columns/sections render as honest **Coming** (Amazon Ads / Attribution / DSP / Traffic) or **Gated** (SmartScout) — never fabricated — so the dashboard doubles as the data-enablement roadmap. Ad Platform keeps real Brands/Campaigns/Top-products + a Marketplaces section preserving Amazon UK grain.
- **New engine + verification.** Added `src/lib/insite-table.tsx`, `InsiteColumnsContext`, `CardSectionTable`, `InsiteCard`; removed orphaned EntityCard + RegionView. Commits `c1a1704` + `978b65a`. Gate green: tsc + **363 vitest** + build + **50 Playwright** + lint; warehouse consumer render verified.

## Plan for Today (2026-07-13)

- **ALDC-622 — Eclipse user-permissions / delete-user failure (Heather / Navira).** New ticket (created today, In Progress, Medium). Navira admin Muhammad Masud hits "Something Went Wrong" when deleting a departed user in the Eclipse v2 web app. Target user's email has now been provided by Navira. Investigating the delete path (frontend → core_api → data store), confirming the **correct identity store at the consumer layer** (Cosmos `user` vs Postgres `app_user` — the two-store trap), then safely removing the departed user with rollback capture, and fixing the underlying delete failure so admins can self-serve.
- **Navira dashboard — fill the scaffold source-by-source.** Next enablement step is SmartScout first (per the card-rebuild boot prompt).

## Blockers / Watch

- **Two-store trap** — must confirm which store prod actually reads before deleting (don't infer from matching values); `app_user` and `user` have near-identical shapes.
