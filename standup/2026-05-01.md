---
tags: [standup, daily]
date: 2026-05-01
---

# Stand-Up — 2026-05-01

## What I Did Today

### GP-243 — Validate Existing Prefect Server (Done ✅)

Validated Brayden's self-hosted Prefect Server at `https://prefect.analyticlabs.io`. All resources healthy:

| Resource | State |
|---|---|
| App Service `aldcprodwbapprefectserver1c01` | Running |
| Postgres `aldcprodpgdbconnector1c01` | Ready (v17) |
| Container App `aldcprodctapprefectworkpool1c01` | Running |
| Work Pool `azure-aci-production` | READY |

Key findings:
- **Auth:** HTTP Basic auth (`PREFECT_API_AUTH_STRING` on the App Service). Not API key — this was previously undocumented.
- **Active deployment:** `Exchange Rates - ALDC_LIBRARY` actively polling as of today. 12 blocks registered.
- **Block persistence:** Verified by creating and reading back a test block, then deleting it.
- **Server API version:** `0.8.4` — compatible with local Prefect 3.6.24.
- **Subscription correction:** Resources are in **Production 2**, not QA subscription as previously documented. Wiki corrected.
- **No issues found.** No expired TLS certs, no dead services, no stale version. Server is ready for GP-247 (repo fork) and GP-217 (CI/CD).

Documentation deliverables from GP-243:
- Fixed subscription error in `wiki/entities/tools/prefect.md` (QA → Production 2)
- Created `wiki/processes/deployment/prefect-connector-deployment.md` — full guide for local dev, Azure deploy, and environment switching
- Filled in the `deployed` and `verified` stages of `/prefect-connector` skill (were placeholders with no actual commands)

### GP-221 — Amazon UK PPC OAuth (Blocked)

Justin's auth code (`ANtGwPgtwMVByjymJsjf`) had expired by the time it arrived. Drafted reply to Lori requesting Justin click the auth link again and send the code immediately. Waiting on response.

---

## Blockers

- **GP-221 (Amazon UK PPC)** — Waiting on Justin/Lori for a fresh OAuth authorization code. No code change needed — just the exchange.
- **GP-238/GP-239 (Windsor auth)** — Waiting on Navira to click Windsor auth links (Google Ads + Facebook Ads).

---

### GP-199 — ASIN Brand Campaign Attribution (Design Approved ✅)

Justin Shuster confirmed the full design via email. All open questions resolved:

**Allocation formula (confirmed):**
1. **Ad → ASINs:** Equal split of daily ad spend across ASINs in the ad creative
2. **ASIN → Orders:** Weight each ASIN's allocated spend across orders by unit ratio
3. **Zero-sale ASINs:** Keep their equal share as UNK (unattributed spend)

Justin explicitly confirmed: "do not weigh the initial split... evenly distribute across ASINs first."

**Historical data:** Option A — no backfill. New logic applies from go-live date forward. Existing SB data stays at brand-level grain.

**Non-ASIN SB ads:** Fall back to current brand-level split. Verify during implementation whether this case exists.

Design locked in. Jira comment #27008 posted. Ready for implementation. Boot prompt written in GP-199.md for a Sonnet execution session using `/gep-feature`.

### GP-247 — Fork connector repo → prefect-connectors (Done ✅)

Created `ALDC-io/prefect-connectors` from `operation-fiasco`. 3-branch model (`main`/`uat`/`development`) with branch protection + Docker publish CI. Three infrastructure bugs found and fixed:

1. **Windows entrypoint path** — `account.py:build_deployments()` normalized `\\` → `/` for Linux ACI
2. **WebSockets disabled on App Service** — enabled `webSocketsEnabled` on `aldcprodwbapprefectserver1c01`
3. **Prefect events WebSocket 403** — `sitecustomize.py` in Docker image returns `NullEventsClient` when `PREFECT_API_AUTH_STRING` is set (Azure App Service proxy blocks WebSocket auth headers)

GHCR credentials block updated from `brayden-marshall` (offboarded) to `russell94paul`. Snowflake block `snowflake-qa-aldc-qa` updated from CosmosDB. Smoke test `astute-waxbill` **COMPLETED** in ACI — data landed in `QA_DG1_ALDC_QA.EXCHANGE_RATES.*` on og35375. GP-247 → Done in Jira.

## Plan for Next Session

- **GP-248** — Snowflake environment isolation (create `QA_DG1_GEP_PREFECT`, `TEST_DG1_GEP_PREFECT`, `PROD_DG1_GEP_PREFECT` + service accounts + PBI workspaces)
- **GP-219** — Sellercloud connector migration (first production connector on Prefect)
- **GP-199** — Begin implementation (design approved, boot prompt ready)
- **GP-221** — Exchange token the moment a new code arrives (standing by)
