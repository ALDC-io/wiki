---
tags: [standup, daily]
date: 2026-05-02
---

# Stand-Up — 2026-05-02

## What I Did Today

### GP-248 — Prefect Snowflake Environment Isolation (Done ✅)

Created 3 isolated Snowflake databases, 3 dedicated service accounts, and 2 PBI workspaces for the Prefect connector migration.

- **Non-prod (og35375):** `QA_DG1_GEP_PREFECT` + `TEST_DG1_GEP_PREFECT` cloned from `TEST_DG1_GEP`. Service accounts `QA_DG1_PREFECT_SVC_DA8904DB` + `TEST_DG1_PREFECT_SVC_DA8904DB` with grants only to Prefect databases.
- **Prod (wj66376):** `PROD_DG1_GEP_PREFECT` created empty with WAREHOUSE, WAREHOUSE_SOURCE, REPORT_COMMON schemas. Service account `PROD_DG1_PREFECT_SVC_DA8904DB`. Required `PROD_DG1_CORE_ADMIN` (ACCOUNTADMIN) — `paulrussell` only has SYSADMIN on prod, insufficient for CREATE ROLE/USER.
- **PBI:** "GEP Prefect QA" + "GEP Prefect Test" workspaces created via PowerShell (`MicrosoftPowerBIMgmt` module).
- **Framework:** Renamed `CORE_SVC` → `PREFECT_SVC` in `account_registry.py` — clearer intent since these are Prefect-only accounts.
- **Tooling:** `provision_gep_prefect.py` (main provisioning), `check_grants.py`, `reset_passwords.py`, `create_pbi_workspaces.ps1`.
- Passwords stored in `vault/infra-credentials.md`.

## Blockers

- **GP-221 (Amazon UK PPC)** — Waiting on Justin to create UK Advertising profile at advertising.amazon.co.uk.
- **GP-238/GP-239 (Windsor auth)** — Waiting on Navira to click Windsor auth links (Google Ads + Facebook Ads).

### Workflow Automation — Option A Monitoring Layer (Done)

Built the monitoring and operational health automation layer for aldc-shipyard:

- **Analysis doc** — `workflow-analysis-current-vs-future.md`: 8 Mermaid diagrams mapping current vs target state, 27 gaps across Snowflake/PBI/Prefect, 3 implementation options compared.
- **monitor.py** — Data freshness (6 tables), task chain health, share integrity (11 objects). Three modes + JSON output.
- **flight_check.py** — Full operational health report replacing manual flight-check process. Combines all monitors + volume sanity + cross-env comparison + PBI refresh status.
- **deploy.py extensions** — `MONITORING.DEPLOY_LOG` table auto-created and recorded per deploy. Prod promotion gate requires prior test deploy (--force override).
- **validate.py extensions** — `--compare` flag for cross-env row count comparison.
- **Skill wiring** — Added §K Flight Check helper to `/gep-feature`, invoked at test-deployed and prod-deployed.
- **Scheduled agent** — Weekly "GEP Prod Weekly Flight Check" (Mondays 08:00 UTC, posts Jira checklist).
- **New skills** — `/session-close` (10-step end-of-session checklist, idempotent, self-improving) and `/session-open` (6-step context priming boot).

### GP-218 — QA/UAT/Prod Work Pools & Promotion Pipeline (Done ✅ — infra)

Split single `azure-aci-production` Work Pool into 3 environment-specific pools with dedicated workers, env vars, and branch-mapped Docker tags. Established the promotion pipeline: feature → development (QA) → uat (UAT) → main (Prod).

- **Work Pools:** `azure-aci-qa` (`:development` tag, `ENVIRONMENT_LEVEL=qa`), `azure-aci-uat` (`:uat`, `test`), `azure-aci-production` (`:main`, `prod`).
- **Snowflake blocks:** 3 credential blocks registered on Azure Prefect server (`snowflake-qa-gep-prefect`, `snowflake-test-gep-prefect`, `snowflake-prod-gep-prefect`).
- **Workers:** 2 new Container Apps (`aldcprodctapprefectwpqa1c01`, `aldcprodctapprefectwpuat1c01`) created via YAML config.
- **Framework fix:** `short_code="GEP"` → `"GEP_PREFECT"` in account.py so `warehouse_database_name` resolves correctly.
- **E2E:** Flow run `ultramarine-parakeet` COMPLETED on QA pool — ExchangeRates data in `QA_DG1_ALDC_QA`. Full GEP verification pending PR #1 merge.
- **PR:** `ALDC-io/prefect-connectors#1` (feature/paulrussell/gp-218/work-pool-config → development).
- **Architecture decision:** Option A (separate Container Apps per tier) for isolation at ~$25/month extra.
- **Wiki:** 6 pages updated + GP-218 ticket page created.

## Blockers

- **GP-221 (Amazon UK PPC)** — Waiting on Justin to create UK Advertising profile at advertising.amazon.co.uk.
- **GP-238/GP-239 (Windsor auth)** — Waiting on Navira to click Windsor auth links (Google Ads + Facebook Ads).

### GP-199 — ASIN Brand Campaign Attribution (Eclipse deploy in progress)

Continued implementation session. All SQL + templates written in previous session; this session advanced the Eclipse deploy sub-flow.

**Connector (dev-first path):**
- PR #119 merged to `development` in connector repo. `amazon_ads.py` + `"ad"` category handler + `/sb/v4/ads/list` endpoint live.
- GitHub Actions `docker-publish.yml` added to connector repo — auto-builds `agent-dcgeneral:development` on merge. Workflow fix applied (rsync instead of cp for build context).
- Workflow failing on missing `AGENT_CONFIG_TEST` secret — needs to be set from on-prem (`workstation-agent` at `192.168.31.210` not reachable remotely).

**SQL review + validation:**
- All 3 SQL files reviewed and confirmed correct.
- Bug fixed: validate manifest had `SNAPSHOT_DATE` (doesn't exist) → `VALID_FROM`.
- validate.py bug fixed: SQL errors on `warn_only: true` checks now warn rather than hard-fail (previously always hard-failed, bypassing warn_only flag).

**Q1 resolved:** Queried PROD directly — 1 VCPM/BRAND_IMPRESSION_SHARE campaign confirmed (inactive). Non-ASIN fallback union in `marketing_fct_activity.sql` is correct and necessary.

**New automation built:**
- `eclipse_deploy_templates.py` — upserts Eclipse template JSON files to CosmosDB via azure-cosmos SDK. Replaces manual UI flow.
- `gep-feature` skill updated: `dev-first` Eclipse sub-flow documented, Eclipse sandbox gap documented, `eclipse_deploy_templates.py` wired in.

**All GP-199 work committed** to clients repo feature branch (`07b4253f`) and aldc-shipyard main.

## Plan for Next Session

- **GP-199** — Set `AGENT_CONFIG_TEST` + `AGENT_CONFIG_PROD` secrets from on-prem → rerun GitHub Actions → Portainer redeploy test agents → run `eclipse_deploy_templates.py` → sandbox deploy + validate. Full boot prompt in `wiki/tickets/gep/GP-199.md`.
- **GP-218 follow-up** — Merge PR #1 to `development`, verify CI builds `:development` image, GEP E2E on QA pool.
- **GP-217** — Wire CI/CD pipeline: merge to `development` → Docker → GHCR → QA Work Pool.
- **GP-219** — Begin Sellercloud connector migration (first production connector on Prefect).
