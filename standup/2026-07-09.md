---
tags: [standup, daily]
date: 2026-07-09
---

# Stand-Up -- 2026-07-09

## What I Did (2026-07-08)

### GP-254 — Lectric Amazon: durable scheduled connector (Completed, TEST)

Agency (Heather) reported the tool's Lectric YTD was behind Seller Central. Root cause: Lectric's Amazon sales were a **one-time static CSV backfill** frozen at 2026-06-03 (no live connector) — June + July were missing. Fixed immediately last session; yesterday made it **durable and self-refreshing**.

- Stood up a scheduled `amazon_sellercentral_v1` all-orders connector in TEST — dedicated topic `AMAZON_LECTRIC` (own schema; separate seller account), `BY_LAST_UPDATE` report (self-heals on status change), US, daily. Deployed via a cloned Windsor Cosmos deploy; scoped one-off agent `gep-amazon-lectric-test` (executor container on the workstation, restart-on-boot).
- Confirmed the core_api **scoped-pick fix is live on the TEST slot** (pick isolates to the Lectric connection only; the real blocker turned out to be the "Small Evening Window" schedule block, not the pick-bug).
- Converted the Lectric fact from a static TABLE to a **hybrid VIEW** = live connector `UNION ALL` frozen history (history yields to connector → cancellations self-heal). Column-aware rewire handled connector schema drift (TEXT price/qty/tax → cast; absent `PROMOTION_IDS` → defaulted).
- Pipeline now fully automatic: connector → hybrid view → union view → `TASK_WAREHOUSE_ORDERLINE_7` (6am PT) → base table → PBI.
- **Validated 5/5 gates at the PBI consumer:** LECTRIC YTD-2026 **$463,892** (ties to Seller Central ~0.2%), lifetime $2,684,625, max date = today; **NAVIRA $123,888,491.56 byte-identical (zero regression).**
- Committed + pushed both repos; GP-254 updated.

## Blockers — need PRs merged (ask)

- **ALDC-io/clients #491** (Lectric Eclipse connector config) — **needs an org-admin merge/bypass.** CODEOWNERS lists only departed owners, so required review dead-locks (same issue as prior GEP PRs). Can Vlad / Mike approve-merge?
- **ALDC-io/aldc-launchpad #2** (deploy + rewire + validation tooling) — should merge normally; needs a review.

## Plan for Today

- **Get #491 + #2 merged** (see blockers).
- Verify the connector **self-refreshed overnight**: `SELECT MAX(TO_DATE(TO_TIMESTAMP_NTZ(PURCHASE_DATE))) FROM TEST_DG1_GEP.AMAZON_LECTRIC.CURRENT_REPORT_ALL_ORDERS;` — if the edge advanced, the daily scan is hands-off; if not, add a workstation scan cron.
- Send Heather the reconciliation reply ($463,892) → get sign-off, then drop the `_RB_*` rollback clones (keep `LECTRIC_HISTORY_STATIC` — it's load-bearing in the view).
- Prod promotion of the Lectric connector is separate/gated (needs prod Eclipse config + Fix A in prod core_api) — not today.
