---
tags: [ticket, aldc, core-api, bug, gep, navira, azure, config, observability, open]
aliases: [ALDC-1175, SALES DETAIL SYNC failure, azure_client_id improperly set]
sources: [https://analyticlabsdc.atlassian.net/browse/ALDC-1175, "Fwd: Routine Error: SALES DETAIL SYNC (jshuster@navira.io, 2026-09-08 22:49 UTC)"]
created: 2026-09-08
updated: 2026-09-08
---

# ALDC-1175 — core_api: one absent env var (`AZURE_CLIENT_ID`) fails EVERY route and is served as HTTP 200 "success"

**Type:** Bug | **Status:** 🔴 To Do (investigating) | **Priority:** High | **Project:** ALDC Scrum
**Sprint:** S11: DL Cortex Perf (ends 2026-09-14) | **Assignee:** Paul Russell
**Relates to:** [[ALDC-994]] (replace slot-swap promotion with a real staging environment)

## Problem

GEP/Navira's client-side `InsiteDaemon` scheduled routine **SALES DETAIL SYNC** failed at
**2026-09-08 17:46:34 EDT (21:46:34 UTC)** with an error that is **[[core_api]]'s own**, not the
client's. A single absent Azure app setting on a core_api instance disables *every* API route on
that worker, and the failure is returned to callers as **HTTP 200 "success"**.

- **Reported by:** Justin Shuster (jshuster@navira.io) — forwarded the routine's automated mail
  (`insite.services@navira.io` → `lcerrito@navira.io`, `jshuster@navira.io`)
- **Environment:** prod (which core_api instance is **not yet proven** — see Open Questions)
- **Reproducible:** ❌ not currently — 25/25 clean probes 1h14m later. Do **not** read as healed.
- **Blocking:** the client's sales-detail data feed; scope of staleness not yet established

## ⭐ The corrected premise — this one matters

The report was first assessed as **"I don't think this is us."** **That is refuted.** The error
string is emitted by our own code:

| Where | What |
|---|---|
| `core_api/v1/func_common.py:92` | `f"Environment Variable {variable_key} improperly set. Value required or improper value assigned."` |
| `core_api/v1/func_common.py:98` | `environment_error = f"Environment variable setup failed - {e}"` |

Grepped every local repo: the string exists in **no repo except `core_api`**. The client's daemon
received our error text as a **return value** (note the f-string `{sub_sales_detail_extract}` in
their traceback) and wrapped it in its own exception — which is exactly what made it look
client-side.

**Lesson:** an error surfaced *by* a client's system is not evidence the error *belongs to* it.
Grep for the string before assigning blame. See [[core_api]] gotchas.

## Symptoms

```
Routine SALES DETAIL SYNC failed with error: Error while getting sales detail extract -
Environment variable setup failed - Environment Variable azure_client_id improperly set.
Value required or improper value assigned.

  File "/home/ubuntu/InsiteDaemon/utils/routine.py", line 15, in run
  File "/home/ubuntu/InsiteDaemon/utils/execution_checks.py", line 204, in wrapper
  File "/home/ubuntu/InsiteDaemon/eclipse_sync/extracts.py", line 388, in sync_sales_detail_extract
    raise Exception(f"Error while getting sales detail extract - {sub_sales_detail_extract}")
```

## What we know (executed evidence)

### 1. The insertion-order signature narrows the fault to ONE variable

`func_common.py` populates a single dict then loops:

```python
for variable_key, variable_value in environment_variables.items():
    if variable_value == None:
        raise ValueError(f"Environment Variable {variable_key} improperly set. ...")
```

Python preserves insertion order, so **the error names the first absent variable.**
`azure_client_id` is key **#21** — therefore every nullable key before it *was populated*:

`cosmos_database, cosmos_hostname, cosmos_key, environment_level, environment_location,
environment_version, encryption_key, master_client_id, master_client_secret, pushover_token,
sys_storage_name, storage_sas1, twilio_sid, twilio_token, twilio_number, azure_subscription_id`

**This is not a cold container or an env wipe** — Cosmos, Twilio and Pushover were all set. It is a
config that lost *specifically* `AZURE_CLIENT_ID`. Keys **#22+** (`azure_tenant_id`,
`azure_client_secret`, `mailjet_*`, `azure_deployment_resource_group`, `GPT_KEY`, `GPT_ORG`) were
**never tested** — the loop short-circuits. They must be verified separately; their health is
UNKNOWN, not proven.

> This is a reusable diagnostic: any `func_common.py` env error tells you the **exact prefix of the
> dict that was healthy**, for free.

### 2. `== None` tests absence, not blankness

An empty string `""` **passes** the check. So the variable was genuinely **absent** from the process
environment. That rules out mechanisms which yield an empty value — notably `docker compose` with
`KEY: ${VAR}` and `VAR` unset, which resolves to `""`.

### 3. The blast radius is EVERY route, and it is sticky per process

`environment_error` is a **module-level global evaluated once at import**:

- `v1/__init__.py:272` — `environment_error = func_common.environment_error`
- `v1/__init__.py:302` — the **first statement in the request `try`, before any auth check or route
  dispatch** — `if environment_error: raise ValueError(environment_error)`

So while a worker process holds a bad config, **every function/option fails**, not just
sales-detail — and it stays broken for the life of that process. Only a restart re-imports.

### 4. A total config failure is served as HTTP 200 "success" ⚠

`v1/__init__.py:1244-1247`:

```python
except Exception as e:
    if environment_error:
        response_dict = func_common.form_response(
            f"{route_function}/{route_option}", 200, "success", f"success", environment_error
        )
```

Status **200**, literal string **"success"**, error in the payload. Two consequences:

1. It explains the client traceback's shape — they got a *string*, not an HTTP error.
2. **Any monitor watching status codes sees green** during a total outage. This is why a client
   email was the detection mechanism.

### 5. Legacy vs FastAPI optionality divergence

`core_api/api/__init__.py` (`GlobalConfig`, the FastAPI rewrite) declares
`azure_client_id: Optional[str] = None` — and likewise `pushover_token`, `twilio_*`, `gpt_key`,
`gpt_org`. The legacy `func_common.py` treats **all ~26 as mandatory**. `azure_client_id` is
load-bearing in only `route_setup.py:547,590,676` (`ClientSecretCredential`) and
`v1/keyvault_client.py`. So a peripheral Azure credential is a hard prerequisite for routes that
never touch Azure — the FastAPI path already models this correctly.

## Reproduction status — honest verdict

Working **credential-free** probe (the env gate is reached with no auth, `client authenticated: false`):

```
POST https://api.aldc.io/v1/account/list   body {}   content-type: application/json
```

At **2026-09-08 23:00:42 UTC** (1h14m after the client's failure), **25/25 probes returned
`environment_error` falsy.**

⚠ **Not read as "healed and fine."** 25 sequential requests likely hit one warm worker and
`environment_error` is per-process — a healthy *aggregate*, not a healthy *population*. Nor does it
establish that `api.aldc.io` is the host the daemon calls. Current state: **UNKNOWN, not ZERO.**

Occurrence frequency is **NOT-VISIBLE, not ZERO** — the routine mails the client's own addresses,
not ALDC, so our mailbox cannot record earlier occurrences.

## Likely causes (ranked — under investigation)

1. **Slot swap / stage↔prod config drift** — `deploy_az_webapp_container.yaml` deploys to
   `slot-name: stage`; settings not marked slot-sticky travel with a swap. Directly the risk
   [[ALDC-994]] exists to remove. ⚠ Must first confirm the prod **Function App** even has a `stage`
   slot — consumption-plan Function Apps often don't, which would kill this outright.
2. **A config edit / restart** at ~21:46 UTC — Azure Activity Log.
3. **Unresolved Key Vault reference** (`@Microsoft.KeyVault(...)`) — secret rotated/disabled, or the
   managed identity lost `get`.
4. **ALDC-1002** (`pr-255`, Snowflake key-pair auth via Key Vault, adds `v1/keyvault_client.py`) —
   but it is an **open PR branch** and `func_common.py`'s env list has *not* changed recently (last
   touch `dd26f2b`). The code's *requirement* did not change → points at the environment side.
5. **A different core_api instance entirely** — qa/test Function Apps, or the on-prem
   `core-api-2.1` container (`compose_on_prem.yaml`, `6661:80`, GitHub env "On-Prem"). All run the
   same code and emit the identical string.
6. **Multi-instance/worker skew** — only some instances hold the bad config.

## Open questions

1. **Which core_api instance served the 21:46:34 UTC failure?** `api.aldc.io` was probed because it
   is *a* core_api host, **not** because the route was proven. There are ≥3 Function Apps
   (prod/qa/test) plus a possible on-prem container, all capable of the same error string — the
   textbook matching-values trap. Decisive instrument: a Function App / Log Analytics request entry
   from the client's source IP at 21:46:34 UTC carrying the env error.
2. What is `SALES DETAIL SYNC` pulling, on what schedule, and **how much client data is stale?**
3. Are keys #22+ of the validation dict actually present? (The loop never tested them.)

## Key code locations

| What | Where |
|---|---|
| Error string origin | `core_api/v1/func_common.py:25-98` |
| Env gate (pre-auth, pre-dispatch) | `core_api/v1/__init__.py:272, 302` |
| HTTP-200 masking | `core_api/v1/__init__.py:1244-1247` |
| FastAPI config (vars correctly Optional) | `core_api/api/__init__.py` → `GlobalConfig` |
| `azure_client_id` consumers | `core_api/v1/route_setup.py:547,590,676`; `v1/keyvault_client.py` |
| Slot-based deploy | `core_api/.github/workflows/deploy_az_webapp_container.yaml` |
| On-prem deploy | `core_api/.github/workflows/deploy_on_premise.yaml`, `compose_on_prem.yaml` |
| Prod resources | [[core_api]] → Environments table (`aldcprodfnapcore1c01` / `aldcprodrsgp1c`) |

## Candidate remedies (not yet evidence-gated)

| | Remedy | Note |
|---|---|---|
| **A** | Restore/repair the `AZURE_CLIENT_ID` app setting on the affected instance | Restores service; does **not** prevent recurrence |
| **B** | Relax the blanket `if variable_value == None` loop so peripheral vars are optional, aligning legacy with `GlobalConfig` | Durable. ⚠ Must not silently downgrade a genuinely-required var into a later, worse runtime failure |
| **C** | Stop returning HTTP 200 "success" for a config failure | ⚠ Status-code changes break callers — the client's daemon parses the **body as a string** today |
| **D** | Alerting so a silent config failure is caught before a client emails us | The real detection gap; candidate for a [[vigil]]-style assurance pass |

## Acceptance criteria

1. The instance that served the failure is **named**, by discriminating check — not inferred from a
   matching error string.
2. `AZURE_CLIENT_ID` present and resolving on **every** instance/slot of **every** core_api
   deployment; stage↔prod app-setting name sets reconciled, slot-sticky flags stated.
3. Keys #22+ verified present.
4. A config failure no longer reports HTTP 200 "success" — or the status is deliberately retained
   for caller compatibility, the reason recorded, and an alert covers it instead.
5. Evidence: target proven, correct at consumer's layer, no-regression with numbers, rollback captured.
6. Client told what data was stale and for how long.

## Investigation Log

**2026-09-08** — Opened. Investigated with the `inquest` council (5 adversarial layers: symptom/scope,
Azure config, code path, consumer route, falsifier). Gates: freshness (local `core_api` on `pr-255`
`814a79f`, **not** deployed trunk); instrument (az CLI live as paul.russell@aldc.io); reproduction
(**does not currently reproduce** — see above). Findings 1-5 established by executed evidence. Route
to the failing instance **not yet proven** — explicitly flagged rather than assumed.

## See Also

- [[core_api]] — repo, runtime, per-env Function Apps
- [[ALDC-994]] — replace slot-swap promotion with a real staging environment (hypothesis #1's root)
- [[GEP]] / [[navira-marketing-dashboard]] — client context
- [[Azure]], [[eclipse-incident-response]]
