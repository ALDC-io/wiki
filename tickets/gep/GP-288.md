---
tags: [ticket, gep, navira, marketing, dashboard, insite, advertising, navira-roadmap]
aliases: [GP-288, Navira dashboard gap closure, prototype gap-closure, InSite gap closure]
sources: []
created: 2026-07-16
updated: 2026-07-19
---

# GP-288 — Navira dashboard prototype gap-closure (data-population lanes)

> Latest (2026-07-19): **Top-bar product filters BUILT (committed, held) + client coverage report produced.** The ASIN/SKU/Master-SKU top-bar filters shipped as ONE unified **"Find products" typeahead** (Product lens; searches ASIN + merchant SKU + master SKU + product name → single `?asin=`; full-catalogue index + server-side long-tail fetch). New TEST view `MARKETING_DIM_PRODUCT_ATTRS` (`GROUP BY ASIN`, 99.4% roster coverage, `NAVIRA_MKT_RO` granted). Teams + Kits stay honest Coming; SmartScout out of scope. Branch `feature/gp288-product-id-filters`, commit `fd2e4b2`; validated (unit 517/517, synthetic e2e 94/94, live warehouse e2e 2/2, tsc/lint clean); **committed, NOT deployed, NOT pushed** (deploy HELD). Also produced a **client-facing Delivery & Coverage report** (branded HTML→PDF, `navira-dashboard-redesign/reports/navira-coverage-report.html`) — exact per-row status of every feature/metric (34 Live / 8 Partial / 5 Awaiting-data / 2 Pending-decision / 2 Not-in-scope), gaps with honest reason + path forward, leak-scanned clean; **produced, not sent**. See "## 2026-07-19" below.
>
> Prior (2026-07-18, plan session): **Top-bar filter source-discovery done — build scoped, not yet built.** Read-only probes proved one dim (`DATA_SHARE.WAREHOUSE_SHARED_DIM_PRODUCT`, 99%+ Navira coverage) powers ASIN + SKU + Master-SKU + Teams. Scope settled: **build ASIN/SKU/Master-SKU** (Product lens, deduped dim join → new `MARKETING_DIM_PRODUCT_ATTRS` view + `NAVIRA_MKT_RO` grant); **Teams + Kits stay honest Coming** (source proven, but Teams client-gated on internal-name labels, Kits gated on component-rollup modeling). Build boot prompt: `aldc-launchpad/boot-prompts/navira-gp288-top-bar-filters-build.md`; plan `~/.claude/plans/gp288-top-bar-filters.md`. See "2026-07-18 (plan session)" below. On clean `main`; nothing committed this session.

> Prior (2026-07-17, session 3): **SHIPPED TO PROD + MERGED TO MAIN.** Fixed the P0 Overview blank-data bug (a channel Platform filter dropped the Amazon `totalSales` anchor → $0 sales / 0.0x MER / "—" ROAS; fix = Overview ignores the channel filter + hides the Platform control, matching the Trends convention), populated the Google→Amazon brand attribution columns, replaced the stale Meta→Amazon note with an honest walled-garden explainer, and built a **two-lane Playwright E2E suite** (synthetic functional 43/43 + warehouse data-integrity 7/7 on live TEST + nightly/manual CI). All consumer-verified on the live TEST warehouse, **deployed to prod** (`navira-mktg.analyticlabs.io`, Vercel CLI/ALDC), then the whole branch **merged to `main`** and **main CI is green** (after fixing two pre-existing failures: repo-wide prettier format + a flaky date-filter E2E). See "2026-07-17 (session 3)". Open: add the 7 `SNOWFLAKE_*` repo secrets so the nightly warehouse job can run. Prior (session 2): 4-lane parallel run (sparklines/bullets, connection-crash fix, brand attribution weave, campaign-attrs view), 471/471, undeployed.

> ⚠ The **prod Basic-Auth 401 is NOT a bug** — it's the intended gate on `navira-mktg.analyticlabs.io` (a curl returns 401; log in in a browser). The session-2 `navira-vercel-401-investigation.md` prompt is moot.

Home ticket for closing the remaining gap between the [[navira-marketing-dashboard|GEP InSite]] dashboard and the client prototype. Layout is already ≈1:1 (21/21 sections byte-faithful, 60-col table, metric-law bugs fixed); the remaining distance is **data population** — fill the honest Coming/Gated scaffold source-by-source — plus one UI feature. **Faithful population only** — never fabricate; unsourced fields stay honest "—"/Coming/Gated. **Status: Consulting/Design** (Sprint 7, assignee Paul, label `navira-roadmap`). All work is **TEST-only** on branch `feature/insite-calendar-brand-reach-region`; a single reviewed prod deploy is the gated end-step for the whole branch. Relates to [[GP-287]] (Amazon Attribution — shares the branch).

Punch-list + running state: `aldc-launchpad/boot-prompts/navira-dashboard-prototype-gap-closure.md`. Full record of dashboard changes lives on [[navira-marketing-dashboard]].

## 2026-07-20 — Lane C: durable marketing-attrs templates deployed to TEST + **cutover/repoint flagged as next step**

Promoted the two hand-made TEST attrs views into durable [[clients-repo]] warehouse templates so an Eclipse core rebuild can't wipe them (the durability follow-up flagged 2026-07-19). Branch `feature/paulrussell/gp288-marketing-attrs-templates` off `main`.

- **Two templates authored** (`GEP/snowflake/warehouse/`): `marketing_dim_product_attrs.sql` (sources `WAREHOUSE.SHARED_DIM_PRODUCT_BASE` — the sanctioned product-dim dependency, env-neutral bare ref, **not** the TEST-only `DATA_SHARE` object) + `marketing_dim_campaign_attrs.sql` (sources `PROD_DG1_GEP.AMAZON_ADS.CURRENT_*`, matching `marketing_dim_campaign.sql`). Both use the `WAREHOUSE_SOURCE`→passthrough-`WAREHOUSE` two-secure-view convention.
- **Deployed to `TEST_DG1_GEP`** (`WAREHOUSE_SOURCE` + `WAREHOUSE`) via vault `TEST_DG1_CORE_ADMIN`, evidence-gated: product view **1 row/ASIN** (14,038=14,038, no fan-out); **source-swap equivalence** vs the validated `DATA_SHARE`-based view = 100% ASIN overlap + SKUS byte-identical + MASTER_SKU 99.85% (21 ASINs pick a different valid `MODE` master); roster coverage **99.42%**; campaign view globally-unique key (149,825=149,825, no fan-out), full body compiles, portfolio names populate. `SELECT` granted `NAVIRA_MKT_RO`; role confirmed to hold `USAGE` on DB + `WAREHOUSE` schema → **grant is live, not inert**. Rollback = `DROP` (4 views; no prior objects at those names). PR merge needs the Vlad/Mike org-admin bypass ([[project_clients_codeowners_stale]]).
- **Live dashboard untouched** — still reads its `WAREHOUSE_TEST_GP226` hand-made views; the durable `WAREHOUSE` copies are staged for the cutover, not wired in piecemeal.

### ⭐ NEXT STEP (do once today's dashboard tweaks are locked + client-test-ready) — swap both marketing consumers off the interim `WAREHOUSE_TEST_GP226` staging schema onto durable `WAREHOUSE` objects
This is the **GP-225/226 marketing cutover**, not a one-line connection change. Two consumers repoint together:
1. **navira-marketing-dashboard** — default schema is `process.env.SNOWFLAKE_SCHEMA ?? "WAREHOUSE_TEST_GP226"` (`warehouse.ts:2144`). Repoint = set `SNOWFLAKE_SCHEMA=WAREHOUSE` + fix the 3 hardcoded `WAREHOUSE_TEST_GP226.` prefixes (the two ATTRS dims + `MARKETING_ATTRIBUTED_ROAS_BY_BRAND`).
2. **PBI Marketing Model `2d8587b5`** (the separate marketing model built to accompany the Daily Sales Data Model `66151728` — see [[project_daily_model_no_touch]]) — repoint its source to the same durable `WAREHOUSE` objects.

**Hard precondition:** every object the dashboard reads unqualified from the staging schema must first exist durably in `WAREHOUSE`: `MARKETING_EFFICIENCY`, `MARKETING_EFFICIENCY_PRODUCT`, `MARKETING_EFFICIENCY_MONTHLY`, `MARKETING_EFFICIENCY_GOOGLE_BRAND`, `MARKETING_FCT_ACTIVITY_UNIFIED`, `MARKETING_GOOGLE_SPEND_BY_DEST`, `MARKETING_META_SPEND_BY_DEST`, `MARKETING_ATTRIBUTED_SALES_BY_DEST`, `MARKETING_ATTRIBUTED_ROAS_BY_BRAND`, `MARKETING_DIM_AGENCY` (+ the two ATTRS dims — **done today**). The uncommitted `marketing_fct_activity_consolidated.sql` is the fact half of exactly this cutover. Evidence-gate each, then flip both consumers together. Boot prompt: `aldc-launchpad/boot-prompts/navira-gp225-marketing-cutover-repoint.md`.

## 2026-07-19 — Top-bar product filters BUILT (committed, held) + client-facing coverage report

Two deliverables on a fresh branch `feature/gp288-product-id-filters`, picker commit **`fd2e4b2`** — **committed, NOT deployed, NOT pushed** (deploy HELD, Paul's call). Full per-lane record on [[navira-marketing-dashboard]].

### (1) Unified "Find products" picker (commit `fd2e4b2`)
The 2026-07-18 plan-session scope (build ASIN/SKU/Master-SKU) was built — shipped as **ONE unified typeahead** on the Product lens rather than three separate boxes (and replacing an interim 3-input build).
- **One typeahead over the full catalogue** searches ASIN + merchant SKU + master SKU + product name at once; type-badged suggestions, removable chips, comma-paste; every pick resolves to the product **ASIN → single `?asin=` param**. Product lens only.
- **Two-halves architecture (both required):** a full-catalogue lightweight **index** shipped to the client for suggestions (`getProductIndex`) + a **server-side `?asin=` fetch** (`buildProductsQuery(…, asins)` drops the top-N cap, `WHERE UPPER(ASIN) IN …`, cap `ASIN_FILTER_MAX`) scoped to the Product view — so a long-tail pick outside the loaded top-2500 still renders (fixes the silent-empty trap).
- **Warehouse view (LIVE on TEST, evidence-gated):** `TEST_DG1_GEP.WAREHOUSE_TEST_GP226.MARKETING_DIM_PRODUCT_ATTRS` = `GROUP BY ASIN` over `DATA_SHARE.WAREHOUSE_SHARED_DIM_PRODUCT` → `ASIN, MODE(MASTER_SKU), LISTAGG(DISTINCT AMAZON_MERCHANT_SKU,'|')`. 1 row/ASIN, **99.4% roster coverage**, consumer-agg byte-identical with/without the join, `SELECT` granted `NAVIRA_MKT_RO`. Rollback = `DROP VIEW`. Evidence: `navira-marketing-dashboard/_gp288_product_attrs_evidence.md`.
- **Teams + Kits stay honest "Coming"** — source proven (Teams = `ACCOUNT_MANAGER`; Kits = `KI_`/`ISKIT`/`SKU_KITS_CSV`) but Teams client-gated on internal pod/person labels, Kits gated on the component→parent rollup model. SmartScout out of scope.
- **Validation (all green):** unit **517/517**, synthetic e2e **94/94**, live warehouse-lane e2e **2/2**, typecheck/lint/prettier clean. Key files: `ProductFinder.tsx`, `lib/product-search.ts`, `FilterBar.tsx`, `lib/filters.ts`, `providers/warehouse.ts`, `app/page.tsx` (cache **v15**).
- **Durability caveat / open follow-up:** the hand-made TEST view can be wiped by an Eclipse rebuild → promote it to a clients-repo **warehouse template** (evidence-gated) so a rebuild can't wipe it. Same lesson as `MARKETING_DIM_CAMPAIGN_ATTRS` and the FUTURE-grant recipe. Not urgent.

### (2) Client-facing Delivery & Coverage report
Produced a **client-facing report** (branded HTML → print-to-PDF) documenting, for the whole dashboard, what prototype features/metrics are **live** (with the real data behind them) vs **not yet delivered** — each gap with an honest reason + a suggested path forward. Authorized client-facing work (Paul asked); **produced ≠ sent** — distribution stays Paul's.
- **File:** `aldc-launchpad/navira-dashboard-redesign/reports/navira-coverage-report.html`. Plan: `~/.claude/plans/prancy-knitting-fern.md`.
- **Grounding:** reconciled the two specs (`prototype-vs-live-comparison.md` + `gap-matrix.md`) against the live build (INSITE column `availability`/`source`, `FilterBar`, view components) — status verified, not inherited from a possibly-stale spec.
- **Structure:** exec summary + a **visual coverage scorecard** (status tiles + per-area bars) + per-area feature-by-feature tables + "beyond the mockup" + "deliberately out of scope (SmartScout)" + an outstanding-items roadmap (awaiting-data / next-build / pending-decision).
- **Exact per-row counts (51 features/metrics itemised):** **34 Live · 8 Partial · 5 Awaiting-data · 2 Pending-decision · 2 Not-in-scope.**
- **Status vocabulary (spec bucket → client label):** BUILT/HAVE → "Live"; PARTIAL → "Live — partial coverage"; DATA-WORK/UNWIRED → "Planned — next build"; NOT-INGESTED (client data) → "Awaiting data access"; NOT-INGESTED (org metadata) → "Pending a decision"; OUT-OF-SCOPE (SmartScout) → "Not in scope (by decision)".
- **Honest gaps represented:** Lectric ad-spend $0 (awaiting Lectric ad-API access — client-side, not a build gap), Lectric COGS/margin ("—", never fabricated), keywords/NTB/DSP/inventory (awaiting data), Teams (pending a client-safe label — no internal names exposed), kits (needs the rollup model).
- **Client-safety gate:** leak-scanned clean — no Snowflake object/schema names, no Jira/ALDC ticket refs, no infra terms, no GEP pod/person names.

## 2026-07-17 (session 3) — SmartScout removal, Overview P0 fix, E2E suite → **DEPLOYED + MERGED**

The milestone session: the branch went from TEST-only to **live in prod and merged to `main`**. Commits `e372db7` (fixes + E2E), `95b16d9` (warehouse CI), `dc2a2fc` (locator fix), `7264fe5` (merge), `7f5614c` (format + date-filter). Gates throughout: tsc + lint + **475/475 vitest** + 43/43 synthetic E2E + 7/7 warehouse E2E + clean prod build.

- **SmartScout removed from scope** (commit `ed9dfc9`, pre-session): 25 files, column picker 60→42, **no "Gated" group remains** (SmartScout was the only gated source). Comparison doc `navira-dashboard-redesign/specs/prototype-vs-live-comparison.md`.
- **Thread #1 (P0) — Overview blank-data root cause + fix.** Symptom (Paul's QA): with a Platform filter of Google→Amazon, the Overview showed Amazon/Meta ROAS "—", **Total Sales $0**, MER 0.0x, TACoS 0.0% (Google 2.9x populated). **Root cause = filter interaction, NOT data staleness or the SmartScout removal:** `mapPlatforms` anchors the entity ground-truth `totalSales` **only on the Amazon channel row** (`warehouse.ts:768`), so any single non-Amazon Platform filter drops it → the portfolio tiles collapse to $0. Reproduced byte-for-byte at the consumer layer via the dev server (`?view=overview&platform=Google→Amazon`). **Fix (Option 1):** the portfolio Overview **ignores the channel filter** (Entity + Date still scope it) — `OverviewView.tsx` filters platforms with `{...filters, platform: null}` — and the **Platform control is hidden on the Overview**, matching the existing Trends-view convention (an inert/ignored filter is worse than an absent one). Verified: the broken URL now shows Amazon 5.2x / Google 2.9x / Meta 2.1x, MER 31.0x, **Total Sales $83.77M**; drill-down views' Platform filter unaffected; locked with a vitest regression + an E2E lock in both lanes.
- **Thread #2 — Google→Amazon brand attribution columns populated.** The Brand→Google→Amazon "Amazon Attribution by brand (14-day)" card rendered 9 brands but every table column was "—" (numbers only in prose). Now **Total cost ← matched Google Amazon spend, Sales ← attributed Amazon sales, ROAS ← derived**, via dedicated `attrSpend`/`attrSales` `InsiteRow` fields read by only those three columns (same pattern as `detailPageViews`/`campaignBudget`). Every walled-garden column (Impressions/Clicks/CTR/CPC/DPV/ACoS/Cost-Sale/Margin) stays honest "—" (Google `PRODUCT_ID = -1`, no impression/click/ASIN grain); an unmatched brand shows Sales only, Total cost + ROAS "—". Live-verified (Brinno $48,601→$60,438 = 1.2x, etc.) + 4 locked unit tests.
- **Thread #3 — Meta→Amazon honest empty-state.** Replaced the stale "Google and Meta report at the campaign level only" note (untrue since Google now has brand attribution) with a Meta-specific explainer: brand-grain attribution isn't available (walled garden, no Quartile-equivalent tagging), but channel-level ROAS is on the Ad Platform view.
- **Playwright E2E suite (2 lanes).** *Lane 1 — synthetic functional* (per-PR, credential-free): every view/filter, platform filter + the **GP-288 Overview regression lock**, column picker + **SmartScout-gone / no-Gated-group lock**, cross-nav, theme, empty states, metric-law; page-object helpers in `e2e/utils.ts`. **43/43 chromium.** *Lane 2 — warehouse data-integrity* (opt-in, live TEST): asserts the real dashboard is never silently blank/$0 where data is expected (the class of bug Paul catches by hand). Separate `playwright.warehouse.config.ts` + `npm run test:e2e:warehouse`, excluded from the default suite via `testIgnore`. **7/7 on live TEST data.** Nightly + `workflow_dispatch` CI in `.github/workflows/e2e-warehouse.yml` (needs the `SNOWFLAKE_*` repo secrets).
- **Deploy + merge.** Deployed to prod via **Vercel CLI** (`vercel --prod`, ALDC-linked project; deploy `dpl_F1yTuSfDRxyciBwDWumLJf2ooACd`) — prod reads the same TEST warehouse, so the fixes are live behind the Basic-Auth gate. **`main` is the Vercel production branch** (a `git push origin main` triggers a git-integration prod build), but the customer domain stays pinned to the CLI deploy. Merged `feature/insite-calendar-brand-reach-region` → `main` (`--no-ff`, `7264fe5`). Greened main CI by fixing two **pre-existing** failures unrelated to this work: repo-wide `prettier --write` (49 files) and a flaky `date-filter.spec` (Playwright `fill()` on a native `<input type=date>` intermittently dropped the commit; replaced with the reliable native-setter + `change` dispatch on the To bound — 8/8 chromium+webkit).
- **Open:** add the 7 `SNOWFLAKE_*` repo secrets (Actions → Secrets) so the nightly warehouse lane runs. `main` is now ~65 commits ahead of where it was; a future tidy-merge is optional (prod is CLI-deployed, not blocked on it).

## 2026-07-18 (plan session) — Top-bar "Coming" filter source-discovery (ASIN/SKU/Master-SKU/Teams/Kits)

Evidence-gated read-only probe (ACCOUNTADMIN via app `.env`, no vault) to decide which of the five disabled
top-bar filters (MASTER SKU, SKU, ASIN, TEAMS, GROUP-KITS) are buildable. **Result overturns three of five
triage calls and corrects the Deferred list — Teams and kit/master-SKU DO have a source.** No code built (plan
session); the plan is `aldc-launchpad/.claude/plans` → `gp288-top-bar-filters.md`, build boot prompt
`aldc-launchpad/boot-prompts/navira-gp288-top-bar-filters-build.md`.

- **One source powers ASIN + SKU + Master SKU (+ Teams): `TEST_DG1_GEP.DATA_SHARE.WAREHOUSE_SHARED_DIM_PRODUCT`**
  (41,905 rows) carries `ASIN`, `PARENT_ASIN`, `PRODUCT_ID`/`AMAZON_MERCHANT_SKU` (merchant SKU), `MASTER_SKU`
  (`MS_RR992`), `ACCOUNT_MANAGER` (Teams), `BRAND`. **Coverage vs 4,981 Navira roster ASINs: 99.4% matched,
  99.4% MASTER_SKU, 99.1% ACCOUNT_MANAGER.** Dim is SKU×condition-grained (max 2,315 rows/ASIN) → naive join
  fans out, but MASTER_SKU ambiguous for only 13 ASINs / ACCOUNT_MANAGER for 44 → a `GROUP BY ASIN` aggregate is
  1 row/ASIN, no fan-out (same dedup pattern as the existing `asin2vendor`/`sku2asin` CTEs).
- **Verdicts:** **ASIN** = build now (roster grain key). **SKU** = build — genuinely finer than ASIN (88.5% of ASINs
  map to >1 merchant SKU, avg 4.05); `sku=asin` in `mapProducts` today is a placeholder → surfacing real merchant
  SKU is a faithfulness fix. **Master SKU** = build — 99.4% coverage, near-1:1 identifier (avg 1.01 ASIN/master, so it
  *identifies* not *groups*), matches the prototype's paste-a-list control.
- **Teams — source PROVEN, kept Coming (client-gated):** `ACCOUNT_MANAGER`, 99.1% coverage, 6 clean values (Team
  Titan/Zeus/Atlas/Apollo/Nepenthe + "Unassigned Brand") — an ideal dropdown. **Deferred by Paul** because the labels
  expose internal GEP pod/person names ("Team Titan (Kayla's Unit)") on a client-facing dashboard — needs a client
  label decision (strip parenthetical / relabel / surface). Corrects the "team owner = no source" Deferred entry.
- **Kits — identifiable, kept Coming (rollup-gated):** 369 Navira kit ASINs via `PRODUCT_ID ILIKE 'KI_%'`; also
  `SUPPLEMENT.SKU_KITS_CSV` (kit SKU + KIT_COST_USD) + SellerCloud `ISKIT`/`FIRSTKITCHILD`/`ISBUNDLE`/
  `DEFAULTKITPARENTPRODUCTID`. The prototype's "Group all kits/components" is a component→parent **rollup** (not a value
  filter) → deferred as its own modeling lane. Corrects the "kit grouping = no source" Deferred entry.
- **Build scope (settled):** ship ASIN/SKU/Master-SKU (Product lens, one deduped dim join → new TEST view
  `MARKETING_DIM_PRODUCT_ATTRS` + `NAVIRA_MKT_RO` grant, mirroring the `MARKETING_DIM_CAMPAIGN_ATTRS` lane); Teams +
  Kits stay honest Coming with the proven source documented. Per-filter Playwright specs in both lanes during dev.

## Done

### Lane 1 — Campaign names verified + honest top-level Type (commit `f57e0c8`)
Probed `MARKETING_DIM_CAMPAIGN` (TEST) → `CAMPAIGN_ID, CAMPAIGN_KEY, CAMPAIGN_NAME` (no type column — corrected a spec contradiction). Name `LEFT JOIN` resolves **99% Amazon / 95.5% overall** (dim Amazon-only → Google/Meta keep the ID fallback). Type had been hardcoded `sponsored-products` for every campaign → now derived top-level from the name prefix (`SP/SB/SBV/SD`; **97.2% classify**, rest honest "—"). 399/399 vitest.

### Lane 2 — Platform-detail wiring: Google/Meta search-visibility + Meta video/reach (commits `698c310` + `015ce4b`)
Consumed `MARKETING_FCT_PLATFORM_DETAIL` (built but unwired). **413/413 vitest** (+14), tsc + lint clean.
- **Live-schema probe first:** the 16 assumed columns match; **`GOOGLE_ROAS` junk (max 0.12) and `GOOGLE_VIDEO_VIEWS` empty → skipped**; `AD_GROUP_ID` Google-null (grain = campaign×day, ad-groups Meta-only); impression-share ratios have no per-row impression weights → honest **unweighted mean of daily values** (never SUM a ratio).
- **Attribution decision (no fabrication):** the prototype's `top_of_search_impression_share`/`top_of_search_bid_adjustment` columns are tagged source = **Amazon Ads**, but this feed is **Google/Meta** — so the real metrics are surfaced as correctly-attributed `context` rows in the "Search visibility / win rates" section (Google) + a new Meta "Video & reach" section; the **Amazon-Ads columns stay "Coming"** (no source mislabeling).
- **Fixed a latent fabrication:** hardcoded `$0.00 daily budget` on every card → real avg daily budget for **124** Google campaigns, "—" otherwise.
- **Consumer-layer validated through the real provider path** (not synthetic): Google search-IS 27.1%, top-of-search 27.1%, abs-top 19.0%, budget-lost 3.5%, rank-lost 69.7%; Meta retention 66→32→20→13% across 83.6k video views.
- **Deploy prereq done:** granted `NAVIRA_MKT_RO` SELECT on the view (additive; rollback captured). Also fixed a pre-existing `ThemeToggle` lint break that was blocking `npm run lint` for the deploy gate.

### Lane 3 — Traffic completion (P1 item 4, commit `942dd22`)
Buy-box %, sessions, units, CVR surfaced as honest retail-readiness rows in the Product card (detail-page-views stays its own column). **Zero new SQL/creds** — same live-validated slice; `units` added to the `Traffic` type; buy-box/CVR suppressed at 0 sessions (no fabricated 0%). Cache v9→v10. 415/415 vitest.

### Lane 4 — "See all" expanded modals (P3.8, commit `a41c6b1`)
Built the prototype's `openExpanded` overlay: reusable accessible `ExpandedModal` (role=dialog, ✕/backdrop/Esc, focus + scroll management) + "See all N →" affordance reusing the **exact** card cell renderer + full-set CSV export. Wired to the only truncated real-data sections (Brand "Top products" cap 8, Platform "Top products" cap 15). **Pure frontend** — no SQL/creds/cache change. 424/424 vitest.

### Lane 5 — P3.9 Cost/Sale + Margin columns (design-spec §8.7, commits `7905735` + `9784f25`)
Restored v1's two flat-table analytical columns. **Cost/Sale** = spend÷purchases (`conversions ?? orders`) — pure-derived, zero SQL/creds/cache. **Margin** = (netSales−COGS)/netSales, **BRAND-grain**, from `SALES_FCT_ORDERLINE.SALES_COGS_CONSOLIDATED` (USD) — brand query already SUMs that fact's net → added `SUM(SALES_COGS_CONSOLIDATED)` in the same CTE: **no new grant** (`NAVIRA_MKT_RO` reads the table live), no new view/DDL, no vault (probed with app's own `.env`).
- **Null-honest COGS (the [[project_lectric_cm_data_gap|Lectric CM]] trap):** COGS stays NULL where unsourced, never → 0; `aggregatePlatformMetrics` leaves `cogs` undefined; Margin renders "—" not a fabricated 100%. **Rejected `MARGIN_NET_CONSOLIDATED`** (probed — doesn't reconcile to net−COGS; opaque/non-additive).
- **Consumer-layer validated live:** NAVIRA 92/92 vendors real margins 28–72% (Brinno 41.2%, TF Publishing 72.3%, K&M 48.5%, Thomastik 28.6%; ~99.1% of sales-bearing revenue carries COGS); LECTRIC COGS NULL → "—". Product/campaign rows → "—" (**per-ASIN Margin = follow-on**, needs SKU↔ASIN COGS crosswalk). Cache v10→v11. **437/437 vitest** (+8).

### Lane 6 — Per-ASIN Margin in the Product lens (P3.9 follow-on, commit `72dbcd1`)
Completes P3.9: added a per-ASIN contribution **Margin = (net − COGS)/net** to the Product-lens flat table (brand-grain Margin was Lane 5 / commits `7905735`+`9784f25`). **TEST-only, not merged/deployed.** tsc + lint clean, **440/440 vitest** (+3).
- **Same-fact self-consistent margin:** both COGS *and* net sourced from `TEST_DG1_GEP.WAREHOUSE.SALES_FCT_ORDERLINE.SALES_COGS_CONSOLIDATED` / `SALES_NET_CONSOLIDATED` (USD-consolidated), rolled from merchant-SKU grain to ASIN grain via the proven traffic **SKU→ASIN crosswalk** (`TRAFFIC_FCT_ACTIVITY`, dominant `CHILD_ASIN` per `PRODUCT_ID`, 1:1 so the join can't fan out — see [[navira-marketing-dashboard]] traffic lane). Numerator and denominator from the same fact/grain → **not** efficiency-attributed net mixed with orderline COGS. `netSales` is margin-only in the UI (no standalone column) so overriding it has zero display regression.
- **Null-honest (the [[project_lectric_cm_data_gap|Lectric CM]] trap):** COGS SUM stays NULL where unsourced (never coerced to 0) → Margin renders "—", never a fabricated 100%. LECTRIC has 0 bridged product rows.
- **Evidence (read-only `.env` probe, no vault):** bridged NET $75.53M < universe $76.51M (no inflation); **2,417/2,500 (96.7%)** top-roster ASINs carry COGS; **98.6%** of NAVIRA COGS $ bridges to an ASIN; the exact generated query validated live at the consumer layer — top margins byte-match the standalone probe, **max 90.8%** (<100%, no impossible margins), one honest **−52.6%** loss-making ASIN rendered faithfully.
- **Files:** `src/lib/data/providers/warehouse.ts` (buildProductsQuery gains `sku2asin` + `cogs_asin` CTEs; `ProductRow` gains `SALES_NET_USD`/`SALES_COGS_USD`; `mapProducts` sets margin-consistent netSales + null-honest cogs), `src/lib/insite-table.tsx` (Margin column comment), `src/app/page.tsx` (cache prefix **v11→v12**), `src/lib/data/providers/warehouse.test.ts` (+3 tests).

## 2026-07-17 (session 2) — 4-lane parallel run (Opus-orchestrated)

Ran two parallel git-worktree teams (FRONTEND: sparklines→attribution; DATA: connection-fix→campaign-attrs). Sub-agents did read-only recon + code drafting only; **all creds, prod DDL/grants, evidence-gates, live consumer-validation, and merges stayed on the orchestrator's main thread**. All four merged on `feature/insite-calendar-brand-reach-region`; **471/471 vitest, tsc+lint clean; nothing pushed or deployed** (17 commits ahead of `origin/feature`, all local).

- **Sparklines + bullet charts** (merge `c97548a`) — monthly-trend `Sparkline` (reused) + latest-vs-prior `BulletChart` (new) on the KPI tiles. Bullet target = **prior period** (Paul's call; **"Hard KPI goals" target = flagged FUTURE feature**, not built). Faithfully wired **only to the entity-anchor card** (Platform→Amazon "mer" scorecard: Ad Spend via `spendAmazon`, MER, TACoS, Total Sales) — `getMonthlyTrends` is entity-grain, so per-brand/product/campaign cards get no marks (honest-empty; entity-grain trend on a per-instance tile would misrepresent). Per-card sparklines = a future SQL lane. No cache change.
- **Date-filter terminated-connection crash fix** (merge `d5deb34`) — `WarehouseProvider.query()` detects an expired-connection error (SDK codes 407002/407001/405503 + message fallback), drops the dead memoized connection (guarded), reconnects, retries **once**; `clientSessionKeepAlive:true` reduces idle death. Infra-only, +5 tests. (Fixes the live "Unable to perform operation using terminated connection" on date-filter change.)
- **Brand-tab Google→Amazon attribution weave** (merge `625edaa`) — each brand's [[GP-287]] attributed sales + ROAS surfaced as a **separate** "Attribution by brand (Google→Amazon)" context section on its Brand card, sourced from the already-fetched Google→Amazon platform's `attributedBrandRoas` (no new fetch, no cache change). **No Flag-A fold** into the brand's Amazon KPIs (kept distinct). Empty-state signposting upgraded. **Brand is the lowest grain the attribution feed reconciles at** (no ASIN/campaign attribution exists; Google/Meta `PRODUCT_ID = -1`).
- **Campaign attributes view — P2 item 5 DONE** (merge `33b35a1`) — NEW TEST view `TEST_DG1_GEP.WAREHOUSE_TEST_GP226.MARKETING_DIM_CAMPAIGN_ATTRS` over `CURRENT_SPONSORED_{PRODUCTS,BRANDS,DISPLAY}_CAMPAIGN` ∪ `CURRENT_API_PORTFOLIO`, keyed on the `AMZ_`-prefixed campaign id; granted `NAVIRA_MKT_RO`. **Evidence-gated end-to-end:** 99.9% join coverage (count + spend); globally-unique key, **0 fan-out**; the generated `buildCampaignsQuery` consumer-validated live — **old vs new aggregation byte-identical** (4,399 rows / $2,746,639.43 / 435.5M impr), attrs populate (portfolio/bid/targeting/state/dates/budget), Google/Meta + by-design per-ad-product gaps (SP no cost-type, SB/SD no targeting, SD no bid/end-date) render honest "—". Wired into `buildCampaignsQuery` (LEFT JOIN + GROUP BY) + `Campaign` type + `mapCampaigns` + `insite-table` campaign cols + `CampaignView` (real STATE title-cased). Cache **v12→v13**. Evidence: `navira-marketing-dashboard/_lane4_campaign_attrs_evidence.md`. Rollback: `DROP VIEW` / `REVOKE`.

### Incidents (this session)
- **Shared `node_modules` twice deleted** by `rm -rf` / `git worktree remove --force` following a live `node_modules` **junction** into the real dir (Windows). Repaired both times with `npm install`; no source/commits lost. Lesson → memory `feedback_worktree_junction_hazard.md` (unlink + verify per-worktree before removing).
- **Prod site `navira-mktg.analyticlabs.io` returned HTTP 401** mid-session. Diagnosed: **Basic Auth gate** (`Www-Authenticate: Basic realm="GEP InSite"`, `Server: Vercel`) — site UP, **not our work** (nothing deployed). Was HTTP 200 on 2026-06-05 → the gate is new/changed since. Separate investigation prompt: `aldc-launchpad/boot-prompts/navira-vercel-401-investigation.md`.

## P2 source discovery — evidence-gated probe (2026-07-17)

Read-only `.env` probe (ACCOUNTADMIN, no vault) across all of `TEST_DG1_GEP` to locate the "Amazon Ads raw" campaign attributes the specs referenced only generically. Result **rewrites two P2 items** and corrects the gap-matrix a **second** time this ticket (it was already wrong about the campaign-type column in Lane 1):

- **Placement breakdown (top-of-search / product-page / rest) = NOT-INGESTED — blocked at source.** The only `PLACEMENT` column in the whole DB is `WAREHOUSE.MARKETING_FCT_CREATIVE_PLACEMENT`, which is **Meta** (facebook/instagram) creative placement for a *different client* (Cibu) — wrong platform, wrong client. `AMAZON_ADS.SPONSORED_PRODUCTS_CAMPAIGN` is config-only (no metrics/placement); there is no SP campaign *report* with a placement group-by. Building it needs a **connector-side change** (request the SP report *with* the placement dimension) + a fresh pull — not a warehouse-SQL lane. **No code written — the gate caught it before any build.**
- **Portfolio + bid strategy + status + start/end dates = FULLY SOURCED (buildable).** `AMAZON_ADS.SPONSORED_PRODUCTS_CAMPAIGN` carries `PORTFOLIOID` (+ `API_PORTFOLIO` dim for name), `DYNAMICBIDDING_STRATEGY`, `STATE`, `STARTDATE`, `ENDDATE`, `BUDGET_BUDGET`, `TARGETINGTYPE`. The gap-matrix tagged status/dates **NOT-INGESTED — wrong, they are all present.** SB/SD have parallel config tables (`SPONSORED_BRANDS_CAMPAIGN`, `SPONSORED_DISPLAY_CAMPAIGN`) with their own bid-strategy + portfolio columns → can cover all three Amazon ad types. Shape = new campaign-attributes view (or extend `MARKETING_DIM_CAMPAIGN`) joining `CURRENT_SPONSORED_*_CAMPAIGN` + `API_PORTFOLIO`, keyed on `CAMPAIGNID`. Open first step: verify `CAMPAIGNID` join-key coverage to the activity fact for Navira. This is warehouse **DDL** (new view + `NAVIRA_MKT_RO` grant) → stays fully evidence-gated + deploy-gated.

## Remaining lanes (priority order)

1. ~~**Campaign attributes view (P2 item 5)**~~ **✅ DONE 2026-07-17 (session 2)** — view `MARKETING_DIM_CAMPAIGN_ATTRS` built + granted + consumer-validated, wired into the Campaign view. See the session-2 section above.
2. **Server-side column prefs (P3.10)** — zero-cred, frontend/app-layer, lower unlock value. **Recon caveat:** the app has NO auth / API routes / writable backend today, so "server-side" can't mean per-user without first standing up an identity+store (sizes M shared / L per-user), not a pure-frontend swap. **Now the only remaining solo-buildable lane.**
3. **Placement breakdown (P2 item 6)** — **NOT-INGESTED / blocked at source** (see discovery above). Needs connector-side report change, not SQL.
4. **SmartScout integration** — biggest single visual unlock (brand market-share, ratings/reviews/BSR, ~45 gated columns, 3–4 scaffold sections per lens). **Client-gated:** needs confirmation a SmartScout feed is contracted/coming. Today all honest Gated.
5. **Campaign-level USD (P2 item 7)** — *blocked*: campaign `COST`/`SALES_AMOUNT` are native currency with no `CURRENCY_CODE` ([[transaction_currency_handling]]). Keep native-currency-labelled at campaign grain until rows are tagged with marketplace currency and consolidated.

*(Client-gated: SmartScout P0, sign-off, keyword text; the single reviewed prod deploy of the whole branch remains the end-step.)*

## Deferred / client-gated (keep honest Coming/Gated)

New-to-Brand (purchases/sales/%), DSP reach/video, FBA inventory, keywords/search-term text, Amazon "Activity" status, budget pacing, and the Reports → Asana workflow. **Meta→Amazon attribution is structurally impossible** (walled garden, no history — client conversation, not a build; see [[GP-287]]).

**⚠ Corrected 2026-07-18 (see the plan-session section above):** `master-SKU`, `team owner`, and `kit grouping` are NO LONGER "no source" — all three are sourced from `DATA_SHARE.WAREHOUSE_SHARED_DIM_PRODUCT` / `SUPPLEMENT.SKU_KITS_CSV` at 99%+ Navira coverage. **Master-SKU is scheduled to build** (with ASIN + SKU, top-bar filters). **Teams** stays Coming only because its labels expose internal pod names (client label decision). **Kit grouping** stays Coming only because the prototype control is a component→parent rollup (its own modeling lane).

## Guardrails

Hug the prototype key-for-key (faithful population, not invention). Evidence-gate every data/SQL change (prove target + consumer-layer + no-regression + rollback). Ask before pulling Snowflake creds. **Pre-deploy grant check:** confirm prod role `NAVIRA_MKT_RO` has `SELECT` on every view the branch now reads (campaign dim, traffic, **platform-detail — done**, grounded, attribution, attribution-by-brand, **`MARKETING_DIM_CAMPAIGN_ATTRS` — granted in TEST 2026-07-17; the prod path is a clients-repo warehouse template, since a hand-created TEST view may be wiped by an Eclipse rebuild**).

## See Also
- [[navira-marketing-dashboard]] — the repo; full per-lane record
- [[GP-287]] — Amazon Attribution feed (shares the branch)
- [[GP-225]] — unified marketing schema · [[project_gp226_roadmap_gap]] — UK/marketplace grain
- [[cross-channel-marketing-attribution]] — tiered measurement model
- [[transaction_currency_handling]] — USD-at-ingestion convention
