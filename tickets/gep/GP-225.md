---
tags: [ticket, gep, navira, marketing, snowflake, schema-design, windsor, phase-1a, scope]
aliases: [GP-225, Unified Marketing Schema Design]
sources: [Jira GP-225, clients/GEP/snowflake/warehouse/marketing_fct_activity.sql, clients/GEP/snowflake/warehouse/marketing_fct_platform_detail.sql, clients/GEP/eclipse/templates/windsor/google_ads.json, clients/GEP/eclipse/templates/windsor/meta_ads.json, clients/GEP/snowflake/mock/mock_google_ads_raw.sql, clients/GEP/snowflake/mock/mock_meta_raw.sql]
created: 2026-05-29
updated: 2026-07-08
---

> **✅ STATUS UPDATE 2026-06-05 — both pre-cutover investigations CLOSED; numbers shifted.**
> - **Jan-2026 anomaly (was open (c)) — CONFIRMED + FIXED.** Root cause: `SHARED_FCT_EXCHANGE_RATE` missing Jan 3–28 2026 (interior gap the forward-only carry-forward task never filled) → NULL `CONSOLIDATED_RATE`/`SALES_GROSS_CONSOLIDATED` on 74,024 orderlines, all currencies. Shadow-validated then fixed (carry-forward rows + re-materialized the window): Jan consolidated **$475K → $2.89M**, no regression. See [[GP-277]] (exchange-rate side-find).
> - **Amazon UK $0 (was open) — CONFIRMED = [[GP-221]] still blocked.** UK profile `1236242149887729` is in ZERO raw `AMAZON_ADS.CURRENT_SPONSORED_*` tables — no warehouse/mapping bug; just no UK pull. (Side note: CA SB/SD raw is also stale — SB stops 2025-08-17, SD 2025-03-02.)
> - **⚠️ Headline numbers MOVED (re-derive before sign-off/cutover):** the Jan heal propagated to `MARKETING_EFFICIENCY` → **MER 30.29x → 31.52x**, NAVIRA actual sales **$72.4M → ~$75.0M**. The 4-page Sandbox report + presentation guide quote the pre-fix figures.
> - **Agency [[GP-254]] now REAL (not the 327 mock):** LECTRIC = **$2,520,823 / 4,206 orders**, 2024-06→2026-06, US-only (the SP-API marketplace filter is ignored for that account). NAVIRA still isolated.
> - **Remaining gates for TASK 6 cutover:** Calibrated-ROAS capped-vs-uncapped decision; Sandbox sign-off (on re-derived numbers); single-owner fact swap w/ [[GP-199]]; PBI preview-clone refresh to pick up the Jan heal + real LECTRIC.

> **✅ STATUS UPDATE 2026-06-04 (pm) — Sandbox validation COMPLETE (cross-channel + agency); cutover gated.**
> - **Unified fact built + reconciled:** `WAREHOUSE_TEST_GP226.MARKETING_FCT_ACTIVITY_UNIFIED` = Amazon (PROD V2 passthrough) ∪ Google ∪ Meta, **820,388 rows** (810,903 + 9,485), exact reconcile, NAVIRA-isolated.
> - **PBI-safe Sandbox preview clone + report:** `GP225 Test Model Preview` (full Test model — 28 tables / 246 measures — on the unified fact, CORE-API chain neutralized, refresh green) + 4-page **`GP225 Marketing Validation`** report in **GEP Sandbox Models** (Truth / MER 30.29x · Flag A/B · Product-Grounded Tier 3 · Multi-Tenant Agency). Live Test "Data Model" **provably untouched** (LAST_ALTERED + refresh-history blast-radius proof).
> - **Validation:** MER + per-channel ROAS + calibrated + Amazon grounded all DAX-proven on the 2024-06+ window. **Flag A** (cross-platform Ad-Sales double-count: Σ platform-attr **$11.6M** vs actual orders **$72.4M**) and **Flag B** (ACOS cost-basis is actually `Order Line` settlement-fee cols, not marketing `COST` → denominator inflates once Google/Meta join) **reproduced in-context + addressed** (MER headline + Calibrated ROAS; no blended `SUM(SALES_AMOUNT)`).
> - **Agency [[GP-254]] integrated** as a 2nd `ENTITY_CODE` (LECTRIC, sales-only, 327-order sample, materialized in `WAREHOUSE_TEST_GP226`) → proves **zero-schema-change multi-tenant** via `ENTITY_CODE` + `SHARED_DIM_ENTITY`. NAVIRA numbers unchanged when sliced.
> - **Gaps (honest):** Contribution Margin (the North-Star metric) not built (needs Phase-2 COGS); Amazon **UK PPC $0** in the fact (US+CA only); **Target+** not built; Google/Meta product-grounding pending (~61% of Google spend = PMax+Shopping, groundable).
> - **Open before cutover (TASK 6):** (a) Calibrated-ROAS capped-vs-uncapped decision; (b) Sandbox sign-off; (c) confirm **Jan-2026** SALES_FCT_ORDERLINE FX-consolidation gap (preliminary: normal volume, NULL `SALES_GROSS_CONSOLIDATED`) + **Amazon UK $0** investigations; (d) single-owner fact swap coordinated w/ [[GP-199]]. Guide: `aldc-launchpad/pbi_ops/navira_marketing_presentation_guide.md`; boot: `aldc-launchpad/boot-prompts/navira-marketing-cutover.md`.

> **✅ STATUS UPDATE 2026-06-03 (pm-2) — Columns LOCKED · clean CANONICAL re-land (single-version) IN PROGRESS · PBI-safe promotion decided.**
> - **Column set finalized** (no additions): every raw col the unified fact + GP-277 efficiency/MMM consume is in the dtype-pinned templates (Google: campaign/account/date/type/currency + clicks/impressions/conversions/conversions_value/spend + platform_detail roas/search-share/video; Meta: + adset_id/reach/actions_purchase/action_values_purchase/video). CTR/CPC/CPM/ROAS/frequency are SQL-derivable; all_conversions/view_through deferred (Justin last-click lock).
> - **Clean bounded re-land** off the `_DEV` schemas into **canonical `GOOGLE_ADS`/`META`** (2024-01-01→now): the schema-version race that produced the 24-fork + consolidation shim is resolved — registry is **keyed by topic** so canonical starts fresh v1; first partition landed single-threaded establishes v1 + `CURRENT_`/`COMBINED_` views before ramping. First partition **proved clean v1 (1 base table, NO fork)** — this also exercised + closed the [[GP-277]] Fix-2 merge-path residual (and surfaced the `session_id` bug, fixed there). Drain of 884 Google daily + 29 Meta monthly partitions in progress; `_DEV` kept as reconciliation reference until canonical reconciles (9,280 rows / $202,231 / 6 accts).
> - **PBI-safe promotion DECIDED (Paul):** promote + validate in TEST **without touching the PBI Test Data Model**. Build computed stack in PBI-*unread* locations (efficiency → empty/unbound `REPORT_COMMON`; computed fact → shadow name, or re-point `WAREHOUSE_TEST_GP226` to canonical; leave the passthrough `WAREHOUSE.MARKETING_FCT_ACTIVITY` the model reads UNTOUCHED) → validate warehouse + DAX → design PBI display → **separate gated cutover** (the only PBI-visible event). All tiers already built — the gap is promotion + PBI, not new tiers. Boot prompt: `aldc-launchpad/boot-prompts/navira-promote-and-gp199-to-data-model.md`.
> - **Shared object w/ [[GP-199]]:** `MARKETING_FCT_ACTIVITY` is rewritten by both this (branches 6/7) and GP-199 (Amazon + fact v3) → one owner for the cutover; PROD has no Google/Meta so promoting the fact to PROD surfaces only Amazon/GP-199 (clean decoupling).

> **✅ STATUS UPDATE 2026-06-02 — Tier-1 revenue IMPLEMENTED + VALIDATED in DEV.** The revenue-gap work below ("Work to close the gap") is **done**: Google `conversions_value` + Meta `actions_purchase`/`action_values_purchase` added to both Windsor templates (committed clients `379056fa`) and to the DEV fact (Branch 6/7 `SALES_AMOUNT`/`CONVERSIONS`). **Row-split gotcha tested — PASS** (no PK fan-out; `SUM(spend)` identical with/without the fields). Real Navira data backfilled from 2026-04-01 into `TEST_DG1_GEP.GOOGLE_ADS_DEV`/`META_DEV` via the scoped `gep-windsor-test` agent; DEV unified fact `WAREHOUSE_TEST_GP226.MARKETING_FCT_ACTIVITY` now carries **COST $29,339 + SALES $115,533** (NAVIRA-only, 6/6 isolation PASS), reconciled to the live Windsor API within attribution-lag tolerance. **Remaining for GP-225 close:** update committed mock SQL (`mock_google_ads_raw.sql`/`mock_meta_raw.sql`) to exercise the revenue path; a clean single-version re-land (the rapid re-land raced Eclipse schema versioning → consolidation shim, see [[navira-dwh-data-landing]]); promotion to the live/PROD fact is a separate PROD-gated ticket. Cross-channel *truth* (blended MER) remains **GP-277**, not this ticket.

# GP-225 — Unified Marketing Schema Design (Snowflake)

**Type:** Story (Scope/Design) | **Status:** Consulting/Design | **Sprint:** S3 | **Sizing:** 2 SP | **Assignee:** Paul Russell
**Roadmap:** [[phase-1a-marketing-ad-platforms|Phase 1A]] — prerequisite for all non-Amazon marketing connectors.

Design the unified Snowflake marketing schema that every Phase 1A connector (Google Ads, Meta, Amazon UK PPC, Target+) feeds into, to enable cross-channel ROAS. Two-layer target: `MARKETING_RAW` (source-specific staging) + `MARKETING_ANALYTICS` (unified grain), with a `REPORT_COMMON` view layer for BI consumption.

## Key Finding — the unified schema already largely exists

`MARKETING_FCT_ACTIVITY` (`clients/GEP/snowflake/warehouse/marketing_fct_activity.sql`) **already unifies** all channels into a single grain via a 7-branch `UNION ALL`:

- Branches 1–5: Amazon Ads SB (ASIN-level + non-ASIN fallback + legacy), SP, SD
- Branch 6: Google Ads (via [[Windsor]])
- Branch 7: Meta/Facebook (via [[Windsor]])

Common grain: `PLATFORM, CAMPAIGN, AD_GROUP, PROFILE, PRODUCT_ID, ACTIVITY_DATE, MARKETPLACE, ENTITY_CODE` with SHA2 keys. Metrics: `CLICKS, IMPRESSIONS, CONVERSIONS, COST, SALES_AMOUNT, UNITS_SOLD`. A sibling view `MARKETING_FCT_PLATFORM_DETAIL` carries platform-specific columns (Google ROAS / impression-share, Meta reach / video).

So GP-225 is mostly **formalizing** what exists (RAW/ANALYTICS layering + `REPORT_COMMON` views + closing the revenue gap below), not green-field design. The "extend `MARKETING_FCT_ACTIVITY` vs build parallel" decision in the ticket is effectively answered: extend — the unification already happened and [[GP-199]] already conforms to its grain.

## Open Questions (from [[phase-1a-marketing-ad-platforms]])

### Q3 — Attribution window & model → **store raw, do not normalize**

The production schema has effectively already decided this: each branch carries the platform's **native** attribution as-is; there is no cross-channel normalization layer, and the ticket itself warns that normalizing attribution across channels "is a project in itself."

- Amazon SP/SD surface the **30-day** window columns (`PURCHASES30D`, `SALES30D`, `UNITSSOLDCLICKS30D`) — the SP advertised-product template requests only the `*30d` fields. Amazon's *API default* is 14-day, but the report exposes 1d/7d/14d/30d and ALDC chose 30d.
- Google conversions arrive via Windsor per the account's model (data-driven by default).
- Meta would arrive per its window (7-day click / 1-day view) — once revenue is actually pulled (see gap below).
- [[Windsor]] only ever returns platform-native attribution and can't be re-windowed historically, so "store raw" is also the only *practical* option.

**Design decision:** store raw per-platform attribution in `MARKETING_RAW`; for Amazon, persist all available windows and surface 30d as the headline in `MARKETING_ANALYTICS` to match prod. Do **not** build a normalized cross-channel attribution model.

**Amazon headline window — DECIDED 2026-05-29 (Paul): keep 30-day** (matches current prod `*30D` columns; revisit only if Navira requests otherwise). Remaining soft confirm for Lori/Heather: she's OK seeing each platform on its native window, clearly labelled, side by side.

### Q8 — Campaign → SKU mapping → **partially answered by [[GP-199]]; no ASIN→internal-SKU dimension exists yet**

The unified fact already has a `PRODUCT_ID` / `PRODUCT_KEY` grain. [[GP-199]] populates it with **ASIN** for Amazon SB (via `EXTRACT_AMAZON_ADS_SB_AD_ASIN_MAP`); SP uses `ADVERTISEDSKU`. Google/Meta rows set `PRODUCT_ID = '-1'` (no SKU linkage).

**The gap, re-characterised (2026-05-29):** `SALES_FCT_ORDERLINE.PRODUCT_ID` uses internal SKUs (e.g. `FBM_AUTO10W`); ad data uses ASINs (e.g. `B0GT2G514J`). An ASIN↔internal-SKU bridge **does exist** — `SHARED_DIM_PRODUCT_BASE` maps `ASIN`/`PARENT_ASIN` ↔ `PRODUCT.ID` from the Sellercloud product master. So the real gap is **(a)** that bridge isn't wired into the unified ad fact's `PRODUCT_ID` resolution, and **(b)** coverage — advertised ASINs absent from Navira's Sellercloud master won't resolve (the UNK case GP-199 deferred). For *agency* customers the master doesn't cover their ASINs at all, so each needs its own product dim. This bridge is what enables Amazon product-grounded ROAS — see [[cross-channel-marketing-attribution]]. Google (UTM) and Meta (catalog) SKU linkage is a later extension (gated Phase 2).

> **Q8 detail handed off to a separate session.** Full SKU-mapping design not yet finalized here.

## Cross-Channel ROAS Gap — Google/Meta revenue is not loaded (the real blocker)

This is the biggest finding and the actual blocker behind "true ROAS across channels" — bigger than attribution-window alignment. It is **not** a [[Windsor]] capability gap; it's a field-selection + mapping gap.

> **Connector context:** Google/Meta are pulled via **Eclipse** (`windsorai_v1` connector, GEP account `da8904db`) into the legacy connectors repo — **not** Prefect (that migration is shelved, 2026-05-28). All template + warehouse-SQL changes below are Eclipse work; "client code" is **GEP**, not `GEP_PREFECT`.

| Branch | Today | Why |
|---|---|---|
| Google (6) | loads `CONVERSIONS`; hardcodes `SALES_AMOUNT = 0` | Windsor Google template (`templates/windsor/google_ads.json`) doesn't select `conversions_value` (it does carry `roas`). Clone of Fusion92's template, which never pulled revenue. |
| Meta (7) | hardcodes `CONVERSIONS = 0` **and** `SALES_AMOUNT = 0` | Windsor Meta template (`templates/windsor/meta_ads.json`) only selects **video-view actions** — no purchase actions, no `action_values`. Same clone gap. |

=> Cross-channel ROAS cannot be computed until revenue is loaded for Google/Meta. **Creds were verified 2026-05-29**, so the external blocker is gone — what remains is warehouse work.

### Work to close the gap (next increment)

1. **Google template** — add `{"name": "conversions_value"}` (VERIFIED present 2026-05-29; pairs with existing `conversions`). Also available: `all_conversions_value`, `conversion_value`, `adnetwork_revenue`, and `roas` (already pulled but unused). Hold back `all_conversions_value` for now — view-through double-counts + row-split risk.
2. **Meta template** — add purchase conversions + value. **Field names VERIFIED 2026-05-29** against live `GET https://connectors.windsor.ai/facebook/fields?api_key={KEY}`: use **`actions_purchase`** (count) + **`action_values_purchase`** (value). Variants that exist: `actions_omni_purchase`/`action_values_omni_purchase` (note PLURAL "values"), `actions_offsite_conversion_fb_pixel_purchase`/`action_values_offsite_conversion_fb_pixel_purchase`. **Do NOT use `action_value_omni_purchase`, `purchases`, `purchases_value`, `purchase_roas`, `website_purchase_roas` — these do not exist in Windsor's catalog** (the earlier candidate guesses were wrong).
3. **Mock SQL** — add `CONVERSIONS_VALUE` to `mock_google_ads_raw.sql`; add purchase-action + `ACTION_VALUES_PURCHASE` columns to `mock_meta_raw.sql`, or the revenue path isn't exercised in the clone.
4. **`marketing_fct_activity.sql`** — Branch 6 `SALES_AMOUNT = SUM(G.CONVERSIONS_VALUE)`; Branch 7 `CONVERSIONS`/`SALES_AMOUNT` mapped to the new Meta fields.

> ⚠️ **Row-splitting gotcha** (both template comments warn): some Windsor field combinations split rows (same PK, partial data each) and corrupt the Snowflake merge. After adding fields, test one day grouped by PK for >1 row **before** full activation. Meta is the high-risk one.

> ⚠️ **Cross-client account filter**: the Windsor team API key pulls **all** Fusion92 accounts too — a per-client account filter must be built into the GEP Windsor templates before real-TEST/Eclipse activation. See [[project_windsor_fusion92_filter]].

### This delivers per-channel ROAS only — not cross-channel truth

The Windsor revenue fields above are **platform-attributed** conversion value (each platform's own pixel + window). They double-count across channels and won't reconcile to actual orders in `SALES_FCT_*`. So this gap-close gives correct **per-channel** ROAS (Tier 1) — valuable for in-channel optimisation — but is **not** a cross-channel source of truth. The grounded model (blended MER + Amazon product-level, denominator = real orders) is designed separately in **[[cross-channel-marketing-attribution]]** and needs its own ticket. Don't mark "true cross-channel ROAS" done off the back of GP-225.

## Relationship to [[GP-199]] — no rework; safe to promote

GP-199 (SB ASIN spend allocation) is **not impacted** by Q3 or Q8:

- It is a **cost-allocation** feature only — its branch sets `CONVERSIONS=0, SALES_AMOUNT=0, UNITS_SOLD=0` and only allocates `ASIN_ALLOCATED_COST`. Q3 concerns sales/conversion columns it doesn't touch.
- It **conforms to** the unified grain (populates `PRODUCT_ID` with ASIN) and **implements** the Amazon-SB answer to Q8.
- The only thing that would force GP-199 rework — switching SB to purchased-product attribution (Approach B) — was explicitly **rejected by Justin Shuster on 2026-05-01**.

=> GP-199 prod promotion does **not** need to wait on GP-225.

## Clone Validation (2026-05-29, parallel session)

Real Navira Google + Meta data loaded into `TEST_DG1_GEP_DEV` (clone only; real TEST/Eclipse untouched). 1,114 Google rows ($16,881, 6 accounts) + 152 Meta rows ($5,371, 2 of 3 accounts — Bridgford had no in-window spend). Every row maps to `ENTITY_CODE = 'NAVIRA'` (0 UNKNOWN); per-brand mapping clean (Brinno, Slobproof, GEP-Amazon, bigsostore, Bridgford, Cibu). Confirms the Windsor field structure lines up 1:1 with `GOOGLE_ADS_CAMPAIGN` / `FACEBOOK_AD_INSIGHTS` and the Branch 6/7 entity-map join is correct. **Note:** Google/Meta validate as spend-only until the revenue fields above are added — ROAS not yet validatable. See [[project_dwh_clone_only_deploy|clone-only deploy]] memory.

## Open Items — PBI measure-layer risks (surfaced 2026-06-04, PBI-safe promotion / TASK 3 DAX validation)

Two measure-layer risks were found while validating the shadow `WAREHOUSE_TEST_GP226.MARKETING_EFFICIENCY` against the live GEP Test "Data Model" DAX. **Both must be solved before the unified fact reaches PBI (the gated cutover).** Neither is a warehouse-data bug — they are how the *consuming DAX measures* behave once Amazon + Google + Meta rows coexist in one `MARKETING_FCT_ACTIVITY`.

### Flag A — cross-platform Ad-Sales double-count
The live measure `[Actual - Marketing - Ad Sales] = SUM('Marketing Activity'[SALES_AMOUNT])` sums platform-attributed sales across **all** rows. Once the fact is unified, this sums Amazon + Google + Meta attributed value — but per-platform attributed value **double-counts across channels** (the same order can be claimed by Amazon's *and* Google's *and* Meta's pixel/window — the "per-channel only, not cross-channel truth" finding above). The shadow `MARKETING_EFFICIENCY` view guards this by keeping `PLATFORM_ATTR_SALES_USD_{AMAZON,GOOGLE,META}` as separate columns explicitly flagged *"never sum across platforms."*
- **Resolution direction:** headline blended efficiency = **ground-truth MER** (`Σ ACTUAL_SALES_GROSS_USD / Σ SPEND_USD`, denominator = real orders from `SALES_FCT_ORDERLINE`); keep platform-attributed sales as **per-platform-only** measures (per-channel ROAS), not a blended `SUM(SALES_AMOUNT)`. See [[cross-channel-marketing-attribution]] (the grounded model).

### Flag B — `[Actual - Cost - Advertising]` misses Google/Meta spend
The live measure `[Actual - Cost - Advertising] = [...(Brand)] + [...(No Sale)] + [...(Product)]` sums three **Amazon spend-model buckets**. Once Google/Meta rows exist, any measure built on this (ACOS = `DIVIDE([Cost-Adv],[Ad Sales])`, TACOS = `DIVIDE([Cost-Adv],[Sales-Gross])`) would **understate total ad cost** (exclude Google/Meta).
- **Resolution direction:** switch the ACOS/TACOS cost basis to `SUM('Marketing Activity'[COST])` (all channels) or add explicit Google/Meta cost buckets. Confirm at the DAX layer on the clone before cutover.

> Tracked as open flags in the promotion session (`aldc-launchpad/PROGRESS.md`, 2026-06-04). To be decided in the PBI display design step and implemented at the single-owner fact cutover (with [[GP-199]]).

### PBI display design + DAX-engine validation (2026-06-04)
Design doc: `aldc-launchpad/pbi_ops/marketing_efficiency_display_design.md`. Two-layer model — tactical platform-reported (Tier 0/1) vs ground-truth (Tier 2 MER + Calibrated ROAS) vs product-grounded (Tier 3 Amazon). Decisions locked (Paul, all client-confirm-flagged): MER headline ROAS-style + inverse cost%; both Amazon-scoped **and** all-channel cost measures; surface Calibrated ROAS now; separate `Marketing Efficiency` / `Marketing Efficiency Product` tables.
- **Validated in the real DAX engine on an ISOLATED clone** (`GP225 Marketing Efficiency (scratch)` in GEP Sandbox Models, binding the shadow `WAREHOUSE_TEST_GP226` views; live "Data Model" untouched). 25 measures parse + compute; grand-totals match the SQL layer (Google/Meta + Actual Sales + Orders exact); filter context proven.
- **⚑ Display nuance found:** Google/Meta spend is tagged `MARKETPLACE_NAME='Cross-Channel'` → **MER sliced by marketplace shows Cross-Channel=0** (no marketplace-level ground truth). Blended MER is an **entity/total-level** metric; per-marketplace MER valid for Amazon only; use **Calibrated ROAS** for per-channel. Reinforces the [[cross-channel-marketing-attribution]] "MER over-credits / Google-Meta blended-only" caveats.

## 2026-06-05 (pm) — Re-derive complete + dashboard-team handoff

Post the A1 exchange-rate heal + the real Lectric backfill, all Sandbox numbers were **re-derived at the PBI/DAX layer** and the stack was packaged for a dashboard-building team.

- **Headline (NAVIRA, re-derived):** MER **31.33x** (Marketing Cost 3.19%), spend $2.40M, actual revenue $75.1M, calibration factor 6.44, Σ platform-attributed $11.67M, Grounded ROAS 36.06x. Order count verified **exact** (`SUM(ORDERS)` == `DISTINCTCOUNT(ORDER_ID)` = 1,716,309). LECTRIC $2.52M / 4,206 orders (sales-only).
- **Calibrated ROAS — capped + uncapped both built.** Open client decision: for Navira (organic-heavy, factor ≫ 1) the **capped** (`LEAST(1,factor)`) definition is *inert* → collapses to Platform ROAS (5.05/2.80/0.99x); **uncapped** carries the channel-mix signal (32.5/18.0/6.4x). Capped DAX verified == warehouse `MARKETING_EFFICIENCY_MONTHLY` to 4 dp.
- **Shadow dims (additive, merge-safe):** the unified fact's Google/Meta rows went blank on the Platform + Marketplace dims (live dims lack Google/Meta + the `UNKNOWN_MARKETPLACE` sentinel). Added shadow `MARKETING_DIM_PLATFORM` + `SHARED_DIM_MARKETPLACE` in `WAREHOUSE_TEST_GP226` so they resolve; facts unchanged. **Must be added to the live dims at cutover.**
- **Team handoff (in `aldc-launchpad`):** `pbi_ops/navira_marketing_data_dictionary.md` (elite grounding doc — objects, grain, columns, caveats, dashboard recipes, Claude-session grounding) + `pbi_ops/navira_marketing_presentation_guide.md` (narrative). `WAREHOUSE_TEST_GP226_TEAM` = self-contained Snowflake snapshot the team can modify without touching dev. Build on the `GP225 Test Model Preview` Sandbox model.
- **Repo moved to ALDC org:** `github.com/ALDC-io/aldc-launchpad` (private), copied out of the personal account.
- **Still gated (cutover, with [[GP-199]]):** live-dim widening, `ENTITY_CODE`/`CHANNEL` on the consumer fact, `REPORT_COMMON` promote, single-owner fact swap; resolve the capped/uncapped decision with Navira.

## 2026-07-08 — Cross-channel unification: current state, this-session changes, architecture direction

> **Context for a planning session:** a Lori/Heather meeting shifted the new-data-model requirements
> slightly. This section is the *most-current* record of what the cross-channel marketing model actually
> is + what changed this session, so meeting requirements can be diffed against reality. **Scope of all
> work below = TEST + repo only; no PROD deploy; live `Data Model` `66151728` never mutated.**

### The framing that matters (established this session)
- **The unified marketing fact ALREADY spans Amazon + Google + Meta and is live in the TEST model Navira
  reviewed.** Integration is not the open problem. The **root of the "why isn't Google/Meta in the same
  table" perception** is the *consumer layer*: the visible **`Marketing Measures`** group (ACoS, Ad Sales,
  Cost) is **Amazon-scoped** — `CALCULATE(SUM(...), PLATFORM_ID IN {Amazon SP, Amazon SB})` — so slicing
  by platform showed only Amazon. Fix = cross-channel measures + a clean channel axis, NOT more warehouse merging.
- **Client goal (restated):** view Google/Meta ad metrics the same way as Amazon in `marketing_fct_activity`,
  and pivot/analyze the new sources like the old in the PBI model.

### Empirical grain reality (deployed `MARKETING_FCT_ACTIVITY_UNIFIED`, probed 2026-07-08)
Mixed-grain fact, unioned at the finest common grain with **sentinels** for absent levels (`PRODUCT_ID='-1'`,
`MARKETPLACE_NAME='Unknown'`) — the correct pattern; each channel fills what it has:

| Channel | Effective grain | Products | Markets | Attr sales |
|---|---|--:|--:|--:|
| Amazon Sponsored Products | product × campaign × adgroup × market × day | 2,790 (all rows) | 3 (US/CA/UK) | $11.36M |
| Amazon Sponsored Brands | mostly campaign × day | 305 (43k/50k rows no-product) | 2 | $1.20M |
| Amazon Sponsored Display | campaign × day | – | 2 | $0 |
| Google Ads | campaign × day | – (all `-1`) | 1 = `Unknown` | $659K |
| Meta | campaign × adset × day | – (all `-1`) | 1 = `Unknown` | $32K |

**Architect's verdict:** channel-agnostic metrics (spend, clicks, impressions, conversions, CTR/CPC/CPM,
platform ROAS) pivot cleanly across all channels — this is most of the analysis and works today. **Product**
and **marketplace** pivots are Amazon-only *because Google/Meta emit no product/marketplace grain* (data-source
reality, not a model gap; the Google grounding layer is the best-effort product/brand bridge). Attributed
sales are per-channel — **never sum across channels** (Google+Meta double-count the same conversion).

### What was BUILT this session (all isolated / additive / validated; nothing deployed to live)
- **Design record:** `aldc-launchpad/pbi_ops/navira_cross_channel_fact_integration_plan.md` (fact-by-fact
  feasibility + change list + the §7 SQL-grounded "what changes to complete" table).
- **Isolated PBI clones** in **GEP Sandbox Models** (live `66151728` untouched; builder
  `pbi_ops/_build_xchannel_demo_clone.py`, variants: baseline/preview/demo/marketing; rollback = drop):
  - `Data Model (Cross-Channel Demo)` (`fa795641-…`) — faithful clone, **validated cell-for-cent vs live**
    (Actual Sales / CM / Order Count / grounding identical; MER within 0.03% refresh drift). Full history
    (2023→today) after widening the de-incrementalized window.
  - `Data Model — Cross-Channel Preview` (`22d6b827-1709-456c-ac86-7d4bedb38044`) — baseline **+ 3
    cross-channel measures** (`Ad Cost (All Channels)`, `Ad Sales (All Channels, attr)`, `ACoS % (All
    Channels)`) **+ Sponsored-Display Platform-dim member**. Refreshed + validated: cross-channel measures
    **populate all 5 platforms**; contrast — the legacy Amazon-scoped `Actual - Marketing - Cost` returns the
    same $2.44M on *every* platform row (the Amazon-only trap).
  - Reports (Fabric-REST builders `pbi_ops/_build_xchannel_demo_report.py` / `_build_xchannel_preview_report.py`):
    **`Navira — Cross-Channel Integration (Demo)`** and **`Navira — Cross-Channel Comparability (Preview)`**.
- **Repo reconciliation (clients repo, Option A — "make committed SQL honest", NO deploy):** branch
  **`feature/paulrussell/navira-marketing-canonical-consolidation`** (off `main` at `da103b18`; local, not
  pushed; marketing-only — **inventory/SKU-profitability/mock excluded** per Paul). Canonical unified fact +
  widened dims. **Equivalence gate (read-only vs deployed truth) caught + fixed two real regressions:**
  1. gp-225's Google/Meta branches hardcoded `SALES_AMOUNT=0` / Meta `CONVERSIONS=0` (stale, predated the
     2026-06-01 revenue-field add) → would have **zeroed Google/Meta attributed sales + Platform ROAS**.
     Fixed to `SUM(G.CONVERSIONS_VALUE)` / `SUM(M.ACTIONS_PURCHASE)` / `SUM(M.ACTION_VALUES_PURCHASE)`.
  2. `META_FACEBOOK` → `META_FACEBOOK_ADS` (matches deployed dim column).
  3. UK marketplace key `NULL_NULL_-1_170` → `_178` (`_170` collided with Bigso/Unknown).
  **Proven:** Google (10,528 rows / $226,270 / $656,704 sales) + Meta (412 / $14,411 / $31,636) match
  deployed `_UNIFIED` cell-for-cent. Reference/rollback bundle: `aldc-launchpad/warehouse_ops/_converge_ref/`.
- **Key structural finding (Phase-0 capture):** deployed TEST is a **hybrid scaffold** — Amazon SP/SB via
  **PROD passthrough** (`WAREHOUSE_SOURCE.MARKETING_FCT_ACTIVITY = SELECT * FROM PROD_DG1_GEP…`), Google/Meta
  via TEST landing (PREPROD Branches 6/7), UK via sandbox landing, SD via prod share. The gp-225 canonical
  fact is a **PROD-target artifact** (reads prod raw + `EXTRACT_AMAZON_ADS_SB_*` views that exist only in
  PROD) — so it can't fully run in TEST, and its Amazon-portion equivalence lives at the (out-of-scope) prod
  cutover. `MARKETING_FCT_ACTIVITY_UNIFIED` is **Snowflake-only, not in the repo**.

### Architecture direction — 4 gaps to "analyze the new data like the old" at high standard
1. **Conformed `Channel` dimension** *(highest UX impact)* — one axis (Amazon/Google/Meta) every row carries;
   today `Platform` conflates ad-type (SP/SB/SD) with channel. Doable as a model calc column (additive).
2. **`SPEND_USD` on the fact** *(correctness)* — `COST` is local currency (CA=CAD, UK=GBP ≈ $80K non-USD
   mixed in); high standard exposes an FX-consolidated spend like the efficiency layer. (Interim: use
   `Marketing Efficiency[Spend (USD)]` for USD money.) The one genuine warehouse follow-up.
3. **Honest sentinel labels** — `'Unknown'` marketplace → `'Cross-Channel (Google/Meta)'`; `PRODUCT_ID='-1'`
   → `'(not product-targeted)'` so product/marketplace pivots bucket rather than drop/confuse.
4. **Grain-guard non-additive measures** — product-ROAS = Amazon-only; attributed-sales = never-sum.

### ⚑ NEW architecture decision (2026-07-08, Paul) — TWO-MODEL, not promote-into-live
Emerging direction (encoded in `_build_xchannel_demo_clone.py` `marketing` variant): **leave the daily
`Data Model` (`66151728`) as-is; stand up a SEPARATE marketing model — `Navira Marketing Model (Test)` — as
a sibling TEST dataset in GEP Test Models.** Build it as a faithful clone first (validates cell-for-cent),
then add marketing features (cross-channel measures, Margin-incl-Ad-Spend) as separate individually-validated
additive steps. This **supersedes** the earlier "promote the additive measures into live `66151728`" plan.
*(The Lori/Heather meeting may further shape this — reconcile against the transcript.)*

### Open decisions / gated (for the planning session to reconcile with the meeting)
- Two-model (`Navira Marketing Model (Test)`) vs promote-into-live — **direction is now two-model**; confirm
  against meeting requirements.
- `SPEND_USD` on the fact (the one warehouse follow-up) — schedule as deliberate additive column.
- **PR #2 — Order Line / Agency canonicalization** (fold the 744-line `scripts/gp254-lectric-native-sales/
  sales_fct_orderline.sql` wrap into canonical `warehouse/sales_fct_orderline.sql`; agency itself already
  live in TEST). Phase-0 captured (`warehouse_ops/_converge_ref/`); fold deferred (post-tonight cleanup).
- Capped vs uncapped Calibrated ROAS (long-standing open client decision) — still open.

## See Also

- [[phase-1a-marketing-ad-platforms]] — Phase 1A workflow, full Open Questions table
- [[navira-data-dictionary-phase1a]] — field-level API/attribution reference
- [[GP-199]] — Amazon SB ASIN attribution (conforms to this schema; promote independently)
- [[Windsor]] — the conversion-value/revenue field-selection gap + row-split gotcha
- [[star-schema-convention]] — ALDC naming (SHA2 keys, currency triple)
- [[google-ads]], [[facebook-ads]], [[amazon-ads]] — connector specs
