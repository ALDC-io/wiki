---
tags: [ticket, gep, navira, amazon-ads, eclipse, connector]
aliases: [GP-257, Amazon UK PPC, Navira Amazon UK Ads]
sources: [conversation 2026-06-10, conversation 2026-06-11, connector PR #125, aldc-launchpad/boot-prompts/test-eclipse-rehab.md]
created: 2026-06-11
updated: 2026-06-11
---

# GP-257 — Navira Amazon UK Sponsored-Products Ad Spend

Bring Navira's **Amazon UK** Sponsored-Products ad spend into the warehouse. UK ad data never flowed because the legacy [[amazon-ads]] connector was **NA-only**; Amazon partitions ad profiles by region endpoint, so UK/EU profiles are only reachable via the EU host. This ticket added EU-region support to the connector and proved the data lands through the real [[Eclipse]] pipeline.

## Root cause

The `amazon_ads` connector hard-coded the NA hosts (`advertising-api.amazon.com` / `api.amazon.com`). UK profile `1236242149887729` (GBP, Global Ecom UK) is only reachable via the **EU stack** (`advertising-api-eu.amazon.com`, LWA token exchange at `api.amazon.co.uk`). A UK/EU-enabled refresh token has existed since 2026-05-08 (vault: `GEP Amazon Ads Refresh Token (UK - 2026-05-08)`) and is a **superset** — it covers 12 EU marketplaces **plus** US/CA/BR/MX, so one connection serves both NA and UK templates.

## Connector fix — PR #125 (`d00fc39`, merged to `development`)

Added to [[amazon-ads]] (`connector/amazon_ads.py`):
- optional **`region`** option (`NA` default = no-op; `EU` re-points LWA + Ads API hosts to the EU stack)
- optional **`profile_ids`** filter to scope a pull to specific profiles

EU-only, **decoupled from GP-199**. Connector prod branch is **`master`** (not `main`). Current `:development` image = `40430ed` (includes `d00fc39`). EU support confirmed baked into the running image.

## Registered template

TEST Eclipse `work_template` (CosmosDB `aldctestcsdb1c01`, db `core`):
- id `c55d33b9f80b496497f7b655ae95ea6d`, connector `amazon_ads_v1`, topic `AMAZON_ADS`, status active
- options: `region:EU`, `profile_ids:["1236242149887729"]`, `category:advertised_product_report` + 6 sales/units fields
- `min_date 2026-03-08` (Amazon SP-report retention floor — rolling ~94d; earlier is permanently 400-rejected, so backfill must run soon)
- connection `66627ed9-…` ("Amazon Ads API", holds the 2026-05-08 superset token)
- Repo source: `clients/GEP/eclipse/templates/amazon_ads/sponsored_products_advertised_product_report_uk.json` (uncommitted — commit via branch+PR to clients `main`)

## Eclipse dispatch — PROVEN end-to-end (2026-06-11)

Ran the template through the **real** [[Eclipse]] pipeline (not a hand-load). Key enabler: target the **core_api stage slot** (`aldctestfnapcore1c01-stage`), which carries the [[GP-277]] pick-fix (`85557db`) — so the connection-scoping pick-bug is not the blocker. See [[eclipse]] § scoped-agent dispatch.

Scoped executor (`agent_id=gep-amazonads-pp-test`, image `:development`=40430ed) confirmed live:
1. scoped `/work/pick` → returns the UK partition (pick-fix good on stage)
2. `POST api.amazon.co.uk/auth/o2/token` → 200 (token is UK/EU-valid)
3. `GET advertising-api-eu.amazon.com/v2/profiles` → 200
4. report created → downloaded → **landed** to `TEST_DG1_GEP.AMAZON_ADS.CURRENT_SPONSORED_PRODUCTS_ADVERTISED_PRODUCT_REPORT`

**Validation (evidence-gated):**
- UK rows land as **GBP**, profile `1236242149887729`
- Raw metrics (clicks/impressions/cost) **exactly match** the independently hand-loaded sandbox (`WAREHOUSE_TEST_NAVIRA_ROADMAP`) per-date (e.g. 2026-03-12: 6 rows / 9 clicks / 1656 impr / £5.53 — identical)
- **0 regression**: US (623,393) / CA (63,711) row counts unchanged; UK purely additive
- **0 duplicate primary-hashes** — merge/history dedup kept data clean even under churn
- Eclipse is *more* complete than the hand-load (captures 2026-03-11, which the sandbox lacked)

## Backfill — PARTIAL (241 clean rows / 33 days, 2026-03-11→05-20)

The full 95-day backfill did **not** complete. Systemic blocker: `advertised_product` reports generate on Amazon's side **slower than the queue message's visibility timeout**, so the same partitions get **picked repeatedly** → `init`-schedule pileup → the connection's `max_connection=5` slots clog (the exact "picked up a second time / zombies" failure the connection's own comment warns about). A self-healing babysitter auto-cleared the clog 118× over 6h before the executor was OOM-killed (exit 137). Landed data stayed clean throughout (dedup).

**Fix for a clean full backfill** (connector change, separate task): apply the existing `zombie_timeout_override` (7200s) as the **queue message visibility timeout** on pick, so a message cannot resurface mid-report. Then a babysat run completes the full history.

## Status & next steps

- **Connector EU support**: done (PR #125, on `development`/`:development`). Prod = `master`, deferred.
- **Eclipse path**: PROVEN + validated. Milestone complete.
- **TEST**: torn down to baseline (scan on, evening block enforced, queues purged, executor stopped). Nothing in PROD.
- **Prod promotion DEFERRED** — UK promotes bundled with the full navira-roadmap (gated on the v3-absorption tripwire, [[GP-199]]/[[GP-200]]), never standalone.
- **Open**: (1) connector visibility-timeout fix → clean full backfill; (2) commit the UK template to clients `main`; (3) rotate `AMAZON_ADS_CLIENT_SECRET` (shared in-chat); (4) refresh the workstation ghcr token.

## See Also

- [[amazon-ads]] — connector (EU-region support added here)
- [[eclipse]] — dispatch mechanics, scoped-agent + stage-slot pattern, zombie/visibility-timeout gotcha
- [[core_api]] — `work_pick` / work lifecycle, GP-277 pick-fix
- [[GP-277]] — `work_pick` connection-scoping fix (Fix A) + dtype-tolerant schema versioning (Fix 2)
- [[GP-199]] / [[GP-200]] — Amazon SB purchased-product + UK orders (prod); the roadmap tripwire
- [[Snowflake]] — `TEST_DG1_GEP.AMAZON_ADS` landing target
