---
tags: [ticket, gep, snowflake, data-share, connector]
aliases: [Navira Snowflake Ingestion, Snowflake-to-Snowflake Connector]
sources: [jira, orchestrator-production-readiness.md, dm-justin-gp261-snowflake-share.md]
created: 2026-05-25
updated: 2026-06-10
status: in-progress
assignee: Paul Russell
---

# GP-261: Configure Navira Snowflake to ALDC for Ingestion

**Jira:** [GP-261](https://analyticlabsdc.atlassian.net/browse/GP-261)
**Status:** In Progress (Development)
**Assignee:** Paul Russell
**Collaborator:** Justin Shuster (jshuster@navira.io)

## Summary

Define and configure an ingestion pipeline from Navira's Snowflake instance to ALDC. First Snowflake-to-Snowflake connector — all existing connectors are API → Python → Snowflake. Navira exposes **secure views** in an `ALDC_SHARE` schema.

## 2026-06-10 (session 2) — Dashboard semantics validated by reverse-engineering the mockup; ASIN-count floor

**Consumer decided:** PBI Test Data Model (consistent with Navira marketing GP-225/226/277), potentially surfaced in **Eclipse 2.1** Liquid App. The `REPORT_COMMON` views serve both surfaces unchanged — Eclipse would read the same model. See [[project_analytics_stack]].

**`REPORT_COMMON.NAVIRA_MAP_VIOLATORS_OVERVIEW` rebuilt** with three evidence-gated semantics (all deployed to non-prod, no-regression proven — 787 sellers identical, headline `PCT_TIMES_MAP_VIOLATORS` unchanged for every seller):
- `SELLING_FBA` = ANY=TRUE (`MAX`) — dominant-brand pick was arbitrary for only 35/787 sellers whose flag disagrees.
- Per-day averages: denominator = `COUNT(DISTINCT CAPTURE_DATE)`, not the (date×brand) observation count — identical for 643/787 single-row-per-day sellers, but per-observation badly understated multi-brand sellers (e.g. 4.76 vs 165.67 avg ASINs).
- Raw additive measures + `BRANDS_90D`/`CAPTURE_DAYS_90D`/`OBSERVATIONS_90D` exposed so a PBI/Eclipse consumer can filter & sort without a redeploy. Computed straight from windowed history (reconciles to a rollup of the seller×brand fact) so seller-level `CAPTURE_DAYS` (a distinct count, non-additive) is correct.

**The "everyone's a 100% violator" problem — root-caused and fixed.** It was a metric/presentation issue, not a population one. Key findings from the data:
- `TOTAL_APPEARANCES` is a **daily price-check count (stock-like)**, an order of magnitude larger than the catalog (e.g. 389 ASINs listed vs ~4,200 appearances/day). Summing it over 90d inflates absolute volume; the **ratio** `viol/appr` survives because both sides inflate equally.
- **431 of 787 sellers (55%) had zero violations in the trailing 90 days.** Of the rest, the 92 sellers stuck at exactly 100% are **single-ASIN sellers** (avg **1.5 ASINs**) with one perpetually-mispriced item — noise. Real businesses average **11.7 ASINs**.
- **Reverse-engineered Justin's mockup against live data** (`screenshots/MAPS_Violator_Visual_data_structure_module_navira_snowflake_data_share.png`). The mockup's seller IDs are placeholders, but its exact column headers + dummy magnitudes (9–32% rate, 150–300 ASINs listed, 7–31 violating) confirm all three column formulas. Ranking real sellers by volume reproduces the mockup exactly — named businesses (Amazon, Sweetwater Sound, Whole9yards) land at mid-range % with hundreds of ASINs.

**Floor swapped from appearances to catalog size.** The min-appearances floor (N≥10) was the wrong lever — the noise has ~1,100 appearances but only 1.5 ASINs. Replaced with `MEETS_MIN_ASINS_LISTED` = **avg ASINs/day ≥ 3**: drops 83 of the 92 always-100% sellers while keeping every mockup-shaped real business (289 sellers survive; the 9 remaining 100% sellers have ≥3-ASIN catalogs all mispriced — legitimate worst offenders). Threshold is tunable in the BI layer. Worst-violators ranking = `PCT DESC, SUM_APPEARANCES_90D DESC`.

**Tooling (warehouse_ops/, throwaway `_`-prefixed):** `_navira_map_violators_threshold_explore.py`, `_navira_map_violators_asin_floor.py` (distribution evidence), `_navira_map_violators_overview_validate.py` (reusable no-regression diff: snapshot → targeted view redeploy → diff).

**Drafted for Navira (Justin/Heather):** `aldc-launchpad/dm-justin-gp261-maps-metric-questions.md` — leads with what we built, then asks to confirm population, the "% times" denominator, the worst-violators ranking, and the catalog-size floor.

**Next:** final validation pass tomorrow (parity + no-regression + consumer-layer) → send to **Lori** for review → pending sign-off, **promote to TEST Snowflake + the Test PBI Model for client (Navira) UAT** (via the standard promotion path, not ad-hoc). GP-261 Jira updated with this plan (comment 2026-06-10).

## 2026-06-10 — Share MOUNTED + MAP Violators pipeline & watchdog deployed (non-prod)

**Share is live in BOTH environments.** Justin (Navira) set up a **cross-region private listing** (`NAVIRA_ALDC_SHARE`, global name `GZT1Z4E98YD`) — his source is `AWS_US_EAST_2`, we're `AZURE_CANADACENTRAL`, so a direct share was impossible and it went via a listing with **cross-cloud auto-fulfillment**.

- **Mount mechanics:** a cross-region listing must be **fulfilled (replicated) into the consumer's region** before it can be mounted. The non-prod account had never "Get"-requested it (`is_ready_for_import=false`); requesting it triggered fulfillment (Snowflake "finished syncing" email), after which `CREATE DATABASE NAVIRA_SHARE FROM LISTING 'GZT1Z4E98YD'` succeeded. Prod had already been requested earlier, so it was already fulfilled. **Lesson:** for a cross-cloud listing, the *consumer* must Request/Get it (per-account, per-env); the provider's auto-fulfillment then replicates. `SHOW SHARES` does **not** show it as an inbound share — it's a listing, found via `SHOW AVAILABLE LISTINGS` (filter `is_targeted=true`).
- **Mounted as** `NAVIRA_SHARE` (imported DB, origin `ZWIRVQE.AUTO_FULFILLMENT_AREA$PUBLIC_AZURE_CANADACENTRAL.ALDC_SHARE`) in both `OG35375` (non-prod) and `WJ66376` (prod). Tooling: `aldc-launchpad/warehouse_ops/_navira_share_mount.py` (probe / mount / inspect).

**Three views are shared** (not just the originally-anticipated MAPS_VIOLATOR), all in `NAVIRA_SHARE.ALDC_SHARE`. Validated row counts (identical both envs):

| View | Rows | Key | Cadence | Date column (TZ-naive, **Eastern**) |
|---|---|---|---|---|
| `NAVIRA_BP_MAP_VIOLATORS_VW` | 332,330 | (CAPTURE_DATE, BRAND, SELLER_ID) | daily ~4AM ET (pull 5AM) | `CAPTURE_DATE` 2025-05-18 → 2026-06-09 |
| `NAVIRA_INVENTORY_VW` | 3,830 | MASTER_SKU | every ~2h (live snapshot) | `CAPTURE_DATE` |
| `NAVIRA_MASTER_SKU_STATUS_VW` | 15,468 | MASTER_SKU | every ~2h | `LAST_STATUS_CHANGED_DT` |

**Scope decision:** focus on **MAP Violators only** for now; `NAVIRA_INVENTORY_VW` + `NAVIRA_MASTER_SKU_STATUS_VW` are mounted/available but deferred to a **future ticket**.

**Deployed to non-prod `TEST_DG1_GEP` (validation only; everything SUSPENDED, prod ships via promotion).** Migration: `aldc-launchpad/warehouse/migrations/gep/navira_map_violators.sql`. Follows GEP convention (WAREHOUSE_SOURCE → WAREHOUSE → REPORT_COMMON):

1. `WAREHOUSE_SOURCE.NAVIRA_MAP_VIOLATORS_SRC` — secure view over the live share (pointer).
2. `WAREHOUSE.NAVIRA_MAP_VIOLATORS` — durable physical history, MERGE-accumulated on the key, `DATA_AS_OF` stamp (stale-but-available). Backfilled 332,330 rows; parity with share = MATCH.
3. `WAREHOUSE.REFRESH_NAVIRA_MAP_VIOLATORS` — daily task 05:30 ET (MERGE; de-dups Justin's trailing-3-day re-push). **Suspended.**
4. `REPORT_COMMON.NAVIRA_MAP_VIOLATORS_SELLER_BRAND_90D` — **additive-measure** fact at seller×brand grain, trailing 90 capture-days. Ratios deliberately kept OUT so a BRAND filter rolls up via ratio-of-sums (not avg-of-avgs).
5. `REPORT_COMMON.NAVIRA_MAP_VIOLATORS_OVERVIEW` — seller-level rollup matching Justin's mockup columns (Seller ID, Other Seller Name, Business Name, Address, Selling FBA, % times MAP violators, avg ASINs listed, avg ASINs w/ violations). %-sanity recompute = MATCH. 787 sellers in 90d window.

**Watchdog** (same migration, hardening layer): `OPS.SHARE_SYNC_HEALTH` log table + `OPS.SP_NAVIRA_MAP_VIOLATORS_HEALTHCHECK()` (checks: SHARE_AVAILABLE / STALENESS / SOURCE_FROZEN) + hourly task `OPS.WATCH_NAVIRA_MAP_VIOLATORS` (**suspended**). Email via `SYSTEM$SEND_EMAIL` is inert until a notification integration (`ALDC_ALERT_EMAIL_INT`) is created — until then WARN/ALERT still logs to the table (which the ops dashboard reads) + an `ALERT_DELIVERY` note. Validated: all checks OK; frozen-source guard correctly does NOT false-alarm on an unchanged daily source within cadence.

**Open design calls for the dashboard:** minimum-appearances threshold (the raw top-violators list is dominated by one-hit low-volume sellers at 100%); averaging denominator (currently per-observation); seller-level `SELLING_FBA` for multi-brand sellers (currently dominant-brand). Brand rollup = per-seller display + BRAND filter (default all); window = trailing 90 days — both confirmed by Paul.

**Next (new session):** continue MAPS features; then end-of-session — put GP-261 work on a feature branch and **promote through the GEP path (development → higher)**; prod gets the pipeline via promotion, NOT ad-hoc. Resume tasks after promotion. Consider creating `ALDC_ALERT_EMAIL_INT` for live alerting.

## Revised Decision (2026-06-01) — Native Secure Data Share + Local Materialization

> **Supersedes the 2026-05-25 Prefect decision below.** Prefect was shelved org-wide (2026-05-28, see [[project_prefect_shelved]] / [[prefect]]), which removed Option C. Re-evaluated the remaining options and selected **Option B: native Snowflake Secure Data Share + scheduled local materialization**, explicitly hardened to address the share failure modes that drove the original Prefect choice.

**Why Option B is now correct (and the failure modes are mitigated, not ignored):**

| Original objection to shares | How the hardened design answers it |
|---|---|
| 42-day frozen share went undetected (Sellercloud staleness) | Watchdog task compares row count + `HASH_AGG` content hash run-over-run; a frozen source raises a staleness alert |
| `CREATE OR REPLACE TABLE` silently drops share grants | That was on ALDC's **provider** side; here ALDC is the **consumer**. Watchdog still probes share availability (`SHOW SHARES` + `SELECT 1` in a TRY block) and alerts if the inbound share disappears |
| Cross-database grant loss caused a 14h outage | `REFERENCE_USAGE` requirement for multi-DB views is called out explicitly in the Justin guide; single-DB views are unaffected |
| Pure share = no data if share drops | **Local materialization** layer: reports read our own snapshot, never the live share — a dropped share degrades to *stale-but-available*, never broken |

**Why not Prefect anymore:** shelved org-wide; new connectors use Eclipse. A native share is also lower-maintenance than an ETL flow for Snowflake → Snowflake (zero-copy, zero compute, no credentials/egress).

### Target architecture (3 layers)

```
NAVIRA_SHARE (imported DB, live read-only)         ← never read by reports
        │  scheduled serverless TASK (full-snapshot CTAS; incremental MERGE only if a view exposes key + change column)
        ▼
{ENV}_DG1_GEP.NAVIRA_RAW (our own physical tables) ← durable, survives a share drop; retains prior snapshot
        ▼
WAREHOUSE_SOURCE views → existing warehouse pipeline ← read only from NAVIRA_RAW
```

- **Mitigation (stale-but-available):** `NAVIRA_RAW` is the durable copy; reports keep serving the last good snapshot, labelled with a `data_as_of` timestamp.
- **Detection:** `OPS.SHARE_SYNC_HEALTH` log table + an hourly watchdog task → `SYSTEM$SEND_EMAIL` (notification integration) + optional Slack/Teams webhook, surfaced on the Launchpad ops dashboard. Three checks: share-down (probe error / not in `SHOW SHARES`), staleness (last good sync older than 2× cadence), source-frozen (row count drop or unchanged content hash).
- **Derived views like `MAPS_VIOLATOR`** likely have no stable change key → default to full-snapshot CTAS each cycle with prior-snapshot retention.

### Environment identity (confirmed live 2026-06-01)

| Env | Org-qualified | Locator | Region |
|---|---|---|---|
| Non-prod (QA/Test/UAT) | `LSUBOGT.YX94045` | `OG35375` | `AZURE_CANADACENTRAL` |
| Prod | `LSUBOGT.MY89318` | `WJ66376` | `AZURE_CANADACENTRAL` |

Both same org + region → a **direct share** works if Navira is also on `AZURE_CANADACENTRAL`; otherwise a **private listing with cross-cloud auto-fulfillment**. See [[reference_aldc_snowflake_identity]] (Launchpad memory) for the source query. Identity query verified working on both accounts via `scripts/_test_identity_query.py`.

## What Navira provides (sent to Justin 2026-06-01)

Setup guide drafted at `aldc-launchpad/dm-justin-gp261-snowflake-share.md` — leads with a **no-SQL Snowsight UI path**, SQL fallback for multi-DB views, and a same-region decision rule. Asks Justin for:

1. Account identity (`CURRENT_ORGANIZATION_NAME/ACCOUNT_NAME/ACCOUNT/REGION`) → determines direct share vs. listing
2. Database/schema + list of secure views to share (e.g. `MAPS_VIOLATOR`)
3. Refresh cadence, timestamp timezone (UTC preferred), historical scope
4. Per-view: stable business key + last-updated column (if any) → incremental vs. full-snapshot
5. Confirmation that the share is created and both ALDC accounts (`OG35375`, `WJ66376`) are added

Two Snowflake rules flagged to Justin: shared views **must be SECURE**; multi-database views need a `REFERENCE_USAGE` grant per extra DB.

## Next Steps

- [x] ~~Send the guide to Justin~~ — sent 2026-06-01
- [x] ~~Confirm direct-share vs. listing path~~ — **cross-region listing** (Navira on `AWS_US_EAST_2`)
- [x] ~~Accept/mount the share, validate objects/row counts~~ — mounted + validated in **both** envs 2026-06-10
- [x] ~~Scaffold ALDC-side SQL~~ — pipeline + watchdog deployed to non-prod `TEST_DG1_GEP` (migration `navira_map_violators.sql`)
- [x] ~~Confirm object list + row counts + as-of back to Justin~~ — drafted (both envs)
- [x] ~~MAPS dashboard semantics~~ — three columns reverse-engineered + confirmed vs mockup; FBA/avg/floor settled (2026-06-10 s2)
- [x] ~~Consumer decision~~ — **PBI Test Data Model** (possibly Eclipse 2.1)
- [x] ~~Min-appearances threshold~~ — superseded by **catalog-size floor** (avg ASINs/day ≥ 3); appearances was the wrong lever
- [ ] **Tomorrow:** final validation pass (parity + no-regression + consumer-layer) → send to **Lori** for review
- [ ] Pending Lori/Navira sign-off: feature branch + promote to **TEST Snowflake + Test PBI Model for Navira UAT** (standard promotion path, NOT ad-hoc); resume tasks after promotion
- [ ] Await Navira reply (`dm-justin-gp261-maps-metric-questions.md`) confirming ranking + floor
- [ ] Future ticket: ingest `NAVIRA_INVENTORY_VW` + `NAVIRA_MASTER_SKU_STATUS_VW`
- [ ] Optional: create `ALDC_ALERT_EMAIL_INT` notification integration for live watchdog email

## Open Questions

- [x] ~~Navira's cloud/region~~ — `ZWIRVQE.KF08603`, locator `TW91345`, **`AWS_US_EAST_2`**
- [x] ~~List of views/schemas~~ — 3 views in `ALDC_SHARE` (see table above)
- [x] ~~Refresh frequency~~ — violators daily ~4AM ET; inventory + SKU-status every ~2h
- [x] ~~MAP Violators consumer~~ — **PBI Test Data Model** (possibly surfaced in Eclipse 2.1)
- [x] ~~Min-appearances threshold N~~ — replaced by catalog-size floor (avg ASINs/day ≥ 3); confirm exact N with Navira (mockup examples were larger sellers, so ≥5/≥10 possible)
- [ ] Navira to confirm "worst violators" ranking (rate vs volume vs ASIN breadth vs persistence)

---

## Superseded — Architecture Decision (2026-05-25)

> Retained for history. Selected Prefect (Option C) before Prefect was shelved; see Revised Decision above.

Three options evaluated:

| Option | Approach | Verdict (2026-05-25) |
|---|---|---|
| A | Direct Data Share (Navira → ALDC) | Rejected — fragile, cross-region blocker, 3 documented share failure modes |
| B | Direct Share + Local Materialization | Rejected then — "still depends on share, double complexity". **Now selected (2026-06-01)** with hardening |
| C | Prefect ETL Pipeline (pull-based) | Selected then — **no longer available, Prefect shelved 2026-05-28** |

**Full original analysis:** `C:\Users\PaulRussell\.claude\plans\whimsical-honking-clarke.md`

Documented share failure modes that informed the original rejection (now mitigated, see table above): Eclipse `CREATE OR REPLACE TABLE` silently dropping share grants (4 incidents, April 2026); a 42-day frozen share going undetected (Sellercloud staleness); cross-database grant loss causing a 14-hour production outage (May 2026).

## Timeline

- 2026-05-25: Email sent to Justin with Prefect recommendation (Option C)
- 2026-05-28: Prefect shelved org-wide → Option C invalidated
- 2026-06-01: Decision revised to native Secure Data Share + local materialization (Option B, hardened); Snowsight setup guide drafted + sent to Justin (email + PDF)
- 2026-06-03: Justin created cross-region private listing `NAVIRA_ALDC_SHARE` (global `GZT1Z4E98YD`), targeted at both ALDC accounts
- 2026-06-10: Requested + fulfilled to our region; **mounted `NAVIRA_SHARE` in both envs**; 3 views validated; MAP Violators pipeline (source view → MERGE history → seller×brand fact → overview) + share-health watchdog deployed to non-prod `TEST_DG1_GEP` (all suspended); scoped to MAP Violators (inventory deferred)
- 2026-06-10 (session 2): dashboard semantics reverse-engineered from Justin's mockup against live data + confirmed; OVERVIEW rebuilt (ANY=TRUE FBA, per-day averages, exposed measures); 100%-violator saturation root-caused (single-ASIN noise) → floor swapped from appearances to **catalog size (avg ASINs/day ≥ 3)**; consumer = PBI Test Data Model (poss. Eclipse 2.1); Jira updated
- Next: validation tomorrow → Lori review → (pending sign-off) feature branch + promote to TEST Snowflake + Test PBI Model for Navira UAT

## Related

- [[snowflake-data-share-refresh]] — `CREATE OR REPLACE TABLE` semantics across a share; mitigation for transient failures
- [[data-share-pattern]] — Data share setup patterns (inbound/outbound, secure view rules)
- [[GP-207]] — ALDC prod-to-test data share (outbound, existing)
- [[GP-208]] — Inventory modelling (share staleness incident)
- [[GP-219]] — Sellercloud migration
- [[project_prefect_shelved]] — Prefect shelved org-wide (2026-05-28)
- [[snowflake]] — ALDC Snowflake platform reference
