---
tags: [ticket, gep, amazon-ads, sponsored-brands, asin, marketing, scope]
aliases: [GP-199, ASIN Brand Campaign Attribution]
sources: [Jira GP-199, connector/connector/connectors/amazon_ads.py, clients/GEP/snowflake/warehouse/marketing_fct_activity.sql]
created: 2026-04-30
updated: 2026-05-01
---

# GP-199 — ASIN Brand Campaign Attribution

**Type:** Scope | **Status:** Design approved — ready for implementation | **Priority:** Top (Lori, 2026-04-28)

## Problem

Sponsored Brands campaign spend is attributed with `PRODUCT_ID = '-1'` in `marketing_fct_activity.sql`. Downstream, spend is distributed evenly across ALL ASINs in a brand via portfolio lookup. A campaign targeting 3 ASINs has its cost diluted across 50+ products, making per-product profitability inaccurate.

Sponsored Products already has accurate per-ASIN attribution via `ADVERTISEDSKU` in the Advertised Product Report.

## Key Research Findings

1. **Amazon does not provide per-ASIN spend reporting for Sponsored Brands** — confirmed API limitation because a single SB ad can feature multiple ASINs. No `sbAdvertisedProduct` report type exists (unlike `spAdvertisedProduct` for SP).

2. **Two data sources enable attribution:**
   - **Ad Metadata API** (`/sb/v4/ads/list`) — returns `creative.asins` per ad (targeting data). Connector does NOT support this yet.
   - **Purchased Product Report** (`sbPurchasedProduct`) — returns `purchasedAsin` per campaign (conversion data). Connector already supports this.

3. **SB Ads Report** (`sbAds`, category `ad_report`) — provides daily metrics per `adId` (impressions, clicks, cost). Connector already supports this but no Eclipse template exists for GEP.

4. The Eclipse `amazon_ads_v1` connector already supports SB categories: `campaign`, `campaign_report`, `ad_report`, `purchased_product_report`. Only `campaign` and `campaign_report` have GEP templates today.

## Approved Design (Justin Shuster, 2026-05-01)

**Approach A (Ad Creative Attribution)** — confirmed via email 2026-05-01.

### Allocation Formula (Confirmed)

Three-step model:

**Step 1 — Ad to ASINs (equal split):**
Split the ad's daily spend equally across all ASINs in that ad's creative.
> Example: Ad has 3 ASINs, $90 spend that day → $30 per ASIN

**Step 2 — ASIN to Orders (unit-ratio weighted):**
Distribute each ASIN's allocated spend across its individual orders that day, weighted by units.
> Example: ASIN-A allocated $30, had two orders (8 units and 2 units) → $24 and $6

**Step 3 — No sales (UNK bucket):**
If an ASIN in the ad had zero orders that day, it keeps its equal share as unattributed spend.
> Example: ASIN-C allocated $30, zero orders → $30 recorded as UNK spend

Justin explicitly confirmed: "do not weigh the initial split... evenly distribute across ASINs first."

### Historical Data (Confirmed)

**Option A — no backfill.** Existing SB data stays as-is (brand-level split). New ASIN-level logic applies from the go-live date — the date we start capturing daily ad-ASIN snapshots. Justin: "good with a backfill from 'go live' or whenever you start capturing the relationship between date => Ad => ASINs."

### Non-ASIN SB Ads Fallback

SB ads with no ASIN attribution at all (e.g., Store Spotlight, Video) continue current behavior: split evenly across the brand. Justin questions whether this case exists — verify during implementation.

### Daily Snapshot Requirement

Each day's ad-to-ASIN composition must be captured so that future ad changes don't retroactively alter past attribution. Justin: "if an ad changes in a month, it should not change what was being advertised today."

## Current State (marketing_fct_activity.sql)

- **Sponsored Products:** Uses `CURRENT_SPONSORED_PRODUCTS_ADVERTISED_PRODUCT_REPORT`, `PRODUCT_ID = ADVERTISEDSKU`, `AD_GROUP_ID` populated
- **Sponsored Brands:** Uses `CURRENT_SPONSORED_BRANDS_CAMPAIGN_REPORT`, `PRODUCT_ID = '-1'`, `AD_GROUP_ID = 'Unknown'`

## Schema Changes

**New raw tables** (auto-created by Eclipse):
- `AMAZON_ADS.CURRENT_SPONSORED_BRANDS_AD` — ad metadata with `creative.asins`
- `AMAZON_ADS.CURRENT_SPONSORED_BRANDS_AD_REPORT` — daily metrics per adId

**New warehouse views:**
- `WAREHOUSE_SOURCE.EXTRACT_AMAZON_ADS_SB_AD_ASIN_MAP` — flattened ad-to-ASIN mapping via `LATERAL FLATTEN` on `creative.asins`. Must preserve daily snapshots (not just latest).
- `WAREHOUSE_SOURCE.EXTRACT_AMAZON_ADS_SB_AD_DAILY_SPEND` — daily ad-level spend from `AD_REPORT`, joined to ASIN map for that date

**Updated view:**
- `marketing_fct_activity.sql` SB section: replace campaign-level query with ad-level query joined to ASIN map. Implement three-step allocation (equal split → unit-ratio → UNK).

**UNK handling:** Design needed — either:
- A synthetic `PRODUCT_ID = 'UNK'` row in `marketing_fct_activity` for spend on ASINs with no matching sales
- Or a separate unattributed-spend bucket that downstream PBI reports can surface

## Connector Changes

File: `connector/connector/connectors/amazon_ads.py`
- Add `SPONSORED_BRANDS_ADS_LIST_ENDPOINT = f"{ADS_API_BASE_URL}/sb/v4/ads/list"`
- Add `"ad"` category handler for SB (mirrors existing `"campaign"` pattern)

## Implementation Plan

1. ✅ **Connector enhancement** — Added `SPONSORED_BRANDS_ADS_LIST_ENDPOINT` + `"ad"` category handler to `amazon_ads.py`. Key finding: `remove_all_lists()` strips Python lists — serialized `creative["asins"]` to JSON string before `dataframe_from_list_path` so it survives as `CREATIVE_ASINS VARCHAR` in Snowflake.
2. ✅ **Eclipse templates** — Created `sponsored_brands_ad.json` (full partition, category `ad`) and `sponsored_brands_ad_report.json` (date partition, category `ad_report`, min_date 2026-05-01).
3. ✅ **Warehouse SQL** — Created `extract_amazon_ads_sb_ad_asin_map.sql` (queries `COMBINED_SPONSORED_BRANDS_AD` using validity range: `VALID_FROM`/`VALID_UNTIL` from `HISTORY_TIMESTAMP_START/END`; COMBINED_* is a SECURE VIEW, not a table; merge_strategy=add only inserts on change not every run), `extract_amazon_ads_sb_ad_daily_spend.sql` (equal-split Step 1 via validity range join), and rewrote `marketing_fct_activity.sql` SB section as a 3-part union (ASIN-attributed + non-ASIN fallback + pre-go-live legacy).
4. ⬜ **Verify non-ASIN SB ads** — Check whether Store Spotlight / Video ads exist in GEP's data during sandbox testing (Q1 open question).
5. ⬜ **Go-live cutover** — Deploy Eclipse templates, run once to populate tables, then deploy SQL. Everything before go-live stays at brand-level grain.

**Branch:** `feature/paulrussell/gp-199/asin-brand-campaign-attribution`

**Estimated effort:** ~20-28 hrs (original 17-22 + UNK handling)

## Design Comment History

- Jira comment (Paul, 2026-04-30): Full design document with comparison table, recommending Approach A.
- Email (Justin Shuster, 2026-05-01): Approved Approach A. Confirmed three-step allocation, Option A (no backfill), UNK handling.
- Jira comment #27008 (Paul, 2026-05-01): Locked-in design summary posted to ticket.

## Boot Prompt (Sonnet Execution Session)

```
You are implementing **GP-199 — ASIN Brand Campaign Attribution** for GEP. Design is fully approved by the client. Use `/gep-feature` skill.

Boot procedure:
1. Read `C:\Users\PaulRussell\repos\wiki\CLAUDE.md`
2. Read this ticket page: `C:\Users\PaulRussell\repos\wiki\tickets\gep\GP-199.md` — the "Approved Design" section is the spec. Pay close attention to the three-step allocation formula and UNK handling requirement.
3. Read required context (parallel reads):
   - `C:\Users\PaulRussell\repos\wiki\entities\tools\amazon-ads.md` — connector docs
   - `C:\Users\PaulRussell\repos\wiki\concepts\architecture\star-schema-convention.md` — naming conventions
   - `C:\Users\PaulRussell\repos\wiki\tickets\gep\GP-169.md` — related marketing attribution pattern
4. Read current source files:
   - `connector/connector/connectors/amazon_ads.py` — connector to enhance
   - `clients/GEP/snowflake/warehouse/marketing_fct_activity.sql` — view to rewrite
   - `clients/GEP/eclipse/` — existing GEP templates (for naming/structure reference)
5. Pick up at the Implementation Plan in GP-199.md.

**Scope:** Approach A — Ad Creative Attribution. Three-step allocation: equal split ad→ASINs, unit-ratio ASIN→orders, UNK for zero-sale ASINs. No historical backfill — new logic from go-live date forward. Non-ASIN SB ads fall back to brand-level split.

**Key constraints:**
- Connector change: add `/sb/v4/ads/list` endpoint + "ad" category handler for SB
- Eclipse: two new GEP templates (SB Ad metadata + SB Ad Report)
- Warehouse SQL: ASIN map view (daily snapshots via LATERAL FLATTEN), daily spend view, marketing_fct_activity.sql SB section rewrite + UNK handling
- All development continues on ticket GP-199 (no sub-tickets)

When done with each implementation step, update the GP-199.md wiki page.
```

## See Also

- [[GP-169]] — Marketing activity marketplace attribution (related pattern)
- [[amazon-ads]] — Amazon Ads connector docs
- [[phase-1a-marketing-ad-platforms]] — Phase 1A context
- [[gep-inventory-data-dictionary]] — Reference for existing schema patterns
- [[star-schema-convention]] — ALDC naming conventions
