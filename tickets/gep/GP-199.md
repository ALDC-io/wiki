---
tags: [ticket, gep, amazon-ads, sponsored-brands, asin, marketing, scope]
aliases: [GP-199, ASIN Brand Campaign Attribution]
sources: [Jira GP-199, connector/connector/connectors/amazon_ads.py, clients/GEP/snowflake/warehouse/marketing_fct_activity.sql]
created: 2026-04-30
updated: 2026-06-11
---

# GP-199 — ASIN Brand Campaign Attribution

**Type:** Scope | **Status:** ✅ **DONE — validated + signed off 2026-06-11 (Jira → Done).** v3 dual-source (real per-purchased-ASIN) fact live in PROD `WAREHOUSE.MARKETING_FCT_ACTIVITY`; `_V2` guard dropped; TEST repointed. Deep-validated at the PBI consumer layer incl. a before/after no-regression proof + ASIN column-quality pass; daily 15:30 UTC Ofelia monitor live. `main` reconciliation in PR #485 (CI-green, pending code-owner review — blocked by stale CODEOWNERS, see below). | **Priority:** Top (Lori, 2026-04-28)

> **2026-06-11 — Final prod validation + sign-off (Jira GP-199 → Done).**
> - **Health re-check:** monitor ALL GREEN (0 mismatch / 0 drops over 1,236 trailing campaign-days; freshness 0d); PBI "Data Model" refresh Completed after the swap.
> - **Deep validation (consumer/PBI layer):** TRAVALO `AMZ_296392704346091` 5/28 = $7.57 / $114.99 / 5 / 8 — identical in Snowflake and the PBI model. **ASIN column-quality pass** over 3,079 SB real-ASIN rows: zero null/blank/sentinel in any dimension or measure; 224 distinct ASINs; halo pattern by design.
> - **Before/after no-regression proof:** reconstructed the pre-swap spend-weighted fact as a scratch view and diffed vs live v3 on identical data — every aggregate byte-identical (cost/sales/conv/clicks/impressions Δ 0), Sponsored **Products** branch untouched. New behaviour = 637 $0-spend halo rows carrying real attributed sales + per-ASIN distribution restated to real purchases (campaign-day totals preserved). Scripts: `aldc-launchpad/warehouse_ops/_gp199_gp200_deep_validation.py`, `_gp199_before_after.py`.
> - **Monitor auto-fire fixed + merged:** root cause was the running `obs-jobs` container having **no Ofelia labels** + an Ofelia **6-field cron** parsing bug (5-field `30 15 * * *` would fire hourly at HH:15:30, not daily). Corrected to `0 30 15 * * *` (daily 15:30 UTC); Ofelia registered `check-gp199`; `gp199_*` metrics live in Pushgateway. Shipped in [[observability]] **PR #1 (merged)**.
> - **pbi_ops** gained a first-class `verify --check attribution` (TRAVALO anchor + SB null/halo scan at the model layer) and "Marketing Activity" in the table probe.
> - **⚠ Merge blocker (housekeeping only — prod is live):** PR #485 reconciles `main` to prod but the `clients` CODEOWNERS lists only departed staff (`@brayden-marshall`, `@strinsberg`) + the PR author (`@russell94paul`), so no eligible reviewer remains. Needs CODEOWNERS updated to a current dev (`data-mission`/Vlad, or Mike Stuart) or an admin bypass. Tracked separately; does not affect the live feature.
>
> **2026-06-10 — PROMOTED v3 to the live prod fact (Paul-directed, ahead of formal Justin sign-off).** Swapped `WAREHOUSE_SOURCE.MARKETING_FCT_ACTIVITY` from Phase-1 spend-weighted to the v3 dual-source body (real `sbPurchasedProduct` where mature, spend-weighted fallback otherwise). Done as `PROD_DG1_CORE_ADMIN` on wj66376.
> - **Swap invariant proven:** ran as `PROD_DG1_ROLE_CORE_SVC_DA8904DB` (ownership preserved); pre/post grand totals **delta $0.00** (cost $2,264,237.28 / sales $11,484,362 / conv 293,133); rows 816,426 → 817,033 (+607 halo splits). Live fact now refs PURCHASED_PRODUCT.
> - **Reconciliation to Amazon source:** 1,235+ SB campaign-days, **0 sales mismatch / 0 drops**. TRAVALO `296392704346091` 5/28 = **$7.57 / $114.99 / 5** across **8 real ASIN rows** — in Snowflake AND at the PBI model layer. (Fact keys SB campaigns as `AMZ_<campaignid>`.) The ~6 cost deltas vs the campaign report are the pre-existing ad-report-vs-campaign-report source difference; v3 left COST untouched.
> - **Cleanup:** TEST (`og35375`) `MARKETING_FCT_ACTIVITY` repointed from `_V2` → live fact; `_V2` revoked from share + dropped (both schemas). `EXTRACT_AMAZON_ADS_SB_PURCHASED_PRODUCT` **kept** (live v3 depends on it) — so this is NOT the wholesale `GUARD_rollback`.
> - **Grants gotcha:** swapping a view drops ownership/share grants unless run as CORE_SVC / `COPY GRANTS`. Restored owner=CORE_SVC + SHARE + REPORT_SVC. Same lesson as [[GP-200]].
> - **Monitoring:** `observability/jobs/check_gp199_attribution.py` (Slack-alerting, daily 15:30 UTC via Ofelia) — P1 if SB sales stop reconciling to Amazon, P2 on stale raw pulls; tracks the **14-day maturation** (recent days fill real attribution by ~2026-06-24). Green baseline confirmed; metrics flow to Pushgateway/Grafana. (Ofelia auto-fire pending a local Docker clock fix; job itself proven.)
> - **`main` source-of-truth:** GP-199 warehouse SQL was never in `main` (`extract_amazon_ads_sb_ad_asin_map`, `..._daily_spend`, `..._purchased_product` absent; `marketing_fct_activity` not v3). **PR #485** captures the 4 objects from prod `GET_DDL` so `main` == prod.
> - **⚠ CONVERGENCE GAP (navira-roadmap):** the unified multi-platform marketing fact (Amazon ∪ Google/Meta/Windsor + entity) lives **only in the TEST sandbox** (`WAREHOUSE_TEST_GP226`) and contains **0 v3 markers**. It must **absorb v3** before it promotes to prod, or it regresses GP-199. PR #485 puts v3 in `main` as the deliberate reconciliation/merge-conflict tripwire. Do NOT promote `development`→`main` wholesale.
> - **Rollback:** `aldc-launchpad/warehouse_ops/_gp199_rollback/PROD_WAREHOUSE_SOURCE_MARKETING_FCT_ACTIVITY_20260610_143202.sql` (live pre-swap DDL).

> **PRIOR (2026-06-03):** GEP QA — Phase 2 deployed to PROD as a guarded `MARKETING_FCT_ACTIVITY_V2` (live prod fact untouched), validated, TEST repointed for Justin's review. Phase 1 spend-weighted remained live in the prod fact. *(Superseded by the 2026-06-10 promotion above.)*

> **2026-06-02 — Justin UAT callouts addressed (3) + Phase 2 started.** Justin reviewed the validation export and raised 3 callouts; all triaged with evidence and fixed in prod `WAREHOUSE.MARKETING_FCT_ACTIVITY`:
> 1. **Attributed Sales = $0 on ASIN rows** — real gap (SB sales went dark at go-live; Amazon provides no ad-level/per-ASIN SB sales). Fixed: **spend-weighted split** — each SB row gets `SALES/CONVERSIONS/UNITS = campaign-day total × (row cost / campaign-day ad cost)`. Reconciles to the campaign report to the penny (TRAVALO 5/28: 3 ASINs × $38.33 = $114.99 / 5 orders). Superseded an interim v1 campaign-level carrier.
> 2. **Impressions/clicks not split** — fixed: equal-split like cost in `extract_amazon_ads_sb_ad_daily_spend.sql` (fractional, sums reconcile; was inflating PBI ~2.5×).
> 3. **Backward restatement** — confirmed: SB ad-report TTL=7d, retroactive to 5/21. Deploys captured rollback DDL; share-grant + ownership restored after each `CREATE OR REPLACE`.
> Decimal formats added to `report_export.py` Marketing tab. Reply email draft: `aldc-launchpad/dm-justin-gp199-callouts-reply.md` (framed as "small adjustments", root-cause kept internal per Paul). `pbi_ops/` consolidated (was `scripts/pbi/`).
>
> **Phase 2 — real per-ASIN attribution (APPROVED, in progress):** spend-weighted is a convention; real attribution = Amazon `sbPurchasedProduct`. Connector PR [#123](https://github.com/ALDC-io/connector/pull/123) **merged** (19 cols incl. sales14d/orders14d + 5-part PK incl. purchasedAsin/attributionType; null-guard). Eclipse template `sponsored_brands_purchased_product_report.json` (min_date 2026-05-21, TTL 14). **Amazon accepted all 19 cols live (200).** When it lands, the fact swaps the sales source for the whole 5/21+ window at once (campaign totals unchanged, per-ASIN distribution restates). **BLOCKED** validating in TEST by a core_api pick bug → see [[GP-277]] / boot-prompt `gp277-fix2-consumer-validation.md`. Connector validated; can alternatively go straight to prod (healthy queue).

> **2026-06-03 — Phase 2 SHIPPED to PROD (guarded) + PROD pick-bug incident.** Real per-purchased-ASIN
> SB attribution is live behind a versioned-view guard; Justin's TEST review surface shows it; promotion pending.
> - **Connector:** cherry-picked the SB purchased-product commit onto `master` (PR #124, GP-199-only — windsorai/GP-226 excluded), rebuilt `agent-dcgeneral-coquitlam:master`. Rollback image `:218d0a9`.
> - **Landed `sbPurchasedProduct`** (5/21, 23 rows): `attributionType` = **Promoted + Brand Halo**; 0 grain-keys span >1 type → SUM-across-type SAFE; single-version table.
> - **Dual-source fact-v3** (`aldc-launchpad/warehouse_ops/_gp199_fact_v3_PROD_deploy.sql`): SB COST always equal-split (unchanged); attributed SALES = real per-purchasedAsin (incl **halo** = sales w/ $0 spend) where pp matured, **spend-weighted fallback** otherwise; both anchored to the campaign-report campaign-day total (invariant). Self-heals as pp matures (~14-day Amazon window, daily pull) — view recomputed per query.
> - **GUARD:** deployed as `MARKETING_FCT_ACTIVITY_V2` + `EXTRACT_AMAZON_ADS_SB_PURCHASED_PRODUCT` (CORE_SVC-owned, granted to `PROD_DG1_GEP` share); **prod fact `MARKETING_FCT_ACTIVITY` never touched** → prod client "Data Model" provably unaffected. TEST view repointed → V2 for Justin. Rollbacks in `aldc-launchpad/warehouse_ops/_gp199_rollback/`.
> - **Validated:** v3 vs deployed fact same-session = 769 campaign-days, 0 mismatches (totals preserved); **Justin's TRAVALO campaign 296392704346091 5/28 reconciles to $114.99/$7.57/5 orders** at the PBI measure layer; non-marketing tabs byte-identical (proven via LAST_ALTERED). Reply draft: `aldc-launchpad/dm-justin-gp199-phase2-real-attribution.md`.
> - **⚠ INCIDENT:** the [[GP-277]] pick bug is **PROD-wide** (fix only on TEST core_api) — a `connection_authorized` scoped agent is NOT isolated in PROD; with the block lifted it drained the broad GEP backlog (Windsor/Sellercloud/etc.). Recovered immediately (block restored, agent stopped); **zero data harm** (no foreign data; GEP's own data pulled early; all live PBI views healthy; prod model unaffected). Corrected: backfill via the **main non-scoped agent on its normal evening schedule** (set up for overnight).
> - **Next:** verify overnight backfill (tomorrow); Justin sign-off; **promote** (swap prod fact → v3 + re-grant + restore CORE_SVC ownership; repoint TEST back; drop `_V2`); Jira note. Consider deploying the GP-277 pick fix to PROD core_api. **Unified marketing fact** (Amazon v3 ∪ Windsor Google/Meta) is the convergence target — boot prompt `aldc-launchpad/boot-prompts/unified-marketing-fct-view.md`.

> **2026-06-04 — Client-review package sent; promotion still gated on sign-off.** Regenerated the TEST validation report fresh against the v3 chain (`report_export.py` forced local-dev, no portal upload — output moved to repo `reports/`, now gitignored; service local-output path repointed there). Re-verified at the consumer layer: `WAREHOUSE.MARKETING_FCT_ACTIVITY` → `WAREHOUSE_SOURCE` → PROD `MARKETING_FCT_ACTIVITY_V2` (v3); 829 halo rows present; **TRAVALO campaign `296392704346091` 5/28 reconciles exactly $7.57 / $114.99 / 5 orders**. Email to Justin finalized + **sent** (`aldc-launchpad/dm-justin-gp199-phase2-real-attribution.md`) — explicit sign-off ask, "how to read the report" (matured dates + campaign-level pivot), Brand-Halo definition, and "what you can monitor once in Prod".
> - **$0-attributed-sales investigation (Paul's callout on the report) — confirmed EXPECTED, not a bug.** Two stacked effects on recent dates: (1) **by design** — on pp-covered days the real attributed sales sit on separate `$0-spend` halo rows (Branch S1), and the advertised-ASIN spend rows are zeroed so totals don't double; (2) **14-day attribution lag** — recent campaign-days are still maturing, and Amazon's own campaign report shows $0 for campaigns it hasn't attributed yet (6/03: only 11 of 34 spending campaigns had source sales). **Discriminating test (read-only, PROD V2):** all **1,278 SB campaign-days in the last 21d match Amazon's campaign-report total to the penny ($49,073.71 = $49,073.71); 0 campaign-days where source>0 but fact=0** → no drops, no over-count. 918/1,278 (72%) campaign-days are $0 *at Amazon's source*. **Spend-weighted fallback would NOT change the $0s** — both real and spend-weighted distribute the *same* campaign-report total across ASINs; when that total is $0 (lag), both yield $0. Only a forecast could populate them, which would break console reconciliation — rejected. Self-heals as the 14-day windows close.
> - **UX note for cutover/report:** on matured covered days, attributed sales live on the $0-spend halo rows, so a spend-row scan looks empty — a campaign-level pivot is the correct view. Flagged to Justin in the email.

> **2026-05-29 ([[GP-225]] schema review):** GP-199 is **not impacted** by GP-225's open questions and is **safe to promote independently**. It is a cost-allocation feature only (`CONVERSIONS=0, SALES_AMOUNT=0`), so the attribution-window question (Q3) doesn't touch it, and it already conforms to the unified `PRODUCT_ID` grain (it *implements* the Amazon-SB answer to Q8). The only thing that would force rework — switching to purchased-product attribution (Approach B) — was rejected by Justin on 2026-05-01.

## Problem

Sponsored Brands campaign spend is attributed with `PRODUCT_ID = '-1'` in `marketing_fct_activity.sql`. Downstream, spend is distributed evenly across ALL ASINs in a brand via portfolio lookup. A campaign targeting 3 ASINs has its cost diluted across 50+ products, making per-product profitability inaccurate.

Sponsored Products already has accurate per-ASIN attribution via `ADVERTISEDSKU` in the Advertised Product Report.

## Key Research Findings

1. **Amazon does not provide per-ASIN spend reporting for Sponsored Brands** — confirmed API limitation because a single SB ad can feature multiple ASINs. No `sbAdvertisedProduct` report type exists (unlike `spAdvertisedProduct` for SP).

2. **Two data sources enable attribution:**
   - **Ad Metadata API** (`/sb/v4/ads/list`) — returns `creative.asins` per ad (targeting data). Connector does NOT support this yet.
   - **Purchased Product Report** (`sbPurchasedProduct`) — returns `purchasedAsin` per campaign (conversion data). Connector already supports this.

3. **SB Ads Report** (`sbAds`, category `ad_report`) — provides daily metrics per `adId` (impressions, clicks, cost). Connector already supports this but no Eclipse template exists for GEP.

4. The Eclipse `amazon_ads_v1` connector already supports SB categories: `campaign`, `campaign_report`, `ad_report`, `purchased_product_report`. Only `campaign` and `campaign_report` have GEP templates today.

## Approved Design (Justin Shuster, 2026-05-01)

**Approach A (Ad Creative Attribution)** — confirmed via email 2026-05-01.

### Allocation Formula (Confirmed)

Three-step model:

**Step 1 — Ad to ASINs (equal split):**
Split the ad's daily spend equally across all ASINs in that ad's creative.
> Example: Ad has 3 ASINs, $90 spend that day → $30 per ASIN

**Step 2 — ASIN to Orders (unit-ratio weighted):**
Distribute each ASIN's allocated spend across its individual orders that day, weighted by units.
> Example: ASIN-A allocated $30, had two orders (8 units and 2 units) → $24 and $6

**Step 3 — No sales (UNK bucket):**
If an ASIN in the ad had zero orders that day, it keeps its equal share as unattributed spend.
> Example: ASIN-C allocated $30, zero orders → $30 recorded as UNK spend

Justin explicitly confirmed: "do not weigh the initial split... evenly distribute across ASINs first."

### Historical Data (Confirmed)

**Option A — no backfill.** Existing SB data stays as-is (brand-level split). New ASIN-level logic applies from the go-live date — the date we start capturing daily ad-ASIN snapshots. Justin: "good with a backfill from 'go live' or whenever you start capturing the relationship between date => Ad => ASINs."

### Non-ASIN SB Ads Fallback

SB ads with no ASIN attribution at all (e.g., Store Spotlight, Video) continue current behavior: split evenly across the brand. Justin questions whether this case exists — verify during implementation.

### Daily Snapshot Requirement

Each day's ad-to-ASIN composition must be captured so that future ad changes don't retroactively alter past attribution. Justin: "if an ad changes in a month, it should not change what was being advertised today."

## Current State (marketing_fct_activity.sql)

- **Sponsored Products:** Uses `CURRENT_SPONSORED_PRODUCTS_ADVERTISED_PRODUCT_REPORT`, `PRODUCT_ID = ADVERTISEDSKU`, `AD_GROUP_ID` populated
- **Sponsored Brands:** Uses `CURRENT_SPONSORED_BRANDS_CAMPAIGN_REPORT`, `PRODUCT_ID = '-1'`, `AD_GROUP_ID = 'Unknown'`

## Schema Changes

**New raw tables** (auto-created by Eclipse):
- `AMAZON_ADS.CURRENT_SPONSORED_BRANDS_AD` — ad metadata with `creative.asins`
- `AMAZON_ADS.CURRENT_SPONSORED_BRANDS_AD_REPORT` — daily metrics per adId

**New warehouse views:**
- `WAREHOUSE_SOURCE.EXTRACT_AMAZON_ADS_SB_AD_ASIN_MAP` — flattened ad-to-ASIN mapping via `LATERAL FLATTEN` on `creative.asins`. Must preserve daily snapshots (not just latest).
- `WAREHOUSE_SOURCE.EXTRACT_AMAZON_ADS_SB_AD_DAILY_SPEND` — daily ad-level spend from `AD_REPORT`, joined to ASIN map for that date

**Updated view:**
- `marketing_fct_activity.sql` SB section: replace campaign-level query with ad-level query joined to ASIN map. Implement three-step allocation (equal split → unit-ratio → UNK).

**UNK handling:** Design needed — either:
- A synthetic `PRODUCT_ID = 'UNK'` row in `marketing_fct_activity` for spend on ASINs with no matching sales
- Or a separate unattributed-spend bucket that downstream PBI reports can surface

## Connector Changes

File: `connector/connector/connectors/amazon_ads.py`
- Add `SPONSORED_BRANDS_ADS_LIST_ENDPOINT = f"{ADS_API_BASE_URL}/sb/v4/ads/list"`
- Add `"ad"` category handler for SB (mirrors existing `"campaign"` pattern)

## Implementation Plan

1. ✅ **Connector enhancement** — Added `SPONSORED_BRANDS_ADS_LIST_ENDPOINT` + `"ad"` category handler to `amazon_ads.py`. Key finding: `remove_all_lists()` strips Python lists — serialized `creative["asins"]` to JSON string before `dataframe_from_list_path` so it survives as `CREATIVE_ASINS VARCHAR` in Snowflake.
2. ✅ **Eclipse templates** — Created `sponsored_brands_ad.json` (full partition, category `ad`) and `sponsored_brands_ad_report.json` (date partition, category `ad_report`, min_date 2026-05-01).
3. ✅ **Warehouse SQL** — Created `extract_amazon_ads_sb_ad_asin_map.sql` (queries `COMBINED_SPONSORED_BRANDS_AD` using validity range: `VALID_FROM`/`VALID_UNTIL` from `HISTORY_TIMESTAMP_START/END`; COMBINED_* is a SECURE VIEW, not a table; merge_strategy=add only inserts on change not every run), `extract_amazon_ads_sb_ad_daily_spend.sql` (equal-split Step 1 via validity range join), and rewrote `marketing_fct_activity.sql` SB section as a 3-part union (ASIN-attributed + non-ASIN fallback + pre-go-live legacy).
4. ✅ **Verify non-ASIN SB ads** — Queried PROD directly (2026-05-02). 1 VCPM/BRAND_IMPRESSION_SHARE campaign confirmed (currently inactive); 394 CPC/PAGE_VISIT campaigns are standard Product Collection with ASINs. Non-ASIN fallback union is necessary and correct. Q1 closed.
5. 🔄 **Go-live cutover** — Eclipse deploy to PROD (test env unavailable). Steps completed 2026-05-05:
   - ✅ All GitHub Actions secrets set (AGENT_CONFIG_TEST, AGENT_CONFIG_PROD, GHCR_PAT)
   - ✅ CI passing — `development` merged to `master` via PR #120 (2026-05-05)
   - ✅ Image `ghcr.io/aldc-io/agent-dcgeneral-coquitlam:master` built (commit 2f81cdc)
   - ✅ Eclipse templates deployed to PROD CosmosDB (`core/work_template`)
   - ✅ Prod agent redeployed on VA1 Prod 1C03 (`aldcproddock1c01`, 192.168.22.70)
   - ✅ SQL reviewed and confirmed (all 3 files, 2026-05-05)
   - ✅ Ad Report data confirmed: `SPONSORED_BRANDS_AD_REPORT` has 5,708 rows (2026-05-01 to 2026-05-21)
   - ✅ Ad metadata confirmed: `SPONSORED_BRANDS_AD_1` has 304 rows, `CURRENT_SPONSORED_BRANDS_AD` view returns 304 rows (2026-05-21)
   - ✅ **Ad metadata table — merge working.** 304 rows in `SPONSORED_BRANDS_AD_1` (schema version 1). PR #122 (null-adId filter) deployed 2026-05-21. Container `0c2ff15f18f2` on VA1 Prod 1C03.
   - ✅ Set `changes.eclipse.completed = true` → sandbox + test + prod deployed and validated

   **Why PROD instead of TEST:** KA1 Test 01 (192.168.36.70) has been disconnected since 2026-02-09. Entire 192.168.36.x subnet unreachable. Eclipse test environment not actively used. Changes are additive only (new tables, merge_strategy=add).

**Branch (clients):** `feature/paulrussell/gp-199/asin-brand-campaign-attribution`
**Branch (connector):** `feature/paulrussell/gp-199/amazon-ads-sb-ad-endpoint` → PR #119 merged to `development` 2026-05-02 → PR #120 merged `development` to `master` 2026-05-05
**Connector fix (2026-05-20):** PR #121 — MIME type fix `sbad` → `sbadresource` + omit null `nextToken`. Merged, Coquitlam image rebuilt via workflow_dispatch.
**Connector fix (2026-05-20):** PR #122 — Filter null-adId rows from `/sb/v4/ads/list` response. Pending merge + image rebuild.
**GitHub Actions:** `.github/workflows/docker-publish.yml` — passing, all secrets set

**Estimated effort:** ~20-28 hrs (original 17-22 + UNK handling)

## Design Comment History

- Jira comment (Paul, 2026-04-30): Full design document with comparison table, recommending Approach A.
- Email (Justin Shuster, 2026-05-01): Approved Approach A. Confirmed three-step allocation, Option A (no backfill), UNK handling.
- Jira comment #27008 (Paul, 2026-05-01): Locked-in design summary posted to ticket.

## 2026-05-20 Debugging Session

### Connector MIME type fix (resolved)

`SPONSORED_BRANDS_AD` had 0 rows since table creation (2026-05-06). Root cause: the `"ad"` category handler in `amazon_ads.py` used the wrong `Accept`/`Content-Type` MIME type:
- **Was:** `application/vnd.sbad.v4+json`
- **Correct:** `application/vnd.sbadresource.v4+json` (matching the `sbcampaignresource` pattern on the campaigns endpoint)

Secondary fix: omit `nextToken` from the POST body when `None` (API spec says omit, not send null).

**PR #121** merged to `master` 2026-05-20. Coquitlam image rebuilt via `workflow_dispatch` (agent_id=dcgeneral-coquitlam, config_name=coquitlam). Prod agent redeployed on VA1 Prod 1C03 — container `6fdd26377db4`. Rollback image tagged `ghcr.io/aldc-io/agent-dcgeneral-coquitlam:rollback-20260520` (image `c4b0f44b2a39`). Container config backup at `/tmp/agent-dcgeneral-coquitlam-master_rollback_20260520.json` on the host.

Also removed a stale duplicate container `agent-dcgeneral-coquitlam-master-fixed` (was running for 12 days alongside the primary).

**Verification:** After MIME type fix, the connector successfully fetches **306 ad rows** with `CREATIVE_ASINS` data (confirmed in CosmosDB session `d4198a1aee5c4cb1b9e4ebfb16bd3099`, `rowcount: 306`, `bytes: 80784`).

### Eclipse merge failure (RESOLVED)

The connector fetches data successfully, but Eclipse's merge step fails with "encountered a merging problem" every run since the template was first deployed (208 schedule entries, all failed/zombie).

**Root cause:** The Amazon Ads `/sb/v4/ads/list` API returns **2 ad objects with `adId: null`** out of 306 total. Eclipse's `warehouse_schema_match` creates the target table with `PRIMARY KEY (ADID, PROFILE_ID)`, which Snowflake enforces as `NOT NULL`. The Core API's stored procedure INSERT fails with:
```
100072 (22000): DML operation to table PROD_DG1_GEP.AMAZON_ADS.SPONSORED_BRANDS_AD
failed on column ADID with error: NULL result in a non-nullable column
```

The 2 null-adId ads are:
1. A VIDEO ad (`CREATIVE_TYPE: VIDEO`, `CREATIVE_CREATIVESTATUS: PENDING_MODERATION_REVIEW`, profile `2874274850477920`, created 2021-11-12)
2. A PRODUCT_COLLECTION ad (`CREATIVE_CREATIVESTATUS: PUBLISHED`, brand "JR Liggett's", profile `2874274850477920`, created 2022-02-04)

Both are old, inactive ads from a secondary profile. Amazon appears to have dropped their `adId` from the API response.

**Fix:** Added null-adId filter in `connector/connector/amazon_ads.py` (the `"ad"` category handler):
```python
ads_list = [ad for ad in ads_list if ad.get("adId") is not None]
```
This reduces the dataset from 306 to 304 rows. The 2 excluded ads have no adId and therefore cannot participate in ad-level attribution — excluding them is semantically correct.

**Diagnosis method:** Reproduced the exact merge SQL locally by querying the staging table `STG_CE312EE01A144EE1992BAB82AA3059DC` (still in `PROD_DG1_STG_DA8904DB.STAGE`). Found `NULL ADID: 2` via `SELECT COUNT(*) ... WHERE ADID IS NULL`. Confirmed INSERT fails on those rows by running the reconstructed SQL directly.

**CosmosDB cleanup performed (2026-05-20):**
- Deleted 203 failed + 5 zombie schedule entries from `schedule` collection
- Deleted 203 stale entries from `work_queue` collection
- Reset `work_partition` (`3617dc27-886c-4307-a402-0d5634acbfc7`): `in_queue=false`, `retry_next=0`, `queue_messages=[]`
- Dropped and let Eclipse recreate `SPONSORED_BRANDS_AD` table

**Stale staging tables:** 20+ STG_ tables (all 306 rows each) remain in `PROD_DG1_STG_DA8904DB.STAGE` from the failed runs. The account's `temp_purge` flag does not clean staging tables on merge failure. Consider manual cleanup after the fix is verified.

**Key CosmosDB references:**
- Work template ID: `b3e72c14-9f5a-4d08-a1c3-7e8f2b0d6a49`
- Work partition ID: `3617dc27-886c-4307-a402-0d5634acbfc7`
- Last failing session: `ce312ee01a144ee1992bab82aa3059dc`
- CosmosDB: `aldcprodcsdb1c01`, database `core`

### CI workflow note

The `docker-publish.yml` workflow defaults to `agent-dcgeneral` (Kamloops) on push-to-master. To build the **Coquitlam** image, use `workflow_dispatch` with `agent_id=dcgeneral-coquitlam` and `config_name=coquitlam`.

## 2026-05-21 Validation + Deployment Session

### SQL fixes applied

1. **SPEND_MODEL column missing** — `extract_amazon_ads_sb_ad_daily_spend.sql` and `marketing_fct_activity.sql` referenced `COSTTYPE` from the SB Ad Report, which doesn't have that column. Fixed by adding `CAMPAIGN_COSTTYPE` CTE that derives SPEND_MODEL per campaign from `CURRENT_SPONSORED_BRANDS_CAMPAIGN_REPORT`. COALESCE fallback to 'UNKNOWN' for edge cases.

2. **GROUP BY ambiguity** — `PROFILE_ID` was ambiguous in two GROUP BY clauses (multiple joined tables have PROFILE_ID). Fixed with ordinal GROUP BY (`GROUP BY 1, 2, 3, 4, 5, 6, 7, 8`).

3. **UNK detection removed** — PRODUCT_ID now shows actual ASINs directly. UNK detection (Step 3) deferred because `SALES_FCT_ORDERLINE.PRODUCT_ID` uses internal SKUs (e.g., `FBM_AUTO10W`), not Amazon ASINs (e.g., `B0GT2G514J`). No product dimension (ASIN → SKU) exists. Future tickets needed.

### Validation results (2026-05-21)

- **ASIN-attributed rows:** 65 rows, 47+ ASINs, $96.87 (growing as ad report dates accumulate)
- **Equal-split verified:** All tested ads show EQUAL split (MAX per-ASIN = MIN per-ASIN)
- **Cost reconciliation:** marketing_fct_activity SB total = campaign report (pre-go-live) + ad report (post-go-live). Zero discrepancy.
- **Legacy unchanged:** 43,078 rows, $203,183.71
- **SP unchanged:** 753,865 rows, $1,952,902.91
- **SPEND_MODEL:** CPC (100% of active campaigns), VCPM (1 inactive campaign)
- **UAT report:** `clients/GEP/tickets/GP-199/uat_validation_report.md`

### Snowflake infrastructure findings

1. **49 WAREHOUSE views had SYSADMIN ownership** instead of `PROD_DG1_ROLE_CORE_SVC_DA8904DB`. Caused PBI Sandbox refresh failure. All 49 transferred to CORE_SVC with `COPY CURRENT GRANTS`.

2. **Data share grants dropped by CREATE OR REPLACE** — new Eclipse views (`COMBINED_SPONSORED_BRANDS_AD`, `CURRENT_SPONSORED_BRANDS_AD_REPORT`) and all recreated WAREHOUSE/WAREHOUSE_SOURCE views were not in the `PROD_DG1_GEP` outbound share. Fixed via `PROD_DG1_CORE_ADMIN` (ACCOUNTADMIN) on wj66376: granted 4 AMAZON_ADS views + 24 WAREHOUSE views + 31 WAREHOUSE_SOURCE views to share. FUTURE GRANTS to shares not supported by Snowflake — must re-grant after each CREATE OR REPLACE.

3. **TEST_DG1_GEP is a standalone database** (STANDARD, not IMPORTED), with stale data through 2026-03-03. PBI Test model now reads marketing data via `SELECT * FROM PROD_DG1_GEP.WAREHOUSE.MARKETING_FCT_ACTIVITY` (cross-database reference to shared PROD).

4. **PBI Sandbox blocked** by pre-existing `INVENTORY_FCT_BALANCE` issue (unrelated to GP-199). Sandbox fix needs separate investigation.

### PBI deployment status

| Environment | Snowflake | PBI Refresh | GP-199 Data |
|---|---|---|---|
| Prod (wj66376) | Views deployed directly | Auto-refreshed 17:03 UTC | 797,009 rows, ASIN rows visible |
| Test (og35375 TEST_DG1_GEP) | Views point to shared PROD | Refreshed 19:00 UTC | 797,009 rows, 65 ASIN rows |
| Sandbox (og35375) | — | Blocked (INVENTORY_FCT_BALANCE) | Stale |

### Data Model Excel validation (2026-05-27)

Added `PRODUCT_ID AS "ASIN"` column to the Marketing tab in `api/credential-exchange/services/report_export.py` (func-aldc-cred-qa). Extended window from 30 to 90 days, removed LIMIT 5000. Justin Shuster can now see pre-go-live (`-1`) vs post-go-live (real ASINs) side-by-side.

**Store Spotlight exception:** "SB - Anysharp - Store Front - KT - Branded" (campaign 321927403350748) shows `-1` post go-live. `CREATIVE_TYPE = STORE_SPOTLIGHT` — links to brand store page, no product ASINs in `CREATIVE_ASINS`. $18.59 total, campaign paused. Expected and explained to Justin.

**Sent to Justin:** 2026-05-27 — email with validation steps, ASIN column walkthrough, Store Spotlight explanation.

### Open items

- [ ] Product dimension (ASIN → SKU mapping) — new ticket needed for UNK detection / Step 2. **This is exactly Phase 1A Q8** — see [[GP-225]]; ad data uses ASINs while `SALES_FCT_ORDERLINE` uses internal SKUs, no mapping dimension exists.
- [ ] Marketplace ID standardization — `NULL_NULL_-1_163` vs `NULL_SushiQuik_-1_163` format mismatch
- [ ] PBI Sandbox fix — `INVENTORY_FCT_BALANCE` "no visible columns" error
- [ ] Stale staging tables — 20+ STG_ tables in PROD_DG1_STG_DA8904DB.STAGE
- [ ] PAULRUSSELLADMIN password on wj66376 — not working, Snowflake Support case submitted
- [ ] Justin UAT sign-off pending

## Boot Prompt (Sonnet Execution Session)

```
You are implementing **GP-199 — ASIN Brand Campaign Attribution** for GEP. Implementation steps 1–4 are complete. Use `/gep-feature` skill.

Boot procedure:
1. Read `C:\Users\PaulRussell\repos\wiki\CLAUDE.md`
2. Read this ticket page: `C:\Users\PaulRussell\repos\wiki\tickets\gep\GP-199.md`
3. Read artifact: `C:\Users\PaulRussell\repos\clients\GEP\tickets\GP-199\artifact.yaml`
4. Read deploy manifest: `C:\Users\PaulRussell\repos\aldc-shipyard\clients\GEP\deploy_manifest\GP-199.yaml`

**Current state (as of 2026-05-21):** Stage = implementing. Eclipse merge working. Sandbox views deployed. Waiting for tomorrow's ad report data to validate allocation.
- Merge fix deployed: PR #122 (null-adId filter) merged, image rebuilt, container `0c2ff15f18f2` on VA1 Prod 1C03.
- `SPONSORED_BRANDS_AD_1`: 304 rows (schema version 1). `CURRENT_SPONSORED_BRANDS_AD` view: 304 rows.
- `EXTRACT_AMAZON_ADS_SB_AD_ASIN_MAP`: 584 rows (293 ads, 216 ASINs), VALID_FROM = 2026-05-21.
- `EXTRACT_AMAZON_ADS_SB_AD_DAILY_SPEND`: 0 rows (expected — ad report max date is 2026-05-20, ASIN map starts 2026-05-21).
- SQL fix during sandbox: ASIN map view uses LEAD() for VALID_UNTIL (COMBINED view doesn't expose HISTORY_TIMESTAMP_END). Removed non-existent COSTTYPE column from daily spend view.
- 20+ stale staging tables in PROD_DG1_STG_DA8904DB.STAGE from failed runs — cleanup pending.

**Next action (2026-05-22):**
1. Verify daily spend allocation:
   - Confirm `EXTRACT_AMAZON_ADS_SB_AD_DAILY_SPEND` has rows for 2026-05-21
   - Reconcile: SUM(ASIN_ALLOCATED_COST) should equal SUM(COST) from ad report for overlapping dates
2. Deploy `marketing_fct_activity.sql` SB section rewrite
3. Validate marketing_fct_activity output
4. Clean up stale staging tables

**Key constraints:**
- Eclipse deployed to PROD (not test) — KA1 test env down since 2026-02-09.
- All development continues on ticket GP-199 (no sub-tickets).
- Rollback image available: ghcr.io/aldc-io/agent-dcgeneral-coquitlam:rollback-20260520
```

## See Also

- [[GP-169]] — Marketing activity marketplace attribution (related pattern)
- [[amazon-ads]] — Amazon Ads connector docs
- [[phase-1a-marketing-ad-platforms]] — Phase 1A context
- [[gep-inventory-data-dictionary]] — Reference for existing schema patterns
- [[star-schema-convention]] — ALDC naming conventions
