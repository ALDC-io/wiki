---
tags: [ticket, gep, navira, power-bi, marketing-model, enablement, data-model-design, advertising]
aliases: [GP-319]
sources: [conversation 2026-08-25, conversation 2026-09-01, agent-factory branch mission/marketing-model-finalization 1ed4277..e7f92f3, aldc-launchpad commits 7538fc4 065ba60 4545fad, Jira GP-319 comments 36055 36056 36073 36082, aldc-launchpad boot-prompts/gp319-marketing-model-design-2026-08-25.md]
created: 2026-08-25
updated: 2026-09-08
---

# GP-319 — designing the Navira Marketing Model, and making it legible

Marketing Model `2d8587b5` in `GEP Test Models` — **the client's live surface**. GP-319 owns three
deliverables: a **design decision** with candidates scored, **client sign-off on correctness** at the
grain their team uses, and an **enablement pack**. It also owns a **gate**: no further
`navira-marketing-dashboard` work (≈80–85% complete) proceeds until the design settles and the
numbers are signed off, and [[GP-318]]'s successor GP-320 does not start until then.

Blocked by [[GP-296]] (the $2.72M divergence, **unassigned** — the last blocker on correctness
sign-off) and GP-321 (staleness, **resolved**).

## The field list IS the user interface

Nicholas consumes this model in Excel PivotTables, so what an analyst can drag is the whole product.
Measured 2026-08-25 against the live model:

```
Marketing 2d8587b5   400 field-list items · 198 visible measures of 383 · 29 visible tables of 42
Daily     66151728   235 field-list items · 111 visible measures of 357 · 20 visible tables of 39
```

⚠ **400, not the 410 that circulated in five boot prompts.** Also 128 of 198 visible measures carry
**no description**, and 30 sit in **no display folder**. That ratio is the problem the enablement
pack has to solve, not decorate.

## ⭐ The finding that explains "they don't like the design"

The model offers **two measures per concept and signals nothing** about which is safe:

| concept | safe at SKU | not safe at SKU |
|---|---|---|
| spend | `Actual - Marketing - Cost` | `Spend (USD)` |
| clicks / impressions / CTR / CPC | `Actual - Marketing - *` | `Ad Clicks`, `Ad Impressions`, `CTR %`, `CPC (local)` |

⚠ **Corrected 2026-08-25, and every prior handoff had it wrong:** this is *not* a cross-table trap.
Four of the five pairs live in the **same table** — `Marketing Measures` — and the unsafe half is
**unguarded**. Only `Spend (USD)` sits on `Marketing Efficiency`. So the trap is inside one folder.

⛔ **And the sharpest fact about the model, which nothing had stated:**
`Actual - Marketing - Cost` — the measure that splits correctly by product and campaign — covers
**Amazon Sponsored Products and Sponsored Brands only**. It is blank for Google, Meta and Sponsored
Display. `Marketing Efficiency[Spend (USD)]` is complete across all five platforms but refuses a
product axis. **So there is no single measure that is both complete and per-product.** That is the
strongest argument for the conformed-core design and it was never put that way.

## Five INERT axes are live here — the crux defect, still open

An **inert** axis neither errors nor blanks: it returns the portfolio grand total on every member, so
it looks perfectly healthy. Measured behaviourally, one DAX query per cell, both controls passing:

| fact | sliced by | members | every row returns |
|---|---|---|---|
| `Sales Measures` | `Campaign` | 400 | 129,902,886.59 |
| `Sales Measures` | `Platform` | 5 | 129,902,886.59 |
| `Sales Measures` | `Marketing Efficiency Product[ASIN]` | 400 | 129,902,886.59 |
| `Marketing Measures` | `Vendor` | 154 | 2,668,277.36 |
| `Marketing Measures` | `Marketing Efficiency Product[ASIN]` | 400 | 2,668,277.36 |

The Daily model **blanks** the same three `Sales Measures` axes. Its guards work; this model's do not.
See [[answerability-guard]].

⭐ **A single-member axis correctly equals the grand total.** The first probe run reported **nine**
inert combinations; four were single-member axes (`Marketplace[Channel Name]`, and
`Agency[Entity Code]` on three facts). Inertness requires a *real split* to have been ignored.
**Without that rule this would have published nine defects where five exist** — the same
false-positive class as calling a blank a zero, in the opposite direction.

## Agency / Lectric — the removal that must happen in one specific order

Paul, 2026-08-25: *no agency data in either model*, and Lectric is being removed. Enumerated:

| | Daily `66151728` | Marketing `2d8587b5` |
|---|---|---|
| `Agency` table | **absent** | present, visible |
| relationships from Agency | 0 | **9** |
| visible entity columns | 0 | **9** |
| measures referencing agency | 0 | **13** (`(All Agencies)`, `(Company Total)`) |

⛔ **`Agency[Entity Code]` is currently the ONLY thing scoping this model to Navira:**

```
Actual - Sales - Gross    all 129,902,886.59   navira 127,139,621.59   LECTRIC 2,763,265.00
Actual - Sales - Net      same 2,763,265.00 remainder
Actual - Cost - COGS      remainder 0.00
Spend (USD)               remainder 0.00
```

⭐ **Lectric carries $2.76M of sales but zero cost and zero ad spend.** So the strip lowers sales,
leaves cost alone, and therefore **raises margin % and raises TACoS/MER** — correct, but it moves
client-visible ratios and must be announced, not discovered. Gross −2.13%, net −2.27%.

**The order is the entire safety argument.** Removing the agency layer *before* a Power Query entity
filter exists publishes another client's revenue inside Navira's totals — strictly worse than the
residue being cleaned. Scope is **2 tables of the 8** carrying `ENTITY_CODE`: `Order Line`
(NAVIRA 3,425,206 / LECTRIC 5,032) and `Marketing Efficiency` (9,548 / 747); the other six are
already 100% NAVIRA, and editing them would be change without effect.

The fix **copies the Daily model's proven idiom verbatim** rather than inventing one:

```
Filtered = Table.SelectRows(V, each [ENTITY_CODE] = "NAVIRA")
```

Rehearsed in `pbi_ops/_gp319_lectric_strip_and_agency_removal.py` — dry-run default, verbatim M
captured before any write, **the apply asserts live M is byte-identical to the capture** so a
concurrent session cannot be silently overwritten, and **phase 2 refuses to run until a gate proves
the remainder is 0.00 AND every Navira anchor is unmoved**. Two checks, because a filter that removed
*too much* would also show a zero remainder. The gate was tested and does refuse. **Not applied.**

Removal order, mirroring the Daily model's 2026-08-13 pass: **13 measures → 9 relationships → 9
entity columns → the `Agency` table.** Tabular refuses any other order. Phase 2 defaults to **hide**
rather than delete: hiding removes the layer from every field list while leaving all DAX resolvable,
and reverts in one edit.

## The three designs, and what the artifacts were missing

Recommendation on file: **C now, A next, B on the shelf as a bridge.** Only C exists today; A and B
are NOT-BUILT and carry no figures.

⚠ **The workbook gave drag-lists, not schemas** — a pivot recipe rather than a data model. Design A
named `MARKETING_FCT_CORE` only in passing; Design B said "the four fact grains" without naming them.
Fixed 2026-08-25 by adding a **fact inventory per design** and a **measured dimension×fact
conformance matrix** (`GP319_Three_Designs_Workbook_v2.xlsx`, sheets 7 and 8).

⭐ **And the readout's designs figure flattens the one thing being decided.** It draws a single
dimension row captioned *"identical in all three: Date · Product · Platform · Campaign"*. That is
incomplete — `Marketplace`, `Agency`, `Brand`, `Vendor` are live dimensions too — and, more
importantly, **the dimension *list* is not what differs between designs; the dim→fact *edges* are.**
Today `Product` does not reach `Marketing Efficiency` at all, and that absence *is* the argument for
A. Still unfixed.

## ⭐ The design decision (2026-08-25) — fix the measure layer, do NOT build a conformed fact

Supersedes *"C now, A next"* above. **Declare `MARKETING_FCT_ACTIVITY_UNIFIED` canonical — it already
is — and rebuild the measure layer on it.**

- **Conformed core fact in the warehouse — rejected.** It would create a new table for figures that
  already live in one object and already reconcile to the cent through a proven lineage. This client
  already carries ~14 copies of the marketing family across 6 schemas; a 15th worsens the
  canonical-object problem. Nothing is left to conform.
- **Long/narrow `(grain, METRIC_KEY, METRIC_VALUE)` — rejected**, and this is the re-test the AC owed.
  The core is 10 metrics wide with a per-source tail; a key-value fact makes every query a pivot and
  destroys the plain star with a real product edge, while fixing nothing — the defect is naming.
- **Curate the surface only — insufficient alone.** Ships *on top*, but alone it leaves the
  two-platform hard-code live, purchases missing, and 16 ROAS measures with no default.

**Tier 0 (hours, additive):** platform-explicit measures (`Ad Spend — Amazon SP+SB` /
`— All Platforms`, same for clicks, impressions, CTR, CPC), deprecating the ambiguous originals by
description + display folder, **never by rename**. Add `Purchases` from the existing `CONVERSIONS`
column, plus `Cost per Purchase` (DERIVED), `Units Sold`, `Ad Sales` — **two of the ten "absent" core
metrics need no pipeline work at all.** Pick ONE canonical ROAS; folder the other fifteen as
diagnostics. One `Core 10` display folder (folders are proven to render in Excel for the web).

**Tier 0 needs two human decisions first:** which ROAS is canonical, and sign-off on the new names,
because they land on a client-visible model and additive means we live with them.

**Tier 1:** record the lineage as canon; promote the $23,689.66 boundary into the model's own
documentation; bind the dashboard's SQL to the same definitions one-for-one — the dashboard reads
Snowflake directly and bypasses every DAX guard, so definition parity is the only thing that keeps
the two consumers from disagreeing.

## The §1 boundary, finally recorded

The Daily-vs-Marketing split lived only in a 2026-08-10 comment that said it *"is currently written
down nowhere in Jira"* — and still did not, five sessions later, while four additive changes went
into both models. Recorded 2026-08-25 as Jira comment 36056. Engagement metrics (Impressions,
Clicks, CTR, CPC, CPM + four quantity measures + the `Campaign` table) are **hidden on Daily, visible
on Marketing**; ad spend is visible on both. Verified: zero visible Daily measures depend on a hidden
one, so nothing is orphaned.

⚠ **The apparent conflict with GP-320's "universal core" is not one.** The core-10 is what the
*Marketing* model must report, not what *Daily* must expose. Same metric, core to one model, out of
scope for the other.

⛔ **Still unconfirmed:** the hide rests on a recollection of Heather's ask. The implementing script's
own docstring says *"confirm against the Avoma transcript before this reaches Heather."* Not done.

## Coverage ceilings worth not re-deriving

- Product-keyable ad spend tops out at **84.35%**. `PRODUCT_ID = '-1'` holds **$457,425 (15.6%)** —
  the platform reported a campaign-level aggregate *instead of* a product, so **no join or connector
  fix recovers it.** Render an explicit `(no product key)` row so totals reconcile; **do not
  allocate it** — that is invention.
- **Sponsored Brands resolves at 0%** at product grain: its `PRODUCT_ID` is an ASIN joined as a SKU —
  384/384 ASIN-shaped, 352 match `.ASIN`, **0** match `.PRODUCT_ID`. Worth ~1.05pp.
- `Product[ASIN]` is **not unique** — 9,754 ASINs on >1 row, 89.7% of the table — so a product
  relationship on the product-grain marketing table is **illegal**, not merely undone.
- Amazon Attribution is **live**, $106,513.30 Google→Amazon attributed sales, but **0 of 22**
  campaign ids match a Google campaign name, so per-campaign ROAS is not defensible. Publisher is.

## ⭐ Cross-model divergence — RESOLVED 2026-08-25, and it was never a data problem

`Actual - Marketing - Cost` differs by **$281,637.02** between the two client-visible models
(Daily 2,949,914.38 vs Marketing 2,668,277.36). **Cause: the measure is hard-coded to two platforms
by a DAX literal.**

```
Actual - Marketing - Cost = CALCULATE(SUM('Marketing Activity'[COST_USD]),
    'Marketing Activity'[PLATFORM_ID] IN {"Amazon Ads Sponsored Products","Amazon Ads Sponsored Brands"})
```

`Actual - Marketing - Clicks` carries the same literal. `Ad Clicks` / `Ad Impressions` are plain
unfiltered `SUM(...)` — genuinely all-platform. **So the celebrated "two measures per concept, 4–11%
apart" is not a data-quality problem: it is one family scoped to two platforms and the other to five,
with nothing in either name saying which.** The 11.3% clicks gap is exactly Google + Meta + SD
clicks (332,408).

⛔ **The consequence to state plainly:** the measure our own guide teaches as *the one that works at
product grain* silently excludes **9.5% of ad spend, including 100% of Google and Meta**, and the
model gives the user no way to discover it.

**The warehouse is complete and the chain reconciles — measured, with `GET_DDL` lineage:**

| | |
|---|---:|
| `WAREHOUSE.MARKETING_FCT_ACTIVITY_UNIFIED`, NAVIRA, all 5 platforms | 2,952,898.82 |
| − Google 2024-01…2024-05 (efficiency view starts 2024-06) | 23,689.66 |
| = `REPORT_COMMON.MARKETING_EFFICIENCY` (day × marketplace) | 2,929,209.16 |
| − refresh lag (model refreshed 08-24, warehouse holds 08-25) | 2,977.83 |
| = PBI `Spend (USD)` | 2,926,231.33 |

SP, SB, SD and Meta agree between the two objects **to the cent**, and the efficiency view is built
**from** the activity fact. The activity fact carries cost, clicks, impressions **and conversions**
for all five platforms at campaign × ad-group × product × date × marketplace grain.

⭐ **The durable lesson: put the scope in the measure name.** A platform allow-list living inside a
DAX literal is invisible at the field list, invisible in the relationship graph, and reads to everyone
downstream as a data defect. Three sessions on this ticket chased it as one.

## Deliverables produced

- `docs/readouts/NAVIRA-MARKETING-MODEL-GUIDE.html` — the [[model-enablement-guide]] for this model,
  **generated from the live model**, matching the Daily guide's section spine.
- `docs/evidence/gp319/GP319_Three_Designs_Workbook_v2.xlsx` — 7 original sheets + the two schema
  sheets.
- `docs/readouts/NAVIRA-MARKETING-MODEL-GUIDE.pdf` — 14 pages, rendered **and machine-checked**
  (page count, headline figures present in the text layer, nav strip suppressed, every `h2` survived,
  no page majority-dark).
- `docs/evidence/gp319/NAVIRA-MARKETING-MODEL-COOKBOOK-PIVOTS.xlsx` — 7 tabs of live PivotTables;
  tab 1 is Heather's Mastersku pivot on the measure that works, with the refusing one beside it.
  Built by `pbi_ops/_gp319_cookbook_workbook.py`, verified 29/29 by the **separate**
  `_gp319_cookbook_workbook_check.py` — which caught a real finding the build log could not.
- Re-runnable read-only instruments, all taking `--daily` to retarget the sibling model:
  `pbi_ops/_gp319_guide_extract.py` · `_gp319_guide_reach_matrix.py` · `_gp319_build_guide.py` ·
  `_gp319_design_schema_sheets.py` · `_agency_residue_census.py`.

## Open

1. The **five inert axes** — highest-value correctness fix here.
2. The **Lectric strip + agency removal** — rehearsed, unapplied.
3. The **`Ad Spend (Starter)` perspective is unproven in Excel**; the only test used a workbook whose
   connection predates it. Five minutes, and it **gates Design C**.
4. **DONE 2026-08-25 — the three description corrections.** The false *"the source does not report
   it"* claim, the stale probe status and the wrong Sponsored Display owner (GP-325, not "GP-320
   phase 7") are all fixed in the Jira description, under a dated correction banner.
5. The readout's **designs figure** — and it now also needs the design decision above.
6. **[[GP-296]] is REFUTED, not open** — measured 2026-08-12: delta **$0.00** on NAVIRA's sales
   anchor across all three copies, and the whole difference is $264,836 sitting 100% inside LECTRIC.
   It is **not** a blocker and never was. The ticket is still `Consulting/Design` and unassigned, so
   the record and the reality disagree — closing it is the action.
8. **Sponsored Display reads exactly 0.00 in 9 of 27 months** on the efficiency fact, while the
   activity fact holds SD rows through 2026-08-25. Verdict recorded as **UNVERIFIED, not ZERO**
   (`gp319_sd_zero_verdict.json`); diagnosis is **GP-325**. That activity-vs-efficiency asymmetry is
   a cheap lead nobody has pulled.
9. **Tier 0 is specced, not built** — no rehearsal script, no rollback captured, nothing applied.
7. **No rendered-layer check** was performed and no screenshots taken, so nothing is signed off by
   our own [[consumer-layer-validation]] rule.

## Related

[[GP-318]] · [[GP-296]] · [[GP-317]] · [[answerability-guard]] · [[model-enablement-guide]] ·
[[consumer-layer-validation]] · [[power-bi]] · [[pbi-xmla-automation]] · [[GEP]]

## 2026-09-01 — read-only design pass (agent-factory mission `marketing-model-reconstruction-v1`)

Branch `mission/marketing-model-v1`, commits `4e076d8`..`6edb791`. Evidence in
`agent-factory/docs/evidence/marketing-model-v1/`. **Nothing deployed. Read-only in TEST.**

**A read-only identity now exists in non-prod.** `R3_CARTOGRAPHY_RO` / `R3_CARTOGRAPHY`,
`USAGE`+`SELECT` on `TEST_DG1_GEP` only, key-pair auth, password unset. It was **watched refusing a
write** (`003001 (42501)`), so read-only is measured rather than assumed. Secret
`snowflake-r3-cartography-nonprod` in `aldc-vault-test`.
⚠ **The checkpoint's credential pairing was wrong on both counts** — `TEST_DG1_CORE_ADMIN` +
`snowflake-admin-nonprod` does not authenticate; that secret is `PAULRUSSELLADMIN`'s (this wiki says
so at GP-281:44), and `TEST_DG1_CORE_ADMIN` holds `USAGE` only. It had been "confirmed" by the
secret's *shape*, never by a login.

**Measured estate.** 84 `MARKETING_*` objects / 34 distinct names / 7 schemas;
`MARKETING_FCT_ACTIVITY_UNIFIED` exists in 5 copies. ⭐ **This ticket's 2026-08-25 rejection of a
conformed core fact cited "~14 copies across 6 schemas" — the measured estate is larger, so the
rejection is understated, not stale.** The design pass therefore recommends **no new fact**.

**The 8-part grain holds and is minimal.** 0 excess rows / 0 colliding keys; dropping
`AD_GROUP_ID`+`PROFILE_ID` yields 10,558 excess. `ENTITY_CODE` has zero NULLs.
Channel mix: Amazon 901,086 · Google 12,315 · Meta 550 — **98.6% Amazon by row count**.
Measured data floors: Meta **2026-04-20**, Google 2024-01-01, Amazon 2024-06-01. Sponsored Display
absent from the fact entirely.

⛔ **The consumer's Snowflake connection defaults to `WAREHOUSE_TEST_GP226`** (`warehouse.ts:2029`).
All six references to the activity fact are **unqualified** and resolve in the clone, which has 22
columns to `WAREHOUSE`'s 24 — it lacks `COST_USD`/`SALES_AMOUNT_USD`, which is why the dashboard
does its own FX. GP-318 repointed four `MARKETING_EFFICIENCY*` objects for exactly this reason and
**left the fact behind**. Delivery of any warehouse change is therefore a **repoint**, not a build.

**LECTRIC — de-scoped, defect deliberately NOT fixed.** `MARKETING_EFFICIENCY.sql:144` selects
`ACTUAL_SALES_GROSS_USD` into both the gross and net slots on the LECTRIC branch, so
`Contribution Margin %` reads too low for that tenant. Measured: NAVIRA `gross−net = 5,015,478.00`,
**LECTRIC = 0.00**. Per the 2026-08-25 decision (*"no agency data in either model, and Lectric is
being removed"*) this is classified legacy; removing the branch eliminates it. ⚠ The strip is
**rehearsed and unapplied**, has a stated ordering constraint, and 111 consumer references plus the
GP-254 agency-aware measures would be left dangling.

**Semantic collisions found (all ours to resolve, not the client's):**
- **MER stays CONTRADICTORY.** The doc's `SPEND/SALES` already ships twice — as PBI
  `Marketing Cost %` (an exact reciprocal of `BLENDED_MER` over the same two columns of the same
  object) and as dashboard `tacos`. `ATTR:37` contradicts its own formula lines, so it cannot
  adjudicate them.
- **TACoS has two live definitions**, from two different facts: PBI uses Amazon settlement
  advertising *fees* off `Order Line`; the dashboard uses Windsor ad-platform *spend* off the
  marketing fact. `ACOS` is worse — numerator and denominator come from different facts.
- ⚠ **"16 ROAS measures" is not reproducible** — enumerations give 12 (Aug probe) / 14 (Jun
  capture). The set collapses to **4 concepts**; 7 are removable by arithmetic (one reciprocal,
  three scalar multiples, three numerically identical to Platform ROAS because the cap is inert —
  verified 0 of 5 months bite). **Enumerate before putting this to the client.**
- `CALIBRATED_ROAS_AMAZON` (warehouse, capped, 5.05x) and PBI `Calibrated ROAS - Amazon` (uncapped,
  32.5x) share a name and differ ~6.4×, and **the marking convention is inverted between layers**.

**Gate that cannot fail:** `Ad Spend Integrity Check` compares a total to the sum of its parts, so a
leak *between* parts keeps it green.

**Validated at the query layer only.** No rendered/visual validation; no PBI model opened. Claims
about what a user sees are from reading dashboard source.

### 2026-09-01 (later) — D3 closed, D4 falsified, D5 delivered, and two claims above are now WRONG

Branch `mission/marketing-model-v1` @ `0d62b19`, **unmerged and unpushed by decision**.

⛔ **Corrections to the section above, both measured:**

1. *"Sponsored Display absent from the fact entirely"* — **FALSE.** 9,566 rows / $6,485.38 /
   2024-06-27..2026-08-31 are present in `WAREHOUSE.MARKETING_FCT_ACTIVITY_UNIFIED`, in
   `WAREHOUSE_SOURCE.MARKETING_FCT_ACTIVITY_PREPROD`, and flow into `REPORT_COMMON.MARKETING_EFFICIENCY`
   through the `AMZ_DAY` union. They are absent only from the clone the dashboard reads. Proved by a
   per-platform census of **both** copies — deliberately not by `923,517 - 913,951 = 9,566` matching
   the SD row count, which is a coincidence, not an identity. ⇒ **The dashboard and the efficiency
   family cannot agree on Amazon ad spend.**
2. *"Validated at the query layer only. No rendered/visual validation"* — **no longer true.** The
   dashboard was executed in a real browser (six tabs, HTTP 200, zero console errors, zero page
   errors, zero failed requests, zero empty cards; screenshots committed). ⚠ In demo-data mode:
   behaviour is real, **no number on those screens is a client figure**. Warehouse-mode render
   remains `NOT-EXERCISED` because the only credential for it is `ACCOUNTADMIN`, which cannot pass
   this engagement's read-only proof.

**D5 recommendation — and it AGREES with this ticket's 2026-08-25 decision.** Keep the fact, add the
semantic contract, repoint the consumer. The only new structure justified is **one conformed
marketplace dimension**: `SHARED_DIM_MARKETPLACE` is 21,867 rows over 29 members — a
product x marketplace bridge doubling as marketplace decoder, vendor decoder **and** the
multi-tenant boundary via `COMPANY_NAME`. A naive name join fans 913,951 rows to 10,410,592,213
(11,390.8x). ⚠ Hazard only: `MARKETPLACE_KEY` is unique per row and every live consumer deduplicates
first, so `NOT-REPRODUCED` as a live defect.

**New defects worth keeping:**

- **The calibration chain is NULL for 22 of 27 NAVIRA months (81% of history).** GP-318 item 2's
  honest NULL for pre-floor Meta propagates through `ATTR_USD_AMAZON + ATTR_USD_GOOGLE + ATTR_USD_META`
  in `MARKETING_EFFICIENCY_MONTHLY:52-53`, voiding `ATTR_CALIBRATION_FACTOR` and all six
  `CALIBRATED_*` measures, which the consumer renders as `0.00`. ⭐ **DAX absorbs BLANK in `+`; SQL
  propagates NULL** — the same intent, two languages, one layer dark and the other not, shipping
  under the same names.
- **The brand filter is inert on 15 measures** across Overview and Ad Platform, on a control measured
  `disabled=false, opacity=1, pointer-events=auto, no title` — in a filter bar where five other
  controls carry a visible `Coming` badge. `filters.ts:296` documents why in code; the page does not.
- **`Calibrated ROAS` is capped in the warehouse and uncapped in PBI**, and only PBI marks the capped
  variant — the unmarked name means opposite things. The warehouse cap never binds (factor 1.00 on
  all five computable months), so its calibrated figure **equals Platform ROAS exactly** (7.12).
- **`Organic Sales Gross`** — `Ad Sales` is scoped to Amazon SP+SB across **six** measures (not nine),
  so $804,227.15 of Google/Meta attributed sales is published as organic. `PARTIALLY_CONFIRMED`:
  definitions from two captures seven weeks apart that agree on the scope and diverge on the
  numerator column (`SALES_AMOUNT` -> `SALES_AMOUNT_USD`, i.e. **PBI already fixed its currency
  exposure and the dashboard cannot follow**). ⚠ Re-read from the live model before raising it.
- **TACoS / ACoS are our naming collisions, not client questions.** CD-9 removed from the client list.

**Still open, and not to be inherited as closed:** human sign-off; deployed `SNOWFLAKE_SCHEMA`
`NOT-VISIBLE`; warehouse-mode render and every PBI rendered figure `NOT-EXERCISED`; the dashboard
suite **not run** (another session holds 31 uncommitted lines in `warehouse.test.ts`);
`MARKETING_FCT_PLATFORM_DETAIL` held open, not rejected.

⭐ **Reusable:** every published figure is re-derivable by a committed check that was **proved able
to fail** — `python -m pytest tests/test_marketing_model_evidence.py -q`. It caught two defects in
itself first: it read a census by a header four tables share, and its figure assertion could not
fail because a document quoted the figure twice.

---

## 2026-09-01 — the finalization pass, and the runbook command that understated the client

Branch `mission/marketing-model-finalization` in `agent-factory`, forked from
`mission/marketing-model-v1` at `efb05cf`, commits `1ed4277` → `e7f92f3`. **Unpushed.**
Nothing deployed, no Snowflake write, no PBI edit, **no credential retrieved**.

⛔ **The documented regeneration command produced a client document that understated its own
evidence.** `05-CLIENT-REVIEW-DEMO-RUNBOOK.md` passes `--tasks .data/tasks.jsonl` — relative to the
working directory — and instructs the operator to regenerate *"shortly before the meeting"*. From
the primary checkout that is correct; from any git worktree it resolves to a `.data/` with no task
store, and `client_review.assemble()` degrades:

```
from a worktree     grounding 4x ASSERTED    status 4x UNSUBSTANTIATED
                    freshness UNAVAILABLE    completion_basis UNAVAILABLE
resolved correctly  grounding 4x SATISFIED   status 4x Complete
                    freshness LAST_VERIFIED  completion_basis DERIVED
```

Same narrative, same code, **different directory**.

⭐ **The interesting half is which way it fails.** The degradation is *visible* — the rendered page
prints UNSUBSTANTIATED four times — so this was never a hidden overclaim, and the fail-closed
design is working exactly as intended. It is the **opposite** failure and still delivery-critical:
the client receives a document reporting four fully-evidenced delivered outcomes as unsubstantiated,
generated by the command the runbook prescribes, at the moment it prescribes it. **A safety
mechanism that fires spuriously at the point of delivery does the damage the mechanism exists to
prevent, in the other direction.**

⚠ **The first fix was wrong and was withdrawn.** Raising on a missing store broke two real
contracts — a review of work with no task store must still assemble, and *no mission record* is not
the same as *a clean mission record*. Replaced with the narrower change: resolve the default
through the shared repo root, fall back for a relative `.data/…`, keep the announced degradation
exactly as it was, and add a **publication gate** that inspects the FINISHED document
(`publication_block()`; `--out` refuses to write one that understates itself). ⭐ **The gate checks
the output, not the inputs** — a path that resolved by luck still passes, one that looked right but
produced a degraded document still fails.

### Also closed this pass

- **F105** — `test_client_review.py` built `.data` from `__file__`, so it was RED in every worktree
  and GREEN only in the primary. It was detecting its own broken input, not a grounding regression.
- **A client-data guard that did not exist where it was needed.** The `.gitignore` rule protecting
  `pbi-ad-sales-live-2026-09-01/` (a live PBI read for a named client — production workspace and
  model names, measured sales/cost/conversion rows) was added to `main` *after* the mission branch
  forked, so the branch was unprotected. It read as safe only because the capture is not present in
  that worktree — **an absence of the file, not a guard.** Ported and proved with a real file.
- **The D5 open citation**, back-filled — deliberately *not* by committing the capture, which is
  what the rule above exists to stop. Its offline validator re-run: **ALL PASS, exit 0**.
- **The dashboard suite recorded as "NOT RUN — would collide with a live session"** → **108
  passed**. ⚠ The blocker was **stale by three weeks**: D5 recorded the collision on 2026-09-01 and
  the file's mtime is 2026-08-11. *A recorded blocker is a claim with a shelf life; re-measure it
  before inheriting it.*
- **A flagged suspicion that was NOT a defect.** D5 doubted `expect(meta.groundedRoas).toBe(0)` as
  possible absence-as-zero (the F103 shape). Both surfaces gate on `groundedSpend > 0` —
  `MetricGrid` renders an em-dash, `PlatformView` omits the row — so the zero never reaches
  `formatRoas`. Recorded as a **verified negative** so nobody reopens it.

### Validated at which layer

- **Query/logic:** 751 tests on the branch; 17 mechanical checks re-derive every load-bearing
  D3/D4/D5 figure from the machine-generated tables.
- **Rendered:** the regenerated artefact in real Chromium — 8 sections painted at 760/1100/1440,
  light and dark, standard and Live Meeting mode; 0 blank, 0 clipped, 0 horizontal scroll,
  0 console errors, 0 offsite requests, **0 operator-only leaks**, renders with JS disabled.
  ⚠ **760 px and above only** — phone widths unmeasured.

### Still open — four of them are one credential decision

| open | verdict | unblocked by |
|---|---|---|
| deployed `SNOWFLAKE_SCHEMA` | `NOT-VISIBLE` | read Vercel's env |
| warehouse-mode rendering | `NOT-EXERCISED` | a read-only-proved credential |
| `DATE RANGE` inertness in warehouse mode | `NOT-MEASURED` | the same |
| every Power BI rendered figure | `NOT-EXERCISED` | a DAX instrument |
| Candidate C (`MARKETING_FCT_PLATFORM_DETAIL`) | held open | a **decision**, not a measurement |
| the three client questions | unchanged | ⚠ CD-1's ROAS set must be **enumerated before asking** — the cited "16" is unreproducible (12 Aug / 14 Jun) |

**The D5 architectural sign-off stands.** Nothing in this pass contradicts it; the §0a reopen
condition was not triggered.

⚠ **Two Jira drafts are unposted** — `boot-prompts/drafts/GP-319-comment-2026-09-01.md` (the design
pass) and `boot-prompts/drafts/GP-319-finalization-comment-2026-09-01.md` (this one). The Atlassian
MCP was unavailable in both sessions. This page confirms GP-319 is the Navira Marketing Model
ticket, which raises confidence in the key the drafts guessed by content match — but the key should
still be eyeballed before pasting.

## 2026-09-08 — three designs built from the reach matrix, and TWO of this ticket's own claims refuted

Read-only synthesis session. **No queries run, no renders, nothing deployed.** Every figure is
re-used from evidence already committed under `aldc-launchpad/docs/evidence/gp319/`, each carrying
the date it was measured. Client-facing artifacts produced for Paul; **not sent to Navira.**

### ⛔ Correction 1 — "Google spend has no product grain" is FALSE, and we published it

The claim rested on `GOOGLE_ADS.GOOGLE_ADS_PRODUCT` holding **0 rows**. That is the *raw aggregator
landing table*. Measured 2026-08-24 in `gp319_windsor_field_reality.json`, the **built** object
beside it tells the opposite story:

| object | rows | carries |
|---|---:|---|
| `GOOGLE_ADS.GOOGLE_ADS_PRODUCT` | **0** | the empty one we cited |
| `WAREHOUSE_SOURCE.MARKETING_GOOGLE_PRODUCT_SPEND` | **181,896** | `OFFER_ID`, `PRODUCT_TITLE`, `PRODUCT_BRAND`, `ACTIVITY_DATE`, `SPEND_USD`, `CLICKS`, `IMPRESSIONS`, `CONVERSIONS_VALUE` |
| `WAREHOUSE_SOURCE.MARKETING_GOOGLE_OFFER_SKU` | view | offer → SKU mapping already exists |

Yet the model reads Google as **100% unkeyed** — $257,800.32, every dollar
(`gp319_unkeyed_spend_split.json`).

⭐ **So Google product-grain spend exists upstream and is not wired into the fact the model reads.**
A wiring gap, not a missing source — and far cheaper. It also *strengthens* the conformed-core
option, which can absorb those rows without redesign.
⚠ **NOT MEASURED:** whether `OFFER_ID` resolves to `PRODUCT_KEY`, and at what coverage.

⭐ **The durable lesson, and it is the [[vacuous-verification]] family pointed at object naming:**
a `*_PRODUCT` table with 0 rows and a `MARKETING_*_PRODUCT_SPEND` table with 181,896 rows sat in the
same database, and the name similarity was enough to make one stand in for the other. **Check which
object a consumer actually reads before concluding a source is absent** — the same rule as never
inheriting a target, applied to an absence rather than a symptom.

### ⛔ Correction 2 — "one credential blocks three measurements" was wrong; two were already answered

| question | status before | measured status |
|---|---|---|
| Does the Google/Meta feed expose a raw final URL? | "blocked on a credential" | **ANSWERED 2026-08-24 — ABSENT** from what lands |
| Does the Google product table exist / how fresh? | "blocked, gates the tier" | **ANSWERED** — see correction 1 |
| Amazon UK: SP advertised-product report, or EU region feed? | blocked | **still open** — and answerable from the Amazon Ads account, not our warehouse |

Enumerated non-system columns: `GOOGLE_ADS_CAMPAIGN` **27**, `META.FACEBOOK_AD_INSIGHTS` **18**.
Neither carries a URL, destination, landing-page or product column.

⇒ **The pointer column (Heather's point 2) is deliverable now for Amazon SP (ASIN, 0.1% unkeyed) and
Google (`OFFER_ID`). Meta has no pointer at all** and cannot get one from any query — it needs an
aggregator field-set request, which is a vendor action, not engineering.
⚠ Stated precisely: **absent from what currently lands ≠ absent from the aggregator's catalogue.**

### The three designs, and the one question that separates them

The only capability the model lacks is **a product axis on all-platform spend**. Measured from
`gp319_guide_REACH.json` (one discriminating query per cell, controls passed): `Marketing Measures`
splits by Product/Brand/Campaign/Platform but covers **Amazon SP+SB only** (grand 2,668,277.36);
`Marketing Efficiency` covers **all three platforms** (2,926,231.33) but **blanks** on Product,
Brand, Vendor, Campaign and Platform. **No measure is both.**

| # | design | shape | est |
|---|---|---|---|
| 1 | **Narrow Core** ⭐ recommended | conformed core-10 fact + per-platform extension facts | ~1.5 d |
| 2 | **Two Facts, One Bridge** | keep both facts, add ONE conformed marketplace dim, fix by naming | ~1 d |
| 3 | **Marketing-Only Model** | move `Order Line` + `Order` out to the Daily model | ~4 d |

**Design 1 is recommended because it already exists** — clone `66fd0f42` verified 2026-08-27, 13
measures PASS, guard raises across platforms and not for a single platform, zero leakage onto
`2d8587b5` (383 measures before and after). Its risk is naming and sign-off, not feasibility.

⭐ **Design 3's argument is one measurement nobody had put on the table.** From
`gp319_model_size.json`: the model is **2.89 GB**, of which `Order Line` is **1,087 MB** (3,430,238
rows / 122 cols) and `Order` is **952 MB** (2,865,574 rows) — **70.6% of the model is sales tables**,
carrying a **33.4 MB** marketing fact. And `Order Line` is the table pinned at 1 August, so *taking
it out of the model removes that entire defect class* rather than patching it.
**Design 3 is orthogonal to 1 and 2** — it decides which model owns sales, not what shape the
measures take — so it can follow Design 1 without rework.

### Nicholas's matrix picks the shape on its own

From `nicholas-metric-matrix-readout.md`: of 369 NOW metrics, **10** are supported by 5 of 5
platforms and **0** by 4 of 5. Today's three live sources carry **291 platform-exclusive** metrics
against an overlap of **11**. ⛔ **A bimodal distribution rules out one wide fact with a column per
metric** — at core grain it would be ~90% null — and is the signature of conformed-core-plus-
extensions. ⭐ **Designing for the future costs nothing:** the set buildable today and the set that
survives Walmart/Target/eBay are the *same ten metrics*; CPM is the only canary.

### Client feedback 2026-09-08 (Nicholas via Heather) — four points, three of which REMOVE scope

1. **Sequence C now, A next, B shelved** — agreed.
2. **Pointer column**: rawest destination per platform, resolution stays client-side. Smaller than scoped; moves independently of the A/B/C fork.
3. ⭐ **No ratio measures from source** — she computes ROAS/CTR/CPC from components. **Closes SALES-009 / B2-04 as client questions** and removes one of Tier 0's two blocking decisions. (The "16 ROAS measures" count was never reproducible: three enumerations gave 12 / 14 / 16.)
4. **Platform ≠ ad type** — three platforms (Amazon, Google, Meta); SP/SB/SD are ad types *within* Amazon. Design C caveat to be rewritten. Channel mix by rows: Amazon 901,086 · Google 12,315 · Meta 550 = **98.6% Amazon**.
5. **`(no product key)` stays TWO visible lines**, never merged — matching the discriminating test: sentinel $487,776.85 + RI orphans $36,433.77 = $524,210.62, residual **−0.00**.

### State on the way out — honest

- ⛔ **Nothing deployed.** No model change, TEST or PROD. No queries run this session.
- ⛔ **Jira comment NOT posted** — the Atlassian MCP call was refused by the auto-mode classifier.
  Draft at `aldc-launchpad/boot-prompts/drafts/gp319-jira-design-options-and-two-corrections-2026-09-08.md`.
  **Ticket key was verified in Jira this session** (GP-319, `QA`, Paul) rather than guessed by content match — which the two 2026-09-01 drafts could not claim.
- ⛔ **Still not done, unchanged:** rendered-layer check on `2d8587b5`; screenshots; the 1,152-pair axis matrix has **never** been pointed at this dataset; the `Ad Spend (Starter)` Excel perspective proof (~5 min, gates Design C).
- ⚠ **`MARKETING_GOOGLE_*` objects are not in the `clients` repo** — searched, no DDL found. They are built somewhere unversioned. Worth its own ticket.
- ✅ Two client-facing artifacts produced (a status board with an interactive access decision, and the three-design comparison). **Neither has been sent to Navira.**

---

## 2026-09-08 (later) — cross-referenced against the ticket-consolidation artifact

Three parallel read-only lanes (connectors · warehouse · Power BI) run against artifact `5af0d836`
(**61 line items → 13 tickets**, contract: *"nothing dropped"*). ⛔ **No Snowflake query, no PBI
connection, no credential.** Every finding is `REPO-ONLY` or `CAPTURED-DEPLOYED`; live confirmation
outstanding. Full detail: `aldc-launchpad/docs/evidence/gp319/SWEEP-2026-09-08.md`.

⚠ **Notes from the 2026-09-08 call are NOT folded in.** Complete against the artifact, incomplete
against the call.

### Corroborated — six artifact rows now have a second instrument

`C1` unmanaged canonical fact (**three objects, not one**) · `C2` five sentinel sites, exact lines ·
`C3` mixed product-key domain · `MC` starter list never proven in Excel · `B3-03` SB ASIN ceiling ·
`P2` Meta has no destination field. ⭐ **`C4` may already be answered** — the 2026-09-01 pass measured
Sponsored Display in *both* copies (9,566 rows / $6,485.38), so the "worth an hour of credentialed
probing" narrows to whether the union *sums* them twice.

### ⛔ Three artifact claims that measurement contradicts

1. **"Sponsored Brands and Display carry their raw target payload as given"** — nothing supports it.
   SB campaign report **16 cols, all metrics**; SB ad report `ADID`/`ADGROUPID`/`CAMPAIGNID`, **no
   ASIN**; SD campaign report **18 cols, all metrics**. A regex for
   `(KEYWORD|TARGETING|SEARCH_TERM|PLACEMENT)_REPORT` across `clients/GEP`, `sf_ops`, `docs/evidence`
   and the dashboard repo returns **zero hits**, and the connector calls **no ad- or creative-level
   endpoint at all**. ⇒ **The pointer ask is three jobs at three costs** — SP a view change,
   Google/Meta an aggregator field-set request, **SB/SD an endpoint we have never called.** The 1.5 d
   estimate does not survive the third.
2. **"No ad-type axis exists anywhere"** — true of the *model*, false of the *estate*.
   ⭐ `WAREHOUSE.MARKETING_DIM_CAMPAIGN_ATTRS` **exists, deployed, carrying `AD_PRODUCT`** (13 cols,
   incl. `TARGETING_TYPE`, `PORTFOLIO_ID/NAME`, `BID_STRATEGY`, `CAMPAIGN_STATE`, `BUDGET_AMOUNT`) —
   and **the Next.js dashboard already reads and renders it** (`warehouse.ts:72`, join asserted
   `warehouse.test.ts:943-948`, rendered `insite-table.tsx:338,393`). A **propagation gap, not a
   greenfield build**, which likely *reduces* the 0.5 d estimate. ⚠ Deployed-only — zero occurrences
   in `clients`. **This also refutes this ticket's own description**, which says the campaign dim is
   "campaign_id + campaign_name only… the dimensions half has not arrived": true of
   `MARKETING_DIM_CAMPAIGN` (3 cols), false of the family.
3. **"The platform field carries two members"** vs `gp319_platform_axis_check.json`
   (2026-08-24T21:53:52Z), which reports **five**. ⭐ Both right on different bases — **five declared,
   two non-blank**. This estate has paid for that distinction twice already
   (*"inertness needs `nonblank > 1`, not `members > 1`"*). **Name the basis, or the ad-type work is
   scoped against the wrong number.**

### Eight findings with no home in the 61

- ⭐ **`ADVERTISEDASIN` lands and nothing selects it** — the fact reads `ADVERTISEDSKU`
  (`marketing_fct_activity.sql:130`). ⛔ **A trap, not just a win:** Heather wrote *"the ASIN, already
  present… just put it in the pointer column as-is"* — but for SP **`PRODUCT_ID` IS the SKU**. Anyone
  following that literally **ships the SKU labelled as an ASIN**, and it reconciles perfectly against
  itself.
- ⛔ **Adding a field FORKS the landed table** — no `ALTER` path anywhere in
  `connector/lib/warehouse/`; field-set equality creates a **new versioned table**. Measured
  precedent in that function's own comments: **48 fragments** from a four-column shortfall,
  2026-08-14. PK is **per table**, so a re-pulled row **survives twice in the union view**. **The
  highest-impact omission — it is the cost of the pointer ticket.** Durable page:
  [[connector-field-addition-forks-the-table]].
- ⛔ **`REPORT_COMMON.MARKETING_EFFICIENCY`'s body is POSITIONAL** — bare `SUM()`, no aliases
  (`:147-151`), four positional `UNION ALL` arms. **A new column must be appended LAST in every arm**
  or every name past it re-maps, silently. `_MARGIN`'s `EFF` CTE re-aggregates *by name* and will
  **drop** a new column. ⛔ **Refutes this ticket's description**: *"explicit `SELECT col AS col`
  lists, so widening is additive and non-breaking — two view edits per measure."*
- **Widening costs 7–8 SQL objects, 3 of them hand-deploys** with no repo source (`_PREPROD`, both
  `UNIFIED`) — extends the "`MARKETING_GOOGLE_*` are unversioned" note above to the fact family.
  `_PRODUCT` is a **second independent widening path** (reads `_PREPROD` directly). ⚠ `CURRENCY_CODE`
  is **grain-forming but never emitted**; the grain itself is nowhere declared.
- **`CURRENT_SPONSORED_BRANDS_PURCHASED_PRODUCT_REPORT` is landed in prod and non-prod with zero
  warehouse references**; columns `NOT-CAPTURED`. The only SB product-shaped lead. ⚠ **A purchased
  product is an outcome, not where the ad points** — price it, don't promise it.
- ⛔ **`PLATFORM_ID` is the display string used as the join key** (`marketing_dim_platform.sql:19-24`
  says so and warns a near-miss joins to nothing). **"Relabel five members to three" is not
  available.**
- **The Windsor field list exists twice and disagrees** — Prefect Google 27 vs Eclipse 26; Meta 18 vs
  16; GEP and [[fusion92]] hold separate copies. Which is authoritative is `NOT-FOUND`.
- ⛔ **GP-287 — a correction to this session's own first reading.** The lane concluded "unmerged and
  undeployed"; **that was wrong.** The feed is deployed and still landing (8,061 rows / $84,501.30
  through 2026-08-22). The real residual is that the *source* sits unmerged on a feature branch. See
  [[GP-287]]. ⚠ **The lesson: neither a branch nor a `clients` template is a deployment instrument
  for a Cosmos-deployed Eclipse feed** — the inverse of the rule about inferring active state from
  repo config, and just as wrong.

### Inconsistencies inside the consolidation itself

- ⚠ **H4 vs Design C.** H4 ("no ratio measures") routes to GP-320 — but **Design C's starter
  perspective ships four ratios**: `CTR`, `Cost per Click`, `Amazon ACOS (fee-based)`,
  `Amazon Advertising Cost %`. **"Ship C now" hands her what H4 declines.** ⭐ **Ask before
  stripping** — hers was about the *source feed*, and the ACoS/TACoS pair are fee-based settlement
  figures she may use. Removing a field a client uses is the GP-318 Heather-regression shape exactly.
- ⭐ **The perspective is already APPLIED and live on `2d8587b5`** — 6 tables / 23 fields, confirmed
  2026-08-24, -25 and -26. The `..._perspective_ROLLBACK_...json` file is a **pre-apply capture, not
  an executed rollback** (`perspectives_before: []`). So "ship C" is really *prove C renders*.
- ⚠ **The labels ticket says the sentinel is "unrecoverable at source"**, while `B3-03` is the
  **$30,892 recoverable** slice of that same sentinel — **our join defect, not the platform's.**
- ⚠ **Three no-key bases in play** — labels card $487,776.85 + $36,433.77 = $524,210.62; per-platform
  SB $203,309 · SD $3,375 · Google $234,111 · Meta $16,630 · SP NULL $2,341; model-layer $514,141.96
  of $2,957,287.69. ⭐ Her split assumed *Amazon `-1`* vs *everything else* — in fact **sentinel `-1`
  is the mechanism for all four non-SP platforms**, so "everything else unattributed" is **$2,378**,
  not the ~$250K it reads as.
- ⚠ **Three ad-spend totals, no two reconcile** — model $2,957,287.69 · warehouse $2,937,010.66 ·
  spec local-currency $2,971,113.44. Model−warehouse $20,277.03 against a currency delta of
  $21,192.45: **$915.42 residual unexplained.** `DERIVED, plausible, not proven`. ⛔ **Never put two
  of the three in one client document.**
- ⚠ **The client-facing readout still says "410 field-list items"** (2026-08-24T21:07:54Z); latest is
  **400** (2026-08-25T16:13:01Z). The artifact uses 400; the document Heather is responding to does not.

### Method note worth keeping

⭐ **`execute_dax` used to turn every DAX error into "no rows"** — `executeQueries` returns HTTP 200
with the error nested in the payload. Fixed in `pbi_ops/client.py`, but **no sweep of prior results
was ever run.** Any analysis in this repo concluding "zero" or "no data" from `execute_dax` before
that fix may have been an error in disguise.
