---
tags: [ticket, gep, inventory, snowflake, pbi, implementing]
aliases: [GP-208, Inventory Intake, Inventory Modelling]
sources: [sources/obsidian-import/work/gep/GP-208, GEP/notes/GP-208_client_questions.md]
created: 2026-04-16
updated: 2026-04-27
---

# GP-208 — Inventory Feed Ingestion & Modelling

Greenfield inventory fact table for [[GEP]]. Replaces the prior unused `inventory_fct_balance` with a clean model sourced from the existing Sellercloud + Amazon pipelines already landing in `PROD_DG1_GEP`.

## Status (2026-04-27) — PAUSED: awaiting client response

- **gep-feature workflow: `scoped` stage — paused pending client Q&A**
- **Requirements rebuilt 2026-04-27**: schema rebuilt from client sample CSV (Iteration 46). 26 columns, 11 NULL Phase 1b placeholders. ITERATION excluded.
- **Phase 1 split into 1a/1b**: 1a ships with existing sources; 1b wires up NULL columns once client confirms scope.
- **Phase 1b investigation complete** (2026-04-27): all 5 column groups researched against connector templates and SP-API/Sellercloud docs. See Phase 1b Investigation section.
- **Client questions posted** to Jira comment 26539 (2026-04-27): Direct FBA/SCC status, kit quantities, velocity, cost & retail.
- **Confirmed internally (no client input needed):** CA FBA ✓, AWD ✓, FBA reserved sub-buckets ✓.
- **Next session**: run `/gep-feature GP-208` once client or CSM/EI Lead responds. Will update open questions Q2–Q4, Q6 (Direct/SCC), Q8 with answers, then transition to `implementing`.
- **Artifact**: `GEP/tickets/GP-208/artifact.yaml` — stage: `scoped`

## Next Session Boot

```
SHIPYARD_HOME = C:/Users/PaulRussell/repos/aldc-shipyard
clients branch:  feature/paulrussell/GP-208/inventory-feed-ingestion-and-modeling
aldc-shipyard: main branch, fully set up (setup.py run, pbi_config.yaml populated for TEST)
workspace file:  aldc-shipyard.code-workspace (multi-root: aldc-shipyard + clients)
```

Resume point: `scoped` stage — waiting for client response to Jira comment 26539.

1. Check Jira GP-208 for client response on: Direct FBA/SCC (Q6), kit quantities (Q8),
   velocity (Q2), cost & retail (Q3).
2. Update artifact open questions Q2, Q3, Q4, Q6, Q8 with answers.
3. Incorporate any schema changes (new columns if client says yes to any).
4. Run Jira status sync → "In Progress", then transition to `implementing`.
5. Share health check: run once per session before sandbox deploy (--check-share).

Confirmed without client input: CA FBA ✓, AWD ✓, FBA reserved sub-buckets ✓.
These get Phase 1b Eclipse template work regardless of client response.

## Current Deploy Plan

Shortened path: TEST first (so we catch PBI integration issues before promoting), then single merge forward.

| # | Step | Where | Status |
|---|------|-------|--------|
| 1 | Prereq check: confirm `TEST_DG1_GEP.WAREHOUSE.PURCHASING_FCT_BALANCE` and `SHARED_DIM_PRODUCT` exist and are fresh | Snowflake | ✅ 2026-04-16 |
| 2 | Run `inventory_fct_balance.sql` against `TEST_DG1_GEP` — creates `WAREHOUSE_SOURCE.INVENTORY_FCT_BALANCE` view and `WAREHOUSE.INVENTORY_FCT_BALANCE` table | Snowflake | ✅ 2026-04-16 (rebuilt after FBA CTE fix) |
| 3 | Run `extract_inventory_current.sql` against `TEST_DG1_GEP` | Snowflake | ✅ 2026-04-16 (rebuilt after MAX-date filter removed) |
| 4 | Re-run QA queries Q1, Q3, Q7a, Q7 against the `WAREHOUSE.INVENTORY_FCT_BALANCE` table to confirm parity with personal-schema results | Snowflake | ⏸ Blocked on Sellercloud share |
| 5 | Add `EXTRACT_INVENTORY_CURRENT` as data source in GEP Test Models pbix | Power BI Desktop | ⏸ Blocked on Sellercloud share |
| 6 | Hide placeholder columns, set aggregation defaults, build baseline visual | Power BI Desktop | ⏸ Blocked on Sellercloud share |
| 7 | Publish to GEP Test workspace, verify in PBI Service | Power BI | ⏸ Blocked on Sellercloud share |
| 8 | Merge feature branch → `GEP/development` → `GEP/user-testing` | Git | ⏸ Blocked on Sellercloud share |
| 9 | Send UAT data dictionary (`_testing/GP-208/uat_column_coverage.md`, move to shareable location first) to client | Shareable channel | ⏸ Blocked on Sellercloud share |

See [[gep-snowflake-pbi-deployment]] for the canonical runbook; this plan is a shortened variant that validates PBI before code promotion.

## TEST Deploy Findings (2026-04-16)

The TEST deploy surfaced two code issues and one upstream infrastructure issue. Both code issues were fixed; the infrastructure issue is out of scope.

### Code fix 1 — Amazon FBA CTE was picking stale per-SKU rows

**Symptom**: After deploying the fact, `SELECT MIN(BALANCE_DATE), MAX(BALANCE_DATE) FROM WAREHOUSE.INVENTORY_FCT_BALANCE` showed dates spanning 2024-08-11 → 2026-04-16. "Current snapshot" fact should only contain today's dates.

**Root cause**: The AMAZON_FBA CTE used `QUALIFY ROW_NUMBER() OVER (PARTITION BY SKU ORDER BY ___ALDC___GLOBAL_HISTORY_TIMESTAMP_START___ DESC) = 1` — this picks the latest capture *per SKU*, which retains rows for SKUs that dropped out of the FBA report months ago. Their "latest" was 2024-08-11 or similar.

**Fix**: Pin to the latest global batch instead of per-SKU latest:

```sql
WHERE ___ALDC___GLOBAL_HISTORY_TIMESTAMP_START___ = (
    SELECT MAX(___ALDC___GLOBAL_HISTORY_TIMESTAMP_START___)
    FROM PROD_DG1_GEP.AMAZON.CURRENT_REPORT_FBA_INVENTORY
)
-- Secondary dedup for rare same-batch SKU dupes
QUALIFY ROW_NUMBER() OVER (PARTITION BY SKU ORDER BY SKU) = 1
```

This aligns with Phase 2's future `CAPTURE_TIMESTAMP`-pinned append-only logic. Row count dropped from 7,470 → 3,510 post-fix, which is the correct count for "latest-batch FBA unioned with latest-date Sellercloud."

**Related concept**: [[accumulating-source-tables]] — this is the same class of issue documented there, but the fix there (latest-per-key) only works for *history* facts. Current-snapshot facts need latest-global-batch. Consider updating that page to distinguish the two patterns.

### Code fix 2 — Extract's MAX(BALANCE_DATE) filter silently hid Sellercloud rows

**Symptom**: After the fact rebuild to 3,510 rows, the extract returned only 388 rows. Filter `WHERE FCT.BALANCE_DATE = (SELECT MAX(BALANCE_DATE) FROM WAREHOUSE.INVENTORY_FCT_BALANCE)` was dropping any rows where BALANCE_DATE ≠ MAX — in Phase 1 this is a silent data loss whenever any source lags.

**Root cause**: The MAX-date filter was designed for Phase 2 (append-only history, where "current" means "latest captured snapshot"). In Phase 1, the fact is already current-snapshot by construction (one row per PRODUCT_ID at its source's latest capture). BALANCE_DATE varies per row depending on which source contributed — FBA rows have today's date, Sellercloud rows have 2026-03-04. Filtering by global MAX throws away the Sellercloud rows.

**Fix**: Remove the `WHERE` clause entirely in Phase 1. Re-introduce it in Phase 2, but pin by `CAPTURE_TIMESTAMP` not `BALANCE_DATE`.

### Infrastructure issue — Sellercloud data share stale 42 days

See next section.

## Sellercloud Staleness Incident (Discovered 2026-04-16)

**Summary**: `PROD_DG1_GEP.SELLERCLOUD_SQL.CURRENT_MAIN_INVENTORY_PANDL` (and its sibling `COMBINED_MAIN_INVENTORY_PANDL`) have not received new data since **2026-03-05 07:01:07 PST** — 42 days of staleness. The Eclipse connector is running and completing partitions successfully in the producer account; the data share into `PROD_DG1_GEP` is where the freeze is.

### Evidence

| Check | Result |
|---|---|
| `MAX(TO_DATE(DATE))` in `CURRENT_MAIN_INVENTORY_PANDL` | 2026-03-04 |
| `MAX(___ALDC___GLOBAL_HISTORY_TIMESTAMP_START___)` | 2026-03-05 07:01:07 PST |
| Same values in `COMBINED_MAIN_INVENTORY_PANDL` | identical (rules out CURRENT-view filter bug) |
| Row counts in both views | ~6.2M, identical ±40 rows |
| Eclipse template "GEP Seller Cloud VPN" partition history | 2026-04-12 → 2026-04-16 all **Complete**, multiple successful runs per day, durations 4–55 secs |
| Eclipse template config | `schema: dbo`, `table: INVENTORY_PANDL`, `category: MAIN` → naming convention says this target = `CURRENT_MAIN_INVENTORY_PANDL` |

### What's ruled out

- ✗ Eclipse template broken — it's healthy and completing
- ✗ CURRENT-view dedup logic — COMBINED is equally stale
- ✗ GP-208 fact/extract code — ingest layer is the bottleneck
- ✗ Single-partition failure — 42 days is full pipeline stoppage, not flaky days

### What's ruled in (speculative, needs producer-side investigation)

- Data share from producer account is frozen — producer has fresh data but it's not being propagated to `PROD_DG1_GEP` consumer
- Producer-side refresh task (`CREATE OR REPLACE TABLE` or similar) stopped on 2026-03-05
- Possible "warehouse reset" needed on producer side (raised by Paul during investigation — unknown exact operation)
- Related wiki: [[snowflake-data-share-refresh]] documents the `CREATE OR REPLACE TABLE` atomicity concern, but not this specific "frozen share" failure mode — that page needs updating

### Blast radius

Any downstream consumer reading `PROD_DG1_GEP.SELLERCLOUD_SQL.CURRENT_MAIN_INVENTORY_PANDL` or `COMBINED_MAIN_INVENTORY_PANDL` has been seeing 2026-03-04 data for 42 days. Client hasn't used inventory views recently per Paul, so detection was delayed. This is a production data freshness incident beyond GP-208 scope.

### Action items for pickup

- Discuss with another engineer (planned 2026-04-17) — determine producer-side ownership and whether a warehouse/share reset is needed
- Investigate what happened on 2026-03-05 07:01 PST — was there a deploy, config change, or producer-side failure that coincides?
- Consider whether to file a separate Jira ticket for the Sellercloud freshness incident, since it affects more than just GP-208
- Once Sellercloud is restored: rerun the TEST fact + extract, re-run QA queries, then resume deploy plan steps 4–9

See also: [[snowflake-data-share-refresh]], [[data-share-pattern]]

## Client Direction (Confirmed 2026-04-16)

- **No data share** from GEP's Snowflake.
- Data sources are the same pipelines that fed the historical `inventory_fct_balance` / `inventory_fct_historical_balance` views — Sellercloud + Amazon FBA in `PROD_DG1_GEP`.
- ITERATION excluded from scope — feed-specific Sellercloud batch number with no pipeline equivalent.
- 11 NULL placeholder columns included in the schema for transparency; each maps to a Phase 1b investigation group below.

## Phased Approach

**Phase 1a — Current snapshot with existing sources (in progress)**
- Grain: `PRODUCT_ID × BALANCE_DATE`, one row per product, latest snapshot only
- `WAREHOUSE.INVENTORY_FCT_CURRENT` refreshed via full replace
- Schema matches client sample feed exactly (26 columns); NULL placeholders for the 11 columns with no current source
- Ship and get Phase 1a client approval before wiring up Phase 1b columns

**Phase 1b — Wiring up NULL placeholder columns (investigation needed)**
- Each of the 5 column groups below requires either a new connector template or client clarification
- Client questions Q2–Q8 map to these groups
- See [[Phase 1b Investigation]] section below

**Phase 2 — Append-only history (future)**
- Switch the table's scheduled task to `INSERT ... WHERE CAPTURE_TIMESTAMP > MAX(existing)`
- Table accumulates forward from Phase 2 cutover date
- `INVENTORY_FCT_HISTORICAL` (separate table from `INVENTORY_FCT_CURRENT`)

## Phase 1b Investigation

The 11 NULL columns fall into 5 source groups. Each group has been investigated against the current connector pipeline.

### Group A — FBA Reserved Sub-Buckets ✅ Template-only fix

**Columns:** `US_FBA_RESERVED_ORDERS_QTY`, `US_FBA_RESERVED_PROCESSING_QTY`, `US_FBA_RESERVED_TRANSFER_QTY`

**Investigation result (confirmed via SP-API docs):**
`GET_RESERVED_INVENTORY_DATA` is **already supported** in the Eclipse Amazon connector (`amazon_sellercentral.py` line 946). No code changes needed. The report returns exactly the 3-way split:

| SP-API column | Our column |
|---|---|
| `reserved_customerorders` | `US_FBA_RESERVED_ORDERS_QTY` |
| `reserved_fc-processing` | `US_FBA_RESERVED_PROCESSING_QTY` |
| `reserved_fc-transfers` | `US_FBA_RESERVED_TRANSFER_QTY` |
| `reserved_qty` | (total lump — optional addition) |

Primary key: `ASIN + FNSKU + SKU`. No new credentials — uses existing FBA Reports permissions on the current Amazon connection.

**Work needed:** Add one new Eclipse template file `GEP/eclipse/templates/amazon/reserved_inventory_report.json` — clone of `inventory_report.json` with `filter_report: "GET_RESERVED_INVENTORY_DATA"` and `primary_key: ["ASIN", "FNSKU", "SKU"]`. Then join to the fact via SKU = PRODUCT_ID.

**Effort: Very low — new template file only, no code or permission changes.**

**Client question (Q5):** Do you want the 3-way reserved breakdown? (We now know we can deliver it with minimal effort.)

---

### Group B — Direct FBA + Supply Chain Connect (SCC) FBA ❌ No source exists

**Columns:** `DIRECT_FBA_INBOUND_WORKING_QTY`, `DIRECT_FBA_INBOUND_SHIPPED_QTY`, `SCC_FBA_INBOUND_WORKING_QTY`, `SCC_FBA_INBOUND_SHIPPED_QTY`

**Investigation result (confirmed via SP-API docs):**
These columns do **not exist in any standard SP-API report.** `GET_FBA_MYI_UNSUPPRESSED_INVENTORY_DATA` has no program-type breakdown. The program-level split in the client's sample likely comes from Amazon's Inventory Performance dashboard (a manual UI export) or a custom Seller Central report — neither of which is available via any SP-API endpoint.

All 4 columns were zero in the 100-row sample, suggesting the Direct FBA and SCC programs may not be active for GEP.

**Work needed:** None until client confirms these programs are active and a data source can be identified. If programs are inactive, columns stay NULL with no action required.

**Client question (Q6):** Are Direct Fulfillment and Supply Chain Connect (SCC) programs actively used? If programs are not active, these columns will remain NULL indefinitely — we should document this in the data dictionary rather than building a connector with no source.

---

### Group C — CA FBA ✅ Template-only fix

**Column:** `CA_FBA_AVAILABLE_QTY`

**Investigation result:**
The existing Amazon connection (`connection_id: b21192a3`) already supports the CA marketplace — confirmed by the live `sales_and_traffic_report_child_asin_CA.json` template which uses `filter_marketplace: "ca"`. The `GET_FBA_MYI_UNSUPPRESSED_INVENTORY_DATA` report type works with the CA marketplace filter on the same connection/credentials.

**Work needed:** Add one new Eclipse template file `GEP/eclipse/templates/amazon/inventory_report_ca.json` — clone of `inventory_report.json` with `filter_marketplace: "ca"` and a distinct `table_name` (e.g. `"FBA_INVENTORY_CA"`). Then join to the fact via SKU = PRODUCT_ID for CA-enrolled products.

**Effort: Very low — new template file only, no code or permission changes.**

**Client question (Q6 continued):** Is GEP enrolled in Amazon.ca FBA? (Required to confirm data will actually populate.)

---

### Group D — AWD (Amazon Warehousing & Distribution) 🔧 New connector category needed

**Columns:** `AWD_INTRANSIT_QTY`, `AWD_AVAILABLE_QTY`

**Investigation result (confirmed via SP-API docs + python-amazon-sp-api docs):**
AWD is supported in the `python-amazon-sp-api` library as `AmazonWarehousingAndDistributionV2024_05_09`. The `listInventory` operation returns per-SKU quantities:

| SP-API field | Our column |
|---|---|
| `totalOnhandQuantity` | `AWD_AVAILABLE_QTY` |
| `totalInboundQuantity` | `AWD_INTRANSIT_QTY` |

This is a **live API call** (not a report). The existing connector's `category: "report"` handler won't cover it — a new `category: "awd"` handler is needed in `amazon_sellercentral.py`. Additionally, the SP-API app registration must be updated with the `awd:distribution_center_inventory:read` scope — this is a new permission not currently on the GEP Amazon connection.

**Evidence GEP IS enrolled:** Sample row `MS_26-7000` had `AWD InT = 5120` — real data.

**Work needed:** (1) New connector category `awd` in `amazon_sellercentral.py`, (2) New template `GEP/eclipse/templates/amazon/awd_inventory.json`, (3) Add `awd:distribution_center_inventory:read` to the SP-API app in Amazon Seller Central.

**Effort: Medium — new connector code + template + permission update. Recommend separate scoping ticket.**

**Client question (Q7):** Confirm AWD scope: should this be included in Phase 1b or tracked as a separate ticket? Note that we need to update the SP-API app permissions, which requires access to the Amazon Seller Central developer console.

---

### Group E — Kit Quantities ⚠️ Investigation needed

**Columns:** `PENDING_KIT_QTY`, `WORKING_KIT_QTY`, `FBM_OFFERED_KIT_QTY`

**Investigation result (confirmed via Sellercloud API docs):**
The Sellercloud REST API (`/api/Inventory`) does **not** expose kit quantity fields (`KitPendingQty`, `KitReadyQty`, etc.). The REST inventory response only includes: `PhysicalQty`, `ReservedQty`, `InventoryAvailableQty`, `OnOrder`, `OnBackOrder`, and aggregates — no kit-specific quantities.

`SUPPLEMENT.CURRENT_SKU_KITS_CSV` (connection at `/media/nextcloud/.../SKU Kitting`) is a manually-uploaded CSV mapping kit structure (which SKUs are kits), not inventory quantities.

**Path forward:** Check whether the `INVENTORY_PANDL` SQL table (already fully ingested via our SQL connector with `fields: []`) has kit quantity columns in GEP's Sellercloud database. If present, they'd already be in `PROD_DG1_GEP.SELLERCLOUD_SQL.CURRENT_MAIN_INVENTORY_PANDL` and accessible with just a SQL change.

**Action needed before Phase 1b:** Run `SHOW COLUMNS IN TABLE PROD_DG1_GEP.SELLERCLOUD_SQL.CURRENT_MAIN_INVENTORY_PANDL` in sandbox Snowflake to check for kit quantity fields. If absent, kit quantities require a custom Sellercloud integration beyond what our current connector supports.

**Client question (Q8):** Are kit quantities a meaningful volume in GEP2 inventory? (Drives priority of investigation.)

---

### Additional columns available NOW from existing FBA connector (zero connector work)

These columns exist in `CURRENT_REPORT_FBA_INVENTORY` and are not in the sample but are potentially useful additions:

| Column in Snowflake | Description | Add to schema? |
|---|---|---|
| `AFN_INBOUND_RECEIVING_QUANTITY` | Units at FC being received (after Shipped, before Fulfillable) | Q4 client question |
| `AFN_UNSELLABLE_QUANTITY` | FBA units damaged/stranded at FC | Q9 — may supplement non-sellable picture |
| `AFN_RESEARCHING_QUANTITY` | Units in unknown/researching status | Q9 — low priority |
| `MFN_FULFILLABLE_QUANTITY` | Merchant-fulfilled (FBM) available qty | Q9 — only relevant if GEP does FBM |
| `AFN_RESERVED_FUTURE_SUPPLY` | Future supply reserved (AWD replenishment) | Q9 — relevant once AWD is wired |

All of these require only SQL changes to `inventory_fct_current.sql` — zero connector work.

---

### ⚠️ Deprecation Risk

Amazon has issued a deprecation notice for several FBA Inventory reports. Verify whether `GET_FBA_MYI_UNSUPPRESSED_INVENTORY_DATA` is on the deprecation list and confirm the timeline. If deprecated, Phase 1a's core FBA data source needs a migration plan before it breaks.

Reference: [SP-API Deprecation Notice — FBA Inventory Reports](https://developer-docs.amazon.com/sp-api/changelog/deprecation-notice-suppressed-listings-reports-and-fba-inventory-reports)

---

### Phase 1b Summary

| Group | Columns | Resolution path | Effort |
|---|---|---|---|
| A — FBA Reserved | 3 | New Eclipse template only | Very low |
| B — Direct/SCC FBA | 4 | No SP-API source exists; confirm with client | N/A |
| C — CA FBA | 1 | New Eclipse template only (CA already configured) | Very low |
| D — AWD | 2 | New connector category + new permission scope | Medium; separate ticket |
| E — Kit Qty | 3 | Check INVENTORY_PANDL columns first; REST API has no kit qty fields | Investigation needed |
| Bonus columns | 4 | SQL-only additions (already in Snowflake) | Zero |

## Architecture

```
PROD_DG1_GEP.SELLERCLOUD_SQL.CURRENT_MAIN_INVENTORY_PANDL   (GEP2 + GEP2-Returns_Unsellable)
PROD_DG1_GEP.AMAZON.CURRENT_REPORT_FBA_INVENTORY            (US FBA — Phase 1a)
PROD_DG1_GEP.WAREHOUSE.PURCHASING_FCT_BALANCE               (open PO quantities)
                                                             ┐
[Phase 1b sources — connectors TBD]                          │
  SP-API Reserved Inventory Report  (Group A)                │
  SP-API FBA Shipments by Program   (Group B)                │
  CA Marketplace FBA Report         (Group C)                │
  SP-API AWD Inventory              (Group D)                │
  Sellercloud Kit Inventory         (Group E)                │
                                                             ┘
   ↓
WAREHOUSE_SOURCE.INVENTORY_FCT_CURRENT  (secure view)
   ↓
WAREHOUSE.INVENTORY_FCT_CURRENT         (physical table; Phase 1 = full replace)
   ↓
WAREHOUSE.EXTRACT_INVENTORY_CURRENT     (latest snapshot + product dim join)
```

## Column Set (Phase 1)

**Keys / dates**: PRODUCT_ID, PRODUCT_KEY, BALANCE_DATE, BALANCE_DATE_KEY, CAPTURE_TIMESTAMP

**GEP2 / Non-sellable**: GEP2_AVAILABLE_QTY, GEP2_RESERVED_QTY, GEP2_ON_HAND_QTY, NON_SELLABLE_QTY

**US FBA**: US_FBA_AVAILABLE_QTY, US_FBA_INBOUND_WORKING_QTY, US_FBA_INBOUND_SHIPPED_QTY, US_FBA_INBOUND_RECEIVING_QTY, US_FBA_RESEARCHING_QTY, US_FBA_RESERVED_QTY, US_FBA_UNFULFILLABLE_QTY

**Purchasing**: ON_ORDER_GEP2_QTY (SUM over `PURCHASING_FCT_BALANCE` by product)

**Derived**: US_FBA_INBOUND_TOTAL_QTY, TOTAL_AVAILABLE_QTY

**Cost & retail** (Sellercloud-sourced, NULL-preserving): GEP2_LAST_COST, GEP2_LAST_SITE_PRICE, GEP2_AVAILABLE_COST, GEP2_ON_HAND_COST, GEP2_AVAILABLE_RETAIL, GEP2_ON_HAND_RETAIL, NON_SELLABLE_COST, NON_SELLABLE_RETAIL

### Dropped from original spec (no upstream source)

| Column | Reason | Re-introduce when |
|--------|--------|-------------------|
| ITERATION | Feed-specific; no feed | N/A unless feed reinstated |
| AWD_AVAILABLE_QTY / AWD_INTRANSIT_QTY | No AWD report ingested | SP-API AWD endpoints enabled upstream |
| CA_FBA_AVAILABLE_QTY | FBA report has no marketplace/country column | CA-marketplace FBA report enabled |
| PENDING/WORKING/FBM_OFFERED_KIT_QTY | Kit tables hold cost only, no quantities | Sellercloud exposes kit quantity fields |

## Files Modified

| File | Purpose |
|------|---------|
| `GEP/snowflake/warehouse/inventory_fct_balance.sql` | Source view + Phase 1 full-replace table |
| `GEP/snowflake/warehouse/extract_inventory_current.sql` | Latest-snapshot public extract |

## Files to Reference (Do Not Modify)

| File | Why |
|------|-----|
| `GEP/snowflake/warehouse/inventory_fct_historical_balance.sql` (deprecated) | Prior velocity + forward-fill patterns; informs Phase 2 |
| `GEP/snowflake/warehouse/shared_dim_product_base.sql` | MASTER_SKU → PRODUCT_KEY join |
| `GEP/snowflake/warehouse/purchasing_fct_balance.sql` | ON_ORDER_QUANTITY source |
| `GEP/snowflake/warehouse/sales_fct_orderline.sql` | Convention reference |

## QA Checklist (Phase 1)

- Row count: `SELLERCLOUD_GEP2 ∪ SELLERCLOUD_UNSELLABLE ∪ AMAZON_FBA` by PRODUCT_ID should match fact row count
- PRODUCT_KEY join rate to `SHARED_DIM_PRODUCT`: alert if < 95%
- Null rate: 0 for mandatory fields (PRODUCT_ID, BALANCE_DATE)
- Duplicates: zero tolerance on PRODUCT_ID (Phase 1 current-only) — `QUALIFY ROW_NUMBER()` handles multi-row SKUs upstream
- Cross-validation: totals for GEP2_ON_HAND_QTY, US_FBA_AVAILABLE_QTY should reconcile with legacy `inventory_fct_historical_balance` latest rows
- Negative reserved values: passthrough unchanged (both Sellercloud GEP2 and FBA). Log only — they represent legitimate upstream adjustments. Revisit with an observability flag column if the rate exceeds ~1% of rows. As of 2026-04-16 Phase 1 test: 1 row across entire fact.

## Moving-Target Source Tables — Design / Build / Test Implication

See [[accumulating-source-tables]] for the generalized pattern and QA workflow. Below is the specific finding from this ticket.

**Finding (2026-04-16, Phase 1 QA):** `PROD_DG1_GEP.AMAZON.CURRENT_REPORT_FBA_INVENTORY` accumulates multiple timestamped versions of each SKU — it is not a single-snapshot table despite the `CURRENT_` prefix. `___ALDC___GLOBAL_HISTORY_TIMESTAMP_START___` distinguishes batches; the same SKU can appear multiple times across batches.

Our Phase 1 view resolves this by picking the latest per SKU at **query time** (not pinned to a snapshot):

```sql
QUALIFY ROW_NUMBER() OVER (PARTITION BY SKU ORDER BY ___ALDC___GLOBAL_HISTORY_TIMESTAMP_START___ DESC) = 1
```

### What this means for testing

A CTAS built at time T and a direct reconciliation query run at time T+N will disagree if the source ingested new batches between T and T+N. During QA we observed:

- **8-hour gap** between CTAS and re-reconciliation → 0.8% drift across FBA measures, with directional signs matching real inventory flow (working↓, shipped↑, available↑, reserved↑)
- **0-second gap** (rebuilt and immediately queried) → 0.18% drift remained, ~1 SKU and ~585 units across 316K, attributable to non-deterministic tie-breaking inside same-timestamp batches

**Practical test tolerance**: ≤1% unit delta, ≤5 SKU delta is acceptable for Phase 1. Beyond that indicates a real bug.

### What this means for design

- Phase 1's "latest at query time" is correct for a current-snapshot fact and cheap to build, but makes deterministic reconciliation impossible — don't block on 100% match
- Phase 2 append-only ingests each new `___ALDC___GLOBAL_HISTORY_TIMESTAMP_START___` batch on arrival; rows are frozen once written. Reconciliation becomes bit-exact because the fact pins to a specific capture timestamp rather than recomputing "latest"
- The accumulating source table is a **gift** for Phase 2 — we don't need to stand up a new history mechanism; we read history from the existing table using `WHERE ___ALDC___GLOBAL_HISTORY_TIMESTAMP_START___ > (SELECT MAX(CAPTURE_TIMESTAMP) FROM fact)`

### Build/test runbook addition

- Always run the **timestamp-drift diagnostic (Q7a)** before interpreting reconciliation output
- Rebuild the CTAS and run reconciliation **in the same worksheet session, back-to-back**, to minimize drift
- Flag any drift > 60 seconds in the diagnostic before drawing conclusions from measure totals

## Sample Feed Reference (Iteration 46, 2026-03-24)

GEP shared a 100-row sample (`GEP/_testing/GP-208/sample_inventory_data.csv`, gitignored). Full analysis in `GEP/_testing/GP-208/sample_analysis.md`. Key takeaways:

- Feed is `MASTER_SKU × CaptureDate` — matches our grain (PRODUCT_ID resolves to MASTER_SKU via `SHARED_DIM_PRODUCT`)
- Reverse-engineered aggregates:
  - `Total US FBA Available` = `US FBA Available` + `US FBA Rsrvd Trnsfr` (transfer-reserved counts as available)
  - `Total US FBA InTransit` = sum of all Working + Shipped across US/Direct/SCC programs + `AWD InT`
  - `Total Non Sellable` = `Non Sellable Qty` + `US FBA Rsrvd Prcss` + `US FBA Rsrvd Orders`
- Reserved-column 3-way split (Orders / Prcss / Trnsfr) is **not in our raw Amazon report** — `AFN_RESERVED_QUANTITY` is a single lump. Sourcing the split requires Amazon SP-API Reserved Inventory Report (not currently ingested)
- `Direct FBA` and `SCC FBA` program columns are all zero in sample — likely unused; SCC ties to AWD
- AWD and CA FBA do populate for some rows (not just placeholders) — worth prioritizing if GEP asks

## Follow-ups

- Add `TOTAL_NON_SELLABLE_QTY` derived column to match the feed's aggregation shape (= NON_SELLABLE_QTY + US_FBA_RESERVED_QTY + US_FBA_UNFULFILLABLE_QTY)
- Research Amazon SP-API AWD endpoints — can we pull AWD Available / Inbound without GEP involvement?
- Research Amazon SP-API Reserved Inventory Report for Orders/Prcss/Trnsfr split
- Research whether `CURRENT_REPORT_FBA_INVENTORY` can be configured for CA marketplace
- Phase 2: switch table refresh to incremental `INSERT` + add `CAPTURE_TIMESTAMP` uniqueness constraint
- Consider a downstream `inventory_fct_velocity` view for 7/30-day velocity (previously in `inventory_fct_historical_balance`)

## Cross-cutting Concerns Raised by This Ticket

- [[snowflake-data-share-refresh]] — operational concern: producer-side `CREATE OR REPLACE TABLE` semantics and their visibility to share consumers. Raised during Phase 1 deploy planning when observing PROD `PURCHASING_FCT_BALANCE` was recreated ~hourly
- [[accumulating-source-tables]] — data-modeling concern: `CURRENT_REPORT_*` tables retain multiple historical batches per natural key; consumers must dedup explicitly

## See Also

- [[gep-inventory-data-dictionary]] — durable column-by-column reference for the fact and extract this ticket produced
- [[snowflake-data-share-refresh]] — share refresh atomicity
- [[accumulating-source-tables]] — moving-target source table pattern
- [[GEP]] — client entity page
- [[star-schema-convention]] — ALDC warehouse naming patterns
- [[data-pipeline-flow]] — Eclipse → Snowflake → PBI pipeline
- [[gep-snowflake-pbi-deployment]] — full deployment runbook
- [[Snowflake]] — tool entity page
