---
tags: [ticket, gep, navira, pbi, dashboard, sku-profitability, cogs, orders, returns, cost-override, return-logic, in-progress]
aliases: [GP-259, Orders Return COGs, SKU Profitability COGs]
sources: []
created: 2026-05-22
updated: 2026-06-16
---

# GP-259 — Orders/Return and COGs for SKU Profitability

Add Orders, Return, and COGS data to the [[GEP]]/Navira SKU Profitability Dashboard for full margin analysis. Jira: GP-259 (status **Development / In Progress**, assignee Paul). Sent 2026-05-06, original ETA 2026-05-18 (passed).

> **✅ Option A DEPLOYED + validated end-to-end in TEST (2026-06-16).** SC source-view blank-COGS backfill live (SYSADMIN + COPY GRANTS), orderline chain rebuilt, **$21.25M recovered, zero regression**, consumer **SQL==DAX byte-exact** (Order Line `[Actual - Cost - COGS]` = $58,135,081.63 == Snowflake fact). PROD promotion separate/gated (Navira sign-off + archive-partition reprocess). See 2026-06-16 session-log entry.

## Status (2026-06-10) — VERIFIED against live PROD

State now confirmed against live PROD (read-only). Net: the COGS work is sound and consistent; the only real remaining data-layer task is exposing return units for the Return Rate KPI.

- **Issue A — COGS dashboard ≠ warehouse: DISPROVEN as a data issue.** Catalog-wide, the dashboard reporting view (`WAREHOUSE.EXTRACT_SALES_DETAIL`) COGS total = warehouse fact (`WAREHOUSE.SALES_FCT_ORDERLINE`) COGS total = **$36,383,905.12, delta $0.00**. CIBU SKUs match penny-for-penny (delta $0.00). Any *visible* mismatch on the dashboard is the **Datavize frontend product-info header card** (shows $0.00/$15.21 instead of the real per-unit cost) — a frontend defect, route to Datavize; not a data/warehouse problem.
- **Issue B — Return Rate KPI: data/measure layer DONE; only frontend placement remains.** Traced the consumer binding (CosmosDB `core` → `explorer_dashboard` → `dataset`): all 38 NAVIRA dashboards (incl. "NAVIRA External", "CIBU") use one dataset **"Sales Model"** (`type=data_model`) bound to **Power BI `GEP Prod Models` → "Data Model"** (workspace `de58032f…`, dataset `74a529b3…`). It does **not** reference `EXTRACT_SALES_DETAIL`. The model already exposes measure **`Actual - Sales - Return Rate %`** (desc: *"GP-256: Unit-based return rate…"*) plus `Returns (Quantity)`, `Gross (Quantity)`, `Refunds`, `Cost - Return Label`, and the `Return Reason` column. **So the `EXTRACT_SALES_DETAIL` column add is NOT needed — do not deploy it.** Remaining blocker = frontend placement of the existing measure on the SKU Profitability surface (Datavize if custom navira-demo; self-serve via Explorer visual editor if standard Explorer).
- **Minor margin gap (separate, do not bundle):** the extract's `MARGIN_NET_CONSOLIDATED` has `// - RETURN_LABEL_FEE_CONSOLIDATED` commented out, while the fact's margin subtracts it → extract margin ≠ fact margin by the return-label fee. Flag only; not part of Heather's ask.
- **Rollback captured:** `aldc-launchpad/warehouse_ops/_gp259_issueB_ROLLBACK_{TEST,PROD}.sql` (verbatim current DDL of both EXTRACT_SALES_DETAIL views).

## Consumer chain (traced — where the dashboard reads from)

From the Lori COGS note (`aldc-launchpad/cogs-fix-note-lori.md`):

```
navira-demo frontend (Eclipse app "SKU Profitability", iframes navira-demo.analyticlabs.io)
  → Explorer "NAVIRA" dashboard (CosmosDB core.explorer_dashboard, account da8904db)
    → dataset "Sales Model"
      → Power BI "GEP Prod Models"
        → WAREHOUSE.SALES_FCT_ORDERLINE   (COGS visual measure: `Actual - Cost - COGS`)
        → WAREHOUSE.EXTRACT_SALES_DETAIL  (flat reporting view; the GP-259 COGS override target)
```

- The navira-demo frontend is **Datavize-owned** (Marshall Johnston; Steven Deutekom ALDC) — its source is NOT in any ALDC-accessible repo. **(Clarified 2026-06-18: that's the *live* surface. ALDC also has a separate concept frontend, [[concept-gep-data]], on the same data — a likely future replacement. "SKU Profitability" now names two distinct apps.)** Frontend-only display defects (e.g. the product-info header "COGS:" card showing $0.00/$15.21 instead of the real $12.82) must be routed to Datavize.
- Memory anchor: "PBI 'Order Line' = WAREHOUSE.SALES_FCT_ORDERLINE".

## COGS architecture (verified deployed state)

The CIBU cost override was deployed the **good** way — upstream, not via a reporting-view gross-qty patch:

- `WAREHOUSE.SALES_FCT_ORDERLINE` is a materialized **TABLE** carrying all return columns + return-adjusted COGS/Net (built from the source views).
- `SUPPLEMENT.COST_OVERRIDE_MANUAL` holds the 5 CIBU SKUs (12.82/12.25/11.84/26.91/26.91; added 2026-05-29). The override feeds the **per-item cost fallback in the source view** (`_gp259_bridge2_DEPLOY_scview.sql:198-199`), so it flows into the fact and inherits the normal return-adjusted COGS path. Verified: 30004 = 69 units × 12.82 = $884.58 in the fact.
- `WAREHOUSE.EXTRACT_SALES_DETAIL` = thin secure view `SELECT * FROM WAREHOUSE_SOURCE.EXTRACT_SALES_DETAIL`; the source extract just passes `ORDERLINE.SALES_COGS_CONSOLIDATED` straight through from the fact — **no gross-qty override CASE**.
- The earlier `_gp259_extract_override.modified.sql` / `EXTRACT_SALES_DETAIL_GP259_SHADOW` was a **shadow draft only — never deployed** (confirmed: object does not exist in PROD). My earlier "gross-qty override causes a mismatch" hypothesis was based on that shadow file and is **disproven** — the deployed path uses the upstream override, so dashboard COGS = warehouse COGS by construction (delta $0.00 catalog-wide).

> **Contradiction resolved (vs the 2026-06-10 first-pass note below):** there is no COGS data mismatch. The only "mismatch" anyone sees is the Datavize frontend header card.

## Return logic EXISTS (CCE correction)

CCE concluded return-adjusted Net/COGS "was never built" and proposed building it in a dbt gold layer. **That is wrong on all counts.** The return logic is live in `WAREHOUSE.SALES_FCT_ORDERLINE` (`_gp259_fct_orderline_src.sql`):

- **Net = Gross − Refunds** (`:199-205`)
- **COGS qty = gross − returns** (FBA: `gross − sellable_returns`, so damaged/defective returns still cost) (`:238-244`)
- **Return label fee** subtracted from margin (`:304-305, :324`; sourced from Amazon `LABEL_COST` `:429`)
- Returns sourced from Amazon reports `CURRENT_REPORT_RETURN` (MFN) + `CURRENT_REPORT_FBA_RETURN` (FBA disposition), `COALESCE`'d over SellerCloud (`:402, :651-712`)

Why CCE couldn't see it: it only has Zeus Memory — no access to the (local/uncommitted) warehouse SQL and cannot introspect a deployed Snowflake secure view. The logic is in the **warehouse transformation layer**, not DAX and not dbt (GEP's warehouse does not use dbt). No return-adjusted DAX measure is needed because Net/COGS arrive pre-adjusted.

## Deployed state (VERIFIED 2026-06-10, live PROD, read-only)

- Return-adjusted COGS/Net **live** in `WAREHOUSE.SALES_FCT_ORDERLINE` (data proof: 120,846 lines with returns, 79,724 units backed out of COGS, $7.3M refunds in PROD). Return logic exists — CCE was wrong.
- CIBU override **deployed upstream** via `SUPPLEMENT.COST_OVERRIDE_MANUAL` (5 rows, 2026-05-29). Fact + extract both show corrected COGS.
- Dashboard reporting view `WAREHOUSE.EXTRACT_SALES_DETAIL` COGS = fact COGS **catalog-wide ($36,383,905.12, delta $0.00)** and per CIBU SKU (delta $0.00).
- `EXTRACT_SALES_DETAIL` is **missing `SALES_RETURNS_QUANTITY`** (has `SALES_GROSS_QUANTITY`) → Issue B.
- Verifiers: `warehouse_ops/_gp259_verify_return_logic.py`, `_gp259_issueA_proof.py`. Rollback: `_gp259_issueB_ROLLBACK_{TEST,PROD}.sql`.

## Heather's spec (re-checked from email — belongs to GP-256)

Thread "New Return Rate KPI's for external dashboards" (Heather Tabor, Navira COO; Lori Beck; Paul cc'd). Heather confirmed **unit-based** (2026-05-04):
- **KPI 1 — Return % per SKU:** `SALES_RETURNS_QUANTITY ÷ SALES_GROSS_QUANTITY × 100`; red when > 0; matches Amazon Seller Central.
- **KPI 2 — Return Rate at Brand level:** `SUM(brand returns) ÷ SUM(brand units) × 100` — sum first, then divide.
- 2026-05-29 Lori: "available now" (measures added to model). 2026-05-30 Heather: "I still do not know how to get it here. All others look good." → placement/passthrough gap on SKU Profitability.
- **Issue B fix:** expose `SALES_RETURNS_QUANTITY` in `EXTRACT_SALES_DETAIL` (it already carries `SALES_GROSS_QUANTITY`) so the KPI can render at the layer the dashboard reads.

## Next actions

1. **Issue B — NO warehouse change.** `EXTRACT_SALES_DETAIL` column add is unnecessary (dashboards bind to PBI `GEP Prod Models`, which already has the measure + fields). The `_gp259_issueB_DEPLOY.sql` / `_ROLLBACK_*.sql` artifacts can be parked/deleted.
2. **Issue B — frontend placement only.** Place `Actual - Sales - Return Rate %` (per-SKU) + a brand-level card on the SKU Profitability surface. Datavize (Marshall Johnston / Steven Deutekom) if it's the custom navira-demo frontend; self-serve via the Explorer visual editor if it's the standard Explorer dashboard. Confirm which surface with Lori/Heather.
3. **Issue A:** no warehouse change. Route the frontend product-info header-card COGS defect to **Datavize**.
4. Optional, separate: decide whether to add `RETURN_LABEL_FEE_CONSOLIDATED` back into the extract's margin (changes margin values — own validation + sign-off). Note: the model already has `Actual - Cost - Return Label` as a measure.
5. **Option A (catalog-wide backfill) — now has a concrete TEST-deploy runbook** (2026-06-12): `aldc-launchpad/boot-prompts/navira-gp259-cogs-backfill-optionA.md`. Still deferred for **PROD** pending Navira sign-off — but the plan is to **deploy in TEST** (evidence-gated, shadow→no-regression+~$21M-recovery proof→rollback→gated fact rebuild) for two reasons: (a) it lifts the new Contribution-Margin layer's ~83% COGS-coverage caveat, and (b) the TEST **before/after evidence pack is what unblocks Navira's Option-A sign-off**. Option A is also the explicit **gate for the marketing efficiency + Contribution-Margin stack PROD repoint** (see [[cross-channel-marketing-attribution]] § PROD promotion gate). ~30.2% of lines ($43.1M gross, mostly pre-2024-05-30) still carry no COGS → margins overstated until it lands. [[GP-PENDING-missing-cogs-cost-history]].

## Related

- [[GP-256]] — Return Rate KPIs (Done; the Heather thread; shares navira-demo frontend/passthrough blocker)
- [[GP-PENDING-missing-cogs-cost-history]] — catalog-wide blank-COGS backfill (Option A, deferred)
- [[GEP]] — Client
- [[data-pipeline-flow]] — Where the product table / facts sit in the pipeline

## Session Log

### 2026-05-22 — Investigation

- Confirmed COGS measure (`Actual - Cost - COGS`) and Orders measures already exist in GEP Prod PBI model — no new measure deployment needed.
- Same navira-demo frontend blocker as GP-256: hardcoded column names prevent new measures from appearing.
- **Next**: Bundle frontend update with GP-256 Return Rate % column update.

### 2026-05-28 — Note: distinct "Missing COGs" data issue split out

- Heather's "Missing COGs" request (5 CIBU SKUs) led to a separate, catalog-wide investigation of **blank COGS data** (≠ this ticket's *measures* scope). Root cause = cost-history horizon + cost-field/timing; fix validated (back-fill + field fallback). Tracked separately at [[GP-PENDING-missing-cogs-cost-history]].

### 2026-06-10 — COGS-mismatch root cause + return-logic confirmation

- **CCE was wrong**: return-adjusted Net/COGS *do* exist, live in `WAREHOUSE.SALES_FCT_ORDERLINE` (warehouse SQL layer, not DAX/dbt). Proven from source. CCE simply can't read warehouse SQL or deployed secure views.
- Mapped the **two-layer COGS architecture** and identified the likely **Issue A** root cause: `EXTRACT_SALES_DETAIL` override uses **gross qty**, the fact uses **return-adjusted qty** → dashboard COGS > warehouse COGS on overridden SKUs that have returns.
- Recommended structural fix: move override upstream (source-view per-item cost fallback, `_gp259_bridge2_DEPLOY_scview.sql`) so COGS flows through the return-adjusted path and dashboard = warehouse.
- Re-checked Heather's email thread → Return Rate KPI spec is unit-based and belongs to [[GP-256]]; **Issue B** = expose `SALES_RETURNS_QUANTITY` in `EXTRACT_SALES_DETAIL`.
- Wrote verifier `warehouse_ops/_gp259_verify_return_logic.py`. **Blocked on Snowflake creds** (`TEST_PW`/`PROD_PW` not set) — deployed PROD/TEST state UNVERIFIED. No prod change until verified (evidence-gated).
- Option A (catalog backfill) remains deferred.

### 2026-06-10 (later) — VERIFIED against live PROD; Issue A disproven

- Ran `_gp259_verify_return_logic.py` + `_gp259_issueA_proof.py` against live PROD (creds from wiki `vault/infra-credentials.md`).
- **Return logic PROVEN live** (data): returns reduce COGS qty, Net = Gross − Refunds; 120,846 return lines / 79,724 units backed out / $7.3M refunds in PROD. CCE definitively wrong.
- **Issue A disproven:** CIBU override was deployed **upstream** (`SUPPLEMENT.COST_OVERRIDE_MANUAL` → source view → fact), NOT via the gross-qty shadow override (which was never deployed and does not exist). Dashboard COGS = warehouse COGS catalog-wide (delta $0.00). The remaining "mismatch" is the Datavize frontend header card only.
- **Issue B confirmed:** `EXTRACT_SALES_DETAIL` lacks `SALES_RETURNS_QUANTITY` → blocks the Return % KPI. Fix = passthrough column add, TEST→PROD with approval.
- **Rollback captured** for both envs (`_gp259_issueB_ROLLBACK_{TEST,PROD}.sql`). No mutations made — awaiting Paul's go-ahead.

### 2026-06-12 (later) — Option A built + validated in TEST (evidence pack DONE)

- Option A validated non-destructively in TEST: **$20.5M COGS recovered / 1,024,475 lines, zero regression (0 per-item mismatches/2.77M), CIBU 5 SKUs auto-recover exactly $12.82/12.25/11.84/26.91/26.91 with no override table.** Coverage 54.5%→86% whole-fact (99.97% of sales-bearing SC lines). This is the artifact that unblocks Navira's Option-A sign-off. Full detail in [[GP-PENDING-missing-cogs-cost-history]] § 2026-06-12. Live TEST deploy DEFERRED pending an intermittent UK-share dependency.
- **Side-finding → [[GP-282]]:** Amazon/SellerCloud dedup — US branch in `SALES_DIM_ORDER_BASE` anti-joins SellerCloud (clean), UK branch (GP-200) does NOT → 1,154 fact lines / **$137K (0.11%) double-counted**. Pre-existing. Repo: `aldc-launchpad/warehouse_ops/_gp259_optionA_FINDINGS_amazon_sellercloud_dedup.md`.
- **Side-finding → [[GP-281]]:** cost-value feed stall 2026-03-03 — product SITECOST/LANDEDCOST stopped changing (likely ALDC Library cost integration, same day as exchange rates). Next workstream w/ GP-282. See [[GP-PENDING-missing-cogs-cost-history]].

### 2026-06-15 — Option A blank-set RE-MEASURED on live cost-history (post [[GP-281]])

- [[GP-281]] repointed `WAREHOUSE.SALES_FCT_COST_HISTORY` to the live share (MAX 2026-03-03 → 2026-06-15). Re-measured the Option-A blank set against the now-live history (read-only, on the existing fact — no share-order dependency; `warehouse_ops/_gp259_optionA_REMEASURE_live.py`).
- **Result: recovery scope UNCHANGED at ~$20.5M / ~1.02M lines.** The blank set is **99.4% pre-March-2026 historical orders** (1,018,936 of 1,024,964 SC blank lines), most predating the cost-history horizon (2024-05-30). Live history natively covers only **4.7% / $7,120** — Option A's earliest-known-cost backfill remains necessary, and earliest-known cost per product is unchanged by the freeze (which hit recent, not historical, entries). The $20.5M figure stands.
- Live TEST deploy of Option A still gated on the same intermittent Amazon-orders share window as the GP-281 orderline rebuild ([[GP-PENDING-data-share-stability]]). Evidence pack updated: `warehouse_ops/_gp259_optionA_EVIDENCE_PACK.md`.

### 2026-06-12 — Option A scoped as a TEST deploy (gates Contribution-Margin PROD)

- New Tier 3.5 **Contribution Margin** layer shipped to TEST ([[cross-channel-marketing-attribution]]) reads the same `WAREHOUSE.SALES_FCT_ORDERLINE` COGS → its margin is **coverage-gated at ~83%** (0% pre-May-2024). Option A is the fix.
- **Decided:** deploy Option A **in TEST** (evidence-gated) — it both sharpens the margin layer and produces the before/after evidence Navira needs to approve Option A for PROD. Runbook: `aldc-launchpad/boot-prompts/navira-gp259-cogs-backfill-optionA.md`.
- **Layer (re-affirmed):** the backfill goes in the **source-view per-item cost resolution** (the CIBU-override path), NOT the extract/reporting view — the documented wrong-layer hazard. As-of cost-history + site→avg→last field fallback (`_gp259_costhist_view.sql`).
- **PROD gate:** Option A landing (or a prominent coverage caveat) is one of the two conditions for the efficiency/margin-stack PROD repoint; the other is GEP/Navira design sign-off in TEST.

### 2026-06-10 (consumer trace) — Issue B needs NO warehouse change

- Traced CosmosDB `core` (prod `aldcprodcsdb1c01`): `explorer_dashboard` (38 NAVIRA dashboards, `account_id=da8904db`) → all share dataset `e80ffd34…` **"Sales Model"** (`type=data_model`) → bound to **Power BI `GEP Prod Models`** (workspace `de58032f…`, dataset "Data Model" `74a529b3…`). Confirmed PBI service (`SERVICE_POWER_BI`) reads `WAREHOUSE.SALES_FCT_ORDERLINE` (not `EXTRACT_SALES_DETAIL`).
- Model already exposes **`Actual - Sales - Return Rate %`** (desc cites GP-256, unit-based) + `Returns (Quantity)`, `Gross (Quantity)`, `Refunds`, `Cost - Return Label`, `Return Reason`.
- **Conclusion:** the measure + data are live and surfaced in the Explorer dataset; the only gap is placing the measure on the SKU Profitability visual. `EXTRACT_SALES_DETAIL` column add abandoned as unnecessary. Routes to frontend (Datavize) or self-serve Explorer editor.
- Cosmos creds: wiki `vault/core-api-local-settings.md` (prod `COSMOS_*`).

### 2026-06-16 — Option A DEPLOYED + validated end-to-end in TEST

- Unblocked by the GP-281 self-heal (Amazon-orders share now stable). Re-validated Option A on the now-live cost-history first (battery `_11`): 0 per-item mismatch on covered lines, **$21.25M recovered**, coverage 53.9%→86.1%, CIBU 5 SKUs exact.
- **Deployed** a LIVE SC source-view edit (`WAREHOUSE_SOURCE.SALES_FCT_SELLER_CLOUD_ORDERLINE`): cost-CASE backfill (COST_EARLIEST CTE + branches 3/4 + join), built **column-signature-identical** to the live view (omitted the `COGS_SOURCE_GP259A` transparency column to avoid downstream UNION risk), as **SYSADMIN + COPY GRANTS** (grants preserved). Compile-test gate passed first. Builder `warehouse_ops/_gp259_optionA_build_LIVE_scview.py`; rollback `_gp259_optionA_ROLLBACK_TEST_scview_FRESH.sql`. Commit `ca228d9`.
- **Rebuilt** orderline chain (`TASK_WAREHOUSE_ORDERLINE_0..9`). Fact: SC COGS +$21.25M (delta == blank-line recovery → covered lines unchanged, no regression); residual blanks = correct return-adjusted-zero (69,900) + 286 genuine no-cost; CIBU exact.
- **Consumer (PBI/DAX 66151728):** SQL==DAX byte-exact — CM COGS $36,960,451.18; Order Line `[Actual - Cost - COGS]` = **$58,135,081.63 == Snowflake fact exactly (3,264,190 rows)**.
- **Incremental-refresh lesson:** the Order Line table is incremental (`RangeStart=2025-07-01`). A normal refresh AND a table-level `RequestRefresh(Full)` did NOT pull the historical restatement (archive partitions frozen). Required a **partition-level `ProcessFull` across all 97 partitions** (`pbi_ops/_gp259_optionA_reprocess_orderline_archive.py`) to land it. Source inspector: `_gp259_inspect_orderline_source.py`. **Must repeat for PROD promotion.**
